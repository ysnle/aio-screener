# AIO Deep QA Report — 2026-05-05

Scope: UI/UX/rendering, data/API pipeline integration, and page-level data/function/analysis consistency.

## Baseline

- Local audit branch: `v48.77` files in `C:\Users\zmfhd\.codex\worktrees\bc2d\AIO`.
- Live GitHub Pages at audit time: `v48.79` from `https://ysnle.github.io/aio-screener/`.
- Browser QA matrix: 21 pages x 2 viewports (desktop 1440x920, mobile 390x844) for both local and live.
- Static source scope: `index.html`, `js/*.js`, `cloudflare-worker-proxy.js`, `sw.js`.

## 1. UI/UX/Rendering

| Target | Pages visible | Missing handlers | Missing page targets | Horizontal overflow | Mobile clipping | Mobile overlaps |
|---|---:|---:|---:|---:|---:|---:|
| Local | 42/42 | 0 | 0 | 0 | 0 after P149 | 0 after P149 |
| Live | 42/42 | 0 | 0 | 0 | 1 (`theme-detail`) | 1 (`home`) |

Findings:
- Local P149 fixed the mobile API onboarding link/cancel overlap.
- Local P149 fixed mobile `#chat-theme-detail-chips` clipping by allowing wrapping for that chip row.
- Sentiment canvases can look blank in canvas-only checks because LightweightCharts hides the original canvas and renders into `.lwc-chart-container`. LWC containers were present with nonzero child canvases.
- TradingView still emits `cannot_get_metainfo`; page rendering remains visible, but the external widget is noisy and should remain treated as a third-party risk.

## 2. Data Pipeline/API Connectivity

Static inventory:
- Pages: 21
- Distinct `data-action`: 133
- URL/domain references: 132 hosts
- Fetch-related call sites: 153
- User-facing key names: `aio_av_key`, `aio_finnhub_key`, `aio_fmp_key`, `aio_fred_key`, `aio_td_key`, `aio_newsdata_key`, `aio_bok_key`, `aio_kosis_key`, `aio_rss2json_key`, `aio_cf_worker_url`, plus LLM/search keys.

Runtime network observations:
- `m.stock.naver.com` direct calls fail by CORS in browser and rely on proxy/fallback handling.
- `query1.finance.yahoo.com` direct calls fail by CORS in browser; proxy paths (`api.codetabs.com`, `corsproxy.io`, etc.) provide partial recovery.
- Public RSS/news proxies (`allorigins`, `corsproxy.org`, `api.cors.lol`) remain intermittent.
- FRED chart section is expected to show setup guidance without `aio_fred_key`; no-key state is not a rendering failure.
- BOK/KOSIS use `_getApiKey()` direct-key fetchers rather than the central `DATA_APIS` registry; this works but is less uniform than the FRED/Finnhub/FMP pattern.

Pipeline conclusion:
- Local UI does not dead-end when external sources fail; major user-facing flows have static/proxy fallback.
- The biggest remaining structural risk is noisy direct-first CORS failure for Korean/Naver and Yahoo endpoints. Functionally tolerated, but it inflates console errors and can slow perceived loading.

## 3. Page-Level Consistency

| Page | Data/function/analysis status |
|---|---|
| `home` | Local mobile onboarding layout fixed. Snapshot/live cards render; API onboarding communicates missing-key state. |
| `signal` | `toggleSignalMode()` class state fixed locally (P147). Scoring/fallback data renders; live still fails this class-state check. |
| `breadth` | Breadth charts render in targeted chart QA; no missing actions/targets. |
| `sentiment` | LWC route verified; canvas-only blank checks are false positives when original canvas is hidden. |
| `briefing` | Uses news cache/proxy pipeline; no dead page issue, but external RSS proxies remain intermittent. |
| `technical` | Page visible; TradingView widget remains third-party noisy. |
| `macro` | Yield curve chart works; FRED chart area correctly requires API key before rendering live FRED series. |
| `fxbond` | Page routing and actions valid; macro/fx values depend on liveData/static fallback. |
| `fundamental` | NVDA search flow passed; SEC/FMP/Yahoo/Finnhub integration is gated by keys/fallbacks. |
| `themes` | RRG and sector 20d chart verified. P148 adds all-fetch-failed fallback for sector 20d. |
| `theme-detail` | Mobile chip clipping fixed locally (P149). |
| `portfolio` | Local benchmark uses `coveredSymSet` and avoids missing failed top holdings (P144). Live still has `topSymSet`. |
| `ticker` | Chart flow passes after activating chart tab; fallback synthetic chart path exists. |
| `market-news` | Handlers valid; data quality depends on RSS/proxy success plus NewsData/Finnhub optional keys. |
| `options` | Page visible; CBOE/options external requests can fail and fall back to VIX-derived estimates. |
| `kr-home` | Page visible; Naver direct calls fail by CORS but fallback/static values prevent blank UI. |
| `kr-supply` | Page visible; Naver investor trend direct calls are noisy and should prefer proxy-first in future. |
| `kr-themes` | Page visible; Korean quote pipeline depends on Naver/proxy/static fallback. |
| `kr-macro` | BOK/KOSIS key-gated live data; static fallback values keep analysis visible. |
| `kr-technical` | VKOSPI chart passed targeted canvas QA. One automated `undefined` token flag was not reproducible in direct DOM snippet capture and is classified as false-positive/ephemeral unless it reappears with a captured snippet. |
| `guide` | Page visible; actions/targets valid. |

## Local-vs-Live Differences

- Live is currently `v48.79`, not the earlier observed `v48.77`.
- Local audit branch still contains fixes not reflected by the live v48.79 script checks:
  - P144 portfolio benchmark coverage fix present locally, absent live.
  - P147 signal mode active class fix present locally, absent live.
  - P149 mobile onboarding/theme-chip layout fixes present locally, absent live.

## Recommended Next Hardening

1. Make Korean/Naver and Yahoo chart paths proxy-first on GitHub Pages to reduce direct CORS failures.
2. Normalize BOK/KOSIS/NewsData/FMP into a single API registry/status model so settings, quotas, and UI badges are consistent.
3. Keep chart QA renderer-aware: Chart.js canvases, LWC containers, TradingView iframes, and key-gated FRED charts need different pass criteria.
