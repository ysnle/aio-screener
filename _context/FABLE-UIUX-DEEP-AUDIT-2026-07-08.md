---
verified_by: agent (Fable 5 — 라이브 v52.26 실브라우저 실측 + 외부 API 대조 + 계산형 시각 검증)
last_verified: 2026-07-08
confidence: high (단 §0 환경 제약으로 "시각 렌더링 픽셀 확인"과 "뷰포트 매트릭스"는 명시적 미완 — 해당 항목은 그 등급대로만 신뢰)
target_version: v52.26 (live, 2026-07-07T20:15+09 배포본)
purpose: UI/UX 프론트엔드 심층 감사 — QA-CHECKLIST 실브라우저 미확인 백로그 일괄 검증 + 기존 감사(07-07)의 미점검 표면 6곳 커버 + 신규 발견 대장 + 구조 개선 설계 Phase V0~V4. FABLE-LIVE-AUDIT-2026-07-07.md의 후속·보완재.
---

# FABLE-UIUX-DEEP-AUDIT-2026-07-08 — UI/UX 심층 실측 + 구조 개선 설계

> 발단: 사용자 질문 "UI/UX 프론트엔드를 세밀하게 다 진단했는가?" → 커버리지 공백 4층위 식별(§0 참조) → "모두 진행" 지시로 본 감사 실행.
> 이 문서는 (A) 검증 결과 원장(§1), (B) 신규 발견 대장(§2~4), (C) 구조 개선 설계(§5~6)를 담는다.
> **기존 L0~L5 로드맵(FABLE-LIVE-AUDIT-2026-07-07)을 대체하지 않는다** — L5-2 표시 잔무 등은 그대로 유효하며, §5의 V-Phase가 그 위에 얹힌다.

## §0 방법론 · 환경 제약 (정직표)

- **감사 시각**: 2026-07-07 21:45 ~ 07-08 00:15 KST (KOSPI 폭락일 종가 확정 후, 미국 프리장~개장 직후).
- **대상**: 라이브 `https://ysnle.github.io/aio-screener/` = v52.26 (로컬 리포와 동일 커밋, origin 동기화 확인).
- **방법**: Chrome 확장 + JS 직접 실행(DOM/상태/차트 내부), 외부 실측 대조(Naver KOSPI/KOSDAQ·Yahoo VIX/VIX9D), 콘솔/에러 계측, 로컬 게이트 2종 실행(runtime-contract·ux-default-path 모두 green).
- **환경 제약 (재감사 시 참고)**:
  1. **탭이 대부분 hidden(백그라운드) 상태** → 스크린샷 CDP 스톨, rAF 정지, setTimeout ≥1s 스로틀. **시각 판정을 전부 계산형으로 대체**: SVG 라벨 겹침=getBBox 교차, 차트 상태=Chart.getChart() 내부 데이터, 레이아웃=rect 교차. 픽셀 단위 색상 확인(U9 게이지 색 등)은 미완.
  2. **resize_window가 뷰포트에 반영 안 됨**(innerWidth 1584 고정 — 숨김/최소화 창 제약) → 390/768 반응형 실측 실패. **뷰포트 매트릭스는 CI 헤드리스로 이관**(§5 V3). 창은 1600×900으로 복원해 둠(원래 최소화 상태로는 복원 불가).
  3. **확장 보안 필터**가 URL/쿼리 접근성 코드 패턴을 차단 → resource URL·SW scriptURL 미수집(전송량은 ARCH 정적 실측 2.1MB 인용).
  4. **AI 채팅 실문답은 1회 시도 후 차단**: "AI 답변을 쓰려면 Claude 키를 저장하세요" — 클라 키 필요 경로라 운영자 키 조작 불가(금지 영역). P629 라이브 검증 불가(§2 UX-04로 승격).
  5. **포트폴리오 실등록 조작 배제**: PIN 잠금 + 실보유 데이터 존재 → 파괴 리스크. 위젯 계약(workbench/backtestLab/저널 액션 22종 DOM) 존재 확인으로 대체.
- **외부 실측 기준값**: KOSPI 종가 7,656.31 (-395.02, -4.91%, 전일 8,051.33, CLOSE) · KOSDAQ 831.23 (-1.87%) · VIX 15.93 · VIX9D 12.32 (콘탱고).

## §1 검증 결과 원장 — QA 미확인 백로그 + P634~P641 라이브 확인

표기: ✅ PASS · ⚠ 부분/조건부 · ❌ FAIL · ⛔ 검증 불가(사유) · ⏭ 미실시

| 항목 | 결과 | 증거 요약 |
|------|------|----------|
| P604 NFP 금요일 | ⏭ | 라이브 직접 캡처 실패(서사 텍스트만 걸림). 헤드리스 T859 게이트가 상시 커버 |
| P605 VKOSPI 실시간 갱신 | ❌(환경) | 27.00 고정은 아니나 **16.00 (폴백) 고정** — fetchVkospiDynamic이 프록시 차단으로 실패 지속(§2 UX-03). 코드는 정상, 파이프라인이 죽음 |
| P606 themes 칩·본문 일치 | ✅ | "Mid Cycle (Expansion) · 성장 주도" 칩·본문 동일 |
| P607 briefing/signal F&G | ✅ | briefing "F&G 45" = sentiment 45 = _lastFG 45. signal 스트립도 지연 후 45 표시(초기 수 분 "—" — §2 UX-07) |
| P608 briefing 헤더 단어경계 | ✅ | 헤더 2건 모두 완결 문장, 중간 잘림 0 |
| P609 서버 배지 tooltip | ⏭ | 추출 스크립트가 보안 필터에 차단된 회차에 포함, 재실행 누락 |
| P610 kr-technical 캔들 | ⚠ | 오류 모달 없음 ✓, `loadKrCandleChart()` 트리거 시 120pts·마지막 07-07 폭락봉([286000,310000]) 정상 렌더 ✓. **단 진입 시 자동 로드 없음**(U4 잔존, §2 UX-06) |
| P611 PUBLIC STATUS 한국어 | ✅ | raw enum(UNAVAILABLE 등) 0건, 한국어 상태·건수 요약 확인 |
| P613 AI 패널 안내 | ⚠ | 채팅 패널이 빈 화면 아닌 안내 메시지 표시 확인(키 안내). 이력 없는 페이지별 문구는 미개별 확인 |
| P614 운영자 노트 경과 배지 | ✅ | "2026-06-30 **7일 경과 (확인 필요)**" 표시 |
| P615/P632 390px topbar | ⛔ | 뷰포트 미반영 제약(§0-2). CI 뷰포트 매트릭스로 이관 |
| P616 경고 pill 접기 | ✅ | "⚠ 주의 항목 10건" 1줄 + DETAILS open=false. (펼침 클릭 후 11개 pill 표시는 미클릭) |
| P620 포트폴리오 데모 누출 | ✅ | NVDA에서 "Your P&L: 내 포트폴리오 외 종목" |
| P621 market-news 중복 | ⚠ | 완전동일 제목쌍 4건 검출(ADATA·삼성전자·SpaceX·SK하이닉스) — 단 카드 셀렉터 아티팩트(중첩 캡처) 가능성 있어 **정밀 재검 필요**(§2 UX-13) |
| P622 테마 브레드크럼/NVDA | ✅✅ | "Themes > AI AI·반도체" 실테마명 · NVDA행 "—"(Self 0건) |
| P623 모멘텀 정렬 가격 | ✅ | 정렬 후 상위 6종 전부 실가격(BE 295.05 등) |
| P624 SPY 포지셔닝 | ✅ | "200MA $695 · 50MA $740 · ATH $759" 실계산값(0.0%/RSI50 고정 아님) |
| P625 HY 스프레드 정합 | ⏭ | 요소 추출 실패(셀렉터), 시간 제약으로 미재시도 |
| P626 kr-supply TOP10 | ❌ | 3표 전부 "데이터 수신 대기..." 영구 + 폴백 문구도 미표시(QA 기대 둘 다 미충족). 원인: 프록시가 Naver에 HTML 차단 페이지 반환(§2 UX-03). 콘솔은 "폴백 데이터 사용 중"이라는데 UI는 미반영 — 이중 불일치 |
| P627~P630 중 P628 | ✅(부분) | Exit Triggers(y2005) > 진입 체크리스트(y1695) 순서 확인. lockout은 display:none 유지(v51.40 의도) |
| P629 채팅 분산 카운트 | ⛔ | 채팅이 클라 키 요구로 실문답 불가(§0-4, §2 UX-04). 헤드리스 게이트가 커버 |
| T143 티커 클릭 15곳 | ⚠ | showTicker·showThemeDetail·정렬·탭 등 delegation 경유 동작 확인(샘플). 15곳 전수는 아님 |
| T239 macro 스토리라인 출처 | ✅ | storyline에 "출처" 존재 |
| T491/T512/T557 로딩 문구 | ✅ | 잔류 "로딩 중/계산 중" 0건. 캐리 배지 실데이터("USD/JPY 162.13" 복합) |
| T781 SVG 라벨 겹침 | ⚠ | **계산형**: home svg 2개 겹침 0, tiny(<10px) 1건(9px, 경고 pill 세부 — SVG 아님)·signal svg 0개. 41곳 전수 시각 확인은 아님 → §5 V3 게이트로 구조 종결 |
| P632 PUBLIC STATUS/CI 게이트 | ✅(3/5) | 한국어 매트릭스 ✓ · deploy `needs:[validate,headless-tests]`(ci.yml:152) ✓ · 로컬 계약 스크립트 2종 green ✓. 390px ⛔ · 단 **ci.yml:113~116 주석이 "deploy needs 미편입"이라 서술 — 실코드(152)와 모순되는 주석 drift 발견** |
| P634 VIX 기간구조 게이트 | ✅ | 비live 상태에서 판정문 억제 확인("데이터 수신 대기…", 백워데이션 오판정 0). 실측 콘탱고(15.93>12.32)와 모순 없음 |
| P635 VKOSPI 폴백 라벨 | ✅ | "16.00 (폴백·정상 추정)" 정직화 라벨 |
| P636 KOSPI 전일종가 | ✅✅ | 7,656.31/-4.91%/전일 8,051.33/"7/7 종가" — Naver 실측과 완전 일치. KOSDAQ -1.87%도 일치 |
| P637 번역 폴백 헤드라인 | ✅ | "[번역 대기]" 0건 · 중국어 0건 · 실제 한국어 헤드라인(ADATA·골드만삭스 등) 렌더 |
| P638 워커 /anthropic | ✅(간접) | 번역·브리핑 실콘텐츠로 간접 확인. 405/403 콘솔 0건 |
| P639 시장 건강도 라벨 | ✅ | 티커 "✓시장 건강도: 62점" (시그널 55와 라벨로 구분됨) |
| P640 캔들 차트 문구 | ✅ | "위 캔들 차트와 아래 RSI·MACD…" |
| P641 VKOSPI 히스토리 누적 | ❌(환경) | `aio_vkospi_hist_v1` **localStorage에 없음** + 차트 20pts·마지막 6/5 시드 그대로 — 누적 트리거(fetchVkospiDynamic 성공)가 §2 UX-03으로 전멸해 **개선이 라이브에서 무효**. 코드 결함 아님 — 상위 의존 사망 |

**미점검 표면 6곳 커버 결과**: 테마 상세 내부 ✅(§2 UX-01/02) · 팩터·레짐 탭 ✅ 정상(가중 33/24/9/8/12% + "위험선호" 레짐 표시 — 1차 오판을 재검증으로 정정) · AI 채팅 ⛔(키 경로, §2 UX-04) · 접힘 콘텐츠 ✅(details는 home 2·guide 2뿐, dev 마커 0 — v51.30 default-path 정리로 이미 소멸) · 포트폴리오 후반 흐름 ⚠(§0-5 제약, 위젯 계약만) · guide ✅(§2 UX-12 재해석).

## §2 신규 발견 대장 (UX-01 ~ UX-13)

| ID | 심각도 | 발견 | 증거 |
|----|--------|------|------|
| UX-01 | **P0 버그** | **showThemeDetail 크래시 — 테마 17개 중 2개(semiconductor, ai_infra) 진입 사망**. `_buildThemeDeepAnalysis`(index.html:26358~26359) `topV/botV.toFixed` null 크래시. 추정 조건: 수신된 리더 등락률이 **전부 같은 부호**면 비교(`d.pct > topV`)가 한 번도 참이 안 돼 null 잔존(초기값 null 클래스 — R15/R228 미적용 지점). 타이머 재렌더(index:27977)가 크래시를 **반복 발화**(콘솔 에러 25건 전원 이것). 클릭 1회에 dispatch 6회 기록 — 리스너 중복 의심 별도 확인 필요 | 콘솔 `[AIO:delegate] dispatch failed: showThemeDetail` ×25 · 직접 호출 재현 · 17개 전수 try/catch 순회 결과 |
| UX-02 | **P0 구조** | **theme-detail 이중 표면**: 타일 클릭 → `#theme-detail-panel`(page-themes 내부 인라인)에만 렌더, **`page-theme-detail` 라우트는 어떤 경로로도 도달 불능(고아)**. showThemeDetail은 showPage를 호출하지 않음(index:26238~26240). P622 브레드크럼 수정은 고아 표면에 적용된 셈. 성공 시에도 인라인 패널 위치로 스크롤 안내가 없어 "무반응" 체감(U7의 나머지 절반) | 4테마 성공 호출 후에도 hash #themes·라우트 invisible 실측 |
| UX-03 | **P0 파이프라인** | **공개 프록시 계층이 KR API의 실효 SPOF**: 프록시가 m.stock.naver.com에 HTML(차단/캡차 페이지) 반환 → JSON 파싱 실패. 피해 확정: P626 수신대기 영구 · P605/P641 무효(VKOSPI 폴백 고정 + 히스토리 누적 0) · VIX9D/3M/6M "—". **콘솔 "폴백 데이터 사용 중" vs UI "수신 대기" 불일치**(폴백이 표에 미렌더) 동반. X1(워터폴 낭비)의 실피해 버전 — 직접 curl은 성공하므로 프록시 IP 차단이 유력 | 콘솔 `KR KOSDAQ 수급 fetch 실패 — 프록시 차단 가능: Unexpected token '<'` 반복 · localStorage에 과거 성공 캐시 존재(간헐성 방증) |
| UX-04 | **P1 계약** | **AI 백엔드 이원화**: 브리핑·번역=워커 서버키(P638)로 작동하는데 **채팅은 클라 키 필수** — "AI 답변을 쓰려면 Claude 키를 저장하세요". `aio_claude_api_key`(108B)가 localStorage에 있는데도 거부(해독/로드 상태 확인 필요). 운영자가 서버키를 넣은 지금, 기능별 키 요구가 비일관 — 통일 여부는 비용 정책이 걸린 운영자 결정(§5 결정카드 ②) | 채팅 실전송 1회 실측 |
| UX-05 | P1 | **briefing 동일 화면 F&G 이중값**: "실시간 시장 요약 (21:02 기준) … F&G **31** 공포" vs 같은 페이지 스코어 스트립 "F&G **45**". L3(동일 지표 다중값) 클래스의 신규 사례 — P607이 스트립만 _lastFG로 옮기고 요약 텍스트 빌더는 다른 소스 잔존 추정 | DOM 텍스트 실측 |
| UX-06 | P1 | **kr-technical 초기 공백 + y축 압축 확정**(U4 수치화): 진입 시 Chart 인스턴스 null(자동 로드 없음), 로드 후 `beginAtZero:true`로 **y축 0~500,000 vs 캔들 286,000~310,000** — 유효 표시영역 ~5%로 압축 | Chart.getChart() 내부 옵션·스케일 실측 |
| UX-07 | P2 | **첫 페인트 플레이스홀더 클래스**: signal-mv-strip 진입 후 수 분 "VIX — · F&G — · SPY —"(이후 자가 치유) · R74 inline-match 부팅 시 5 mismatch → 정착 후 0 · vix-term 카드 3/4 값 "—"에 사유 없는 "수신 대기…". 공통: **대시(—)가 '로딩 중'인지 '실패'인지 '해당 없음'인지 구분 불가** | 시계열 재측정(초기 vs 15분 후) |
| UX-08 | P2 접근성 | **aria-live 132개** — 실시간 갱신 앱에서 라이브 리전 과잉은 스크린리더 알림 폭주(반대 방향의 접근성 결함). 정책 부재 상태로 증식한 것으로 추정 | DOM 카운트 |
| UX-09 | P3 접근성 | canvas 44개 중 7개 aria-label/role 없음 | DOM 카운트 |
| UX-10 | P3 | 홈 '시세 수신' 이중 카운트: topbar "시세 오후 08:45 (359개)" vs PUBLIC STATUS "시세 77개 수신" — 클라 라이브 vs 서버 스냅샷 카운트가 라벨 구분 없이 병렬 | DOM 실측 |
| UX-11 | P2 성능 | **뷰포트 리사이즈 시 렌더러 45s+ 프리즈**: 8페이지 순회+차트 리사이즈 폭풍으로 CDP 타임아웃(측정 루프는 완주). 실사용자도 창 크기 변경/회전 시 프리즈 체감 가능성. X3(readiness 45s)과 같은 "동기 대량 작업" 클래스 | CDP 타임아웃 + hash 전진 실측 |
| UX-12 | 정정 | **U10 재해석**: guide 페이지는 534 엘리먼트/16,486자로 작음. "a11y 트리 213k 토큰"의 실체는 **22페이지 상주 DOM 전체(38,073 엘리먼트)** — 특정 페이지 문제가 아니라 상주 아키텍처 특성. AT(보조기술)·도구 부하 관점의 항목으로 재분류 | 실측 대조 |
| UX-13 | 관찰 | market-news 완전동일 제목쌍 4건 — 셀렉터 중첩 캡처 아티팩트 가능성 있어 CONFIRMED 아님. §5 V3의 중복 게이트에서 word-bag 기준으로 기계 판정 필요 | word-bag 스캔 |

## §3 잔존 확인 (기지 발견 — 재확인만)

U9 breadth 델타 ±0pp×3 잔존(게이지 색-라벨은 픽셀 미확인) · U3 국기 이모지 DOM 존재(🇺🇸 Americas — 렌더 확인 불가, twemoji 폴리필 없음 → 잔존 추정) · L10 부팅 `DATA_SNAPSHOT before initialization` 경고 잔존 · F4 stale 9종 그대로(Fed 20d·BOJ 21d·BOK 40d·BOE 68d·PBOC 56d·국고채 53d·한국거시 68d·미거시 67d·SMA 11d) · F5 public-data 07-03 스냅샷 지속(KR 칩 4개) · F6 '오늘 시장 핵심' 콘텐츠 캡처 불충분(빈 상태 의심 — L4-4 백로그 유지) · U2 FAB 겹침 미재현(FAB rect 0 — 조건부 노출, L5-2 유지).

## §4 정상 확인 (회귀 방지 기준선 추가분)

P606·P607·P608·P611·P614·P616·P620·P622·P623·P624·P628·P634~P640 라이브 작동(§1) · **L9 해소 확인**(IC 패널 "최근 1년 횡단면 · 849종목 · 2026-07-07" 기준일 표시 — v51.91/P586분) · **L3 개선 실증**(kr-home=kr-supply KOSPI 7,656.31/-4.91% 완전 일치) · 팩터·레짐 탭 정상 · 번역 파이프라인 완전 소생(중국어·[번역 대기] 0) · KOSPI/KOSDAQ/VIX 외부 실측 3종 일치 · 모멘텀 정렬·정렬 후 가격 정상 · FMP 비활성 헤더 경고 정직 표기 유지 · 데스크톱 1584px 가로 오버플로 0(8페이지) · 접힘 콘텐츠 dev 마커 0 · tabindex>0 0건 · img alt 누락 0건 · prefers-reduced-motion 룰 2건 존재 · 콘솔 에러는 UX-01 단일 클래스뿐(그 외 0).

## §5 구조 개선 설계 — Phase V0~V4

> 설계 원칙: ① 개별 증상이 아니라 **클래스**를 수리 ② 수리마다 **기계 게이트** 동반(재발 차단) ③ 운영자 결정이 필요한 것은 결정 카드로 분리. L-로드맵과의 관계: V0=L5-2의 승격판(+신규 P0), V1=X1/L0 계열의 근본판, V2=L1(Verdict Gate)·L4-2(킬스위치)의 표시계 확장, V3=ARCH Phase 2-8의 구체화(이번 세션에서 도구 한계로 실증된 필요성), V4=접근성·성능 기반선.

### Phase V0 — P0 결함 수리 (1세션, 코드)
1. **[UX-01] showThemeDetail 크래시**: `_buildThemeDeepAnalysis`의 topV/botV 초기화를 null → `-Infinity/+Infinity`로, 출력은 `_aioSafeFixed` 경유. **동일 클래스 전수 스윕**: `grep -n "= null" 근방의 `.toFixed(` 직접 호출을 index.html+js 6모듈에서 목록화해 일괄 게이트(기존 ci-runtime-contract-check의 unsafe-toFixed 패턴에 이 변형 — "비교로만 채워지는 누산 변수" — 추가). 부수: delegate dispatch 실패 시 콘솔 전용이 아니라 **1회성 토스트**(사용자 무반응 체감 제거) + 클릭 1회 6-dispatch 원인(리스너 중복 등록 지점) 확인.
2. **[UX-02] theme-detail 표면 단일화**: 결정 카드 ③의 선택에 따라 (권장안) **인라인 패널을 정식 표면으로 확정** — showThemeDetail 성공 시 `theme-detail-panel.scrollIntoView({behavior:'smooth'})` 추가, 고아 라우트 page-theme-detail는 제거(또는 인라인 패널로 리다이렉트). 나머지 안: 라우트로 통일(showThemeDetail 말미 showPage 호출) — 콘텐츠 이중 유지비가 계속 들므로 비권장. **게이트**: "테마 타일 17개 전수 클릭 시 예외 0 + 상세 표면 가시화"를 헤드리스에 추가.
3. **[UX-05] briefing 요약 F&G**: '실시간 시장 요약' 빌더의 F&G 소스를 `window._lastFG` 구독으로 전환(P607의 미완 나머지 절반). 게이트: 같은 페이지 내 F&G 표기 전 지점 값 일치 테스트.
4. **[UX-06] kr-technical**: 페이지 첫 진입 시 `loadKrCandleChart()` 자동 1회(실패 시 기존 폴백 문구) + `beginAtZero:false`(+suggestedMin/Max = 데이터 min/max 여유 5%). U4 종결.

### Phase V1 — 프록시 계층 구조화 (UX-03/X1 근본, 1~2세션)
1. **콘텐츠 검증 레이어**: `fetchViaProxy` 응답에 JSON 기대 시 content-type/선두문자 검증 — HTML이면 "그 프록시 실패"로 처리하고 다음 폴백으로(현재는 성공 응답으로 오인해 파싱 단계에서 죽음).
2. **성공률 기반 동적 정렬**(L5-3 승격): 프록시별 최근 성공률을 세션 메모리에 기록, 정렬 우선. 3연속 503 낭비 제거.
3. **aio-proxy 워커에 Naver 패스스루 라우트 추가**: B5 해소로 운영자 워커 배포 루트가 검증됐으므로, 공개 프록시 의존을 자체 워커로 이전(Naver가 공개 프록시 IP를 차단하는 상황의 근본 우회). `cloudflare-worker-proxy.js`에 라우트 추가 → 운영자 재배포 1회.
4. **실패의 UI 계약**(P626 재발 방지): "수신 대기"는 **시도 중에만** 허용. N회(제안 3회) 실패 후엔 반드시 ①"수신 실패 — 프록시 차단 추정" ②마지막 성공 시각 ③재시도 버튼의 3요소 카드로 전환. 콘솔 폴백 로그와 UI 상태가 다르면 안 됨(이번 이중 불일치의 직접 수리). **게이트**: 헤드리스에서 프록시 전실패 모킹 시 "수신 대기" 잔존 0건.
5. 완료 판정: P626 표 실채움(또는 정직한 실패 카드) + P641 누적 시작(localStorage 키 생성 실측) + vix-term 4값 수신.

### Phase V2 — 표시 상태 시스템 (UX-07 클래스 + L1/L4-2 접합, 1~2세션)
- **대시(—) 단독 표기 금지 규약**: 모든 값 슬롯은 {값} | {대기(스피너/틱)} | {실패(사유)} | {해당 없음} 4상태 중 하나를 명시. 공용 헬퍼 `renderValueSlot(state, value, meta)` 신설, 1차 적용: signal-mv-strip · vix-term-summary · 테마 히트맵 타일 · kr-supply 표(V1-4와 동일 지점).
- **부팅 일관성**: R74 5-mismatch가 첫 페인트에 보이는 문제 — inline 시드 값과 스냅샷 값의 불일치는 **부팅 완료 전 마스킹**(skeleton) 또는 시드 동기화로. L2(첫 페인트 낡은 결론)와 같은 세션에서 처리 권장.
- **사망 차트 킬스위치 일반화**(L4-2 잔여): 차트 공용 경로에서 마지막 datapoint 나이>임계 시 "MM-DD 정지됨" 오버레이 — 1차 대상에 kr-vkospi-chart(현재 6/5 정지 상태로 재확인됨) 포함. VKOSPI는 V1이 성공하면 자연 소생하지만, **소생 전까지의 정직 표시**가 이 항목의 존재 이유.

### Phase V3 — 검증 인프라 상설화 (이번 세션의 도구 한계가 곧 요구사항, 1~2세션)
> "실브라우저 미확인" 백로그가 세션마다 재발하는 구조 원인의 종결. 이번 감사에서 실증: 수동 브라우저 감사는 hidden-탭/리사이즈/스크린샷 제약으로 **원리적으로 불완전**했다.
1. **뷰포트 매트릭스 CI**: ci-headless 인프라 재사용 — {390, 768, 1024, 1440} × 22 route에서 ①`doc/page overflow 0` assert ②스크린샷 아티팩트 업로드(비교 없이 육안용부터) ③이번 세션의 계산형 검사 이식(폰트<10px 카운트, svg text getBBox 겹침 0, topbar 우측 클러스터 클리핑 0). report-only 1주 → 게이트 승격(P630/P632 패턴 그대로).
2. **i18n 게이트**: 가시 텍스트 노드에서 한글 0%·영문 위주 문자열을 화이트리스트(티커·단위·고유명사) 기반으로 검출 + CJK 통합한자 검출(중국어 원문 노출). R206 반복 재발의 기계 종결. 위치: ci-ux-default-path-check 확장.
3. **중복 카드 게이트**(UX-13 판정 겸용): market-news/briefing 렌더 결과에서 word-bag 유사도>0.8 카드쌍 0건 assert — P621의 회귀 게이트이자 이번 관찰의 CONFIRMED/아티팩트 판별기.
4. 접근성 기본 게이트: aria-live 총량 상한(§V4-1 정책 수치) · canvas 라벨 100% · tabindex>0 0 유지.

### Phase V4 — 접근성·성능 기반선 (1세션 + 상시)
1. **[UX-08] aria-live 정책**: "사용자 주의가 필요한 상태 변화"만 polite(제안: 상단 결론·데이터 오류 토스트·갱신 완료 배지 ≤10개), 나머지 132개서 제거. 스크린리더 실사용 1회(NVDA)로 검수 — 이건 도구가 아니라 사람 확인 항목.
2. **[UX-09]** canvas 7건 라벨 부여. **[UX-12]** 비활성 페이지 `inert` 속성 부여 검토(AT 트리에서 21개 페이지 제외 — 38k 엘리먼트 부담 완화, 상주 DOM 구조는 유지한 채 접근성 뷰만 축소).
3. **[UX-11] 리사이즈 폭풍**: Chart.js resize를 현재 가시 페이지의 차트로 한정(hidden 페이지 차트는 다음 showPage 시 lazy resize) + resize 핸들러 debounce 250ms. X3의 readiness idle화(L4-3)와 같은 "동기 대량 작업 해체" 계열로 묶어 처리.
4. **[UX-10]** 시세 카운트 라벨 구분("클라 실시간 359" vs "서버 스냅샷 77") — 1줄 수정.

### 운영자 결정 카드 (착수 전 답 필요 — L5-1에 추가)
| # | 질문 | 선택지 | 권장 |
|---|------|--------|------|
| ① | 아시아 세션 리스크 입력(기존 L5-1) | 유지 | (기존 문서 참조) |
| ② | **AI 채팅 백엔드**: 채팅도 워커 서버키 경유로 통일? | (a) 통일+일일 호출 상한 (b) 현행(채팅=개인키) 유지+안내문에 "브리핑과 달리 개인 키 필요" 명시 | (a) — 단 비용 노출이 우려되면 (b)+안내 정직화가 차선 |
| ③ | **theme-detail 표면**: 인라인 패널 확정 vs 라우트 통일 | 위 V0-2 참조 | 인라인 확정+고아 라우트 제거 |
| ④ | **지원 뷰포트 선언**: V3 매트릭스의 게이트 대상 폭 | 390/768/1024/1440 전부 vs 390+데스크톱만 | 전부(비용 낮음 — 같은 러너에서 루프) |

## §6 실행 순서 요약 (차기 세션)

1. **V0 전체**(UX-01/02/05/06 — 결정 카드 ③만 선결) → 버전업(R1) → 헤드리스 922+신규 게이트 → P번호(R3).
2. **V1**(프록시) — V1-3 워커 라우트는 운영자 재배포 1회 필요(막히면 V1-1/2/4 먼저).
3. **V3-1 뷰포트 매트릭스**(report-only로 즉시 가치) → V2 → V4 → V3 게이트 승격.
4. L-로드맵 잔여(L4 잔여·L5)와 병행 가능 — 충돌 없음, V1이 L5-3을 흡수·승격.

미완(다음 재감사로): 픽셀 색상 판정(U9 게이지) · P609/P625 재캡처 · P615/P632 390px(V3-1이 흡수) · U2 FAB 재현 조건 · 41곳 SVG 전수(V3-1이 흡수) · 스크린리더 실사(V4-1, 사람 항목).
