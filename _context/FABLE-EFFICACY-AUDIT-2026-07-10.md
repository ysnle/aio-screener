# FABLE-EFFICACY-AUDIT-2026-07-10 — 라이브 페이지별 시장데이터 완비·사용성·실효성 진단 + 보강 설계

> **상태**: 진단 17.5/22페이지 + §4 보충 실측(EF-19/20 추가 발견) + **Batch 1~4 구현 전체 완료(v52.40~v52.43, 2026-07-10 같은 세션)**. EF-20(AI `/anthropic` 403)만 운영자 조치 대기로 미해소.
> **대상**: 라이브 v52.34 (2026-07-10 00:43~01:30 KST 1차 실측 + 06:20~09:10 KST 보충 실측/구현/재검증) + 로컬 v52.39→v52.43(미배포, 이 세션 전체 구현 포함) 대조
> **방법**: Chrome MCP 실브라우저 페이지 순회 + 네트워크/콘솔 로그 추적 + curl 직접 업스트림 검증(EF-18) + WebSearch 실캘린더 재확인(EF-03) + Node 격리 로직 검증
> **핵심 교훈**: EF-01/08/09/11(절반)/05가 재검증 결과 원 진단과 다른 실제 원인이거나 미재현으로 판명 — "라이브 스냅샷 1회 관찰"과 "재현 가능한 구조적 결함"을 구분하는 재검증 단계가 실제 수정 범위를 여러 차례 바꿨다(상세: §5 각 Batch 완료 기록). **다음 필요 작업**: 커밋+배포(사용자 지시), EF-20 운영자 확인, §5 말미 "세션 종료 시점 잔여 항목" 참조.

---

## §0. 요구 정의 (사용자, 2026-07-10)

각 페이지에 대해 4축 진단: **(1)** 현재 시장 상황 파악을 위한 필수·핵심 데이터가 모두 있는가 **(2)** 놓친 요소는 없는가 **(3)** 읽고 사용하는 데 어려움이 없는가 **(4)** 실제 매매(트레이딩)와 학습에 도움이 되는가(실효성).

**종합 결론 (요약)**:
- **시세 레이어는 강함**: 라이브 81심볼 수신, signal 65슬롯·fxbond 67슬롯·스크리너 873행·kr-themes 394슬롯 등 라이브 표면 대부분 정상 작동. KR 지수/환율은 7/9 종가 정확.
- **경제지표(펀더멘털 데이터) 레이어가 약함**: 매크로 내러티브가 "5월 NFP 172K"(6월 NFP는 7/2 발표됨), 시계열 4~5월 기준, kr-macro 2월 기준 항목 — **시세는 초 단위인데 경제지표는 1~2개월 지연**. 시그널의 거시 축(10%)과 매크로 페이지의 실효성을 직접 훼손.
- **같은 지표가 표면마다 다른 값**: technical의 S&P(7,527.34) vs 홈/시그널 라이브(7,490.7), breadth 게이지(32/38/48%) vs 같은 페이지 판정("BROAD RALLY") vs 홈 스트립(52%) — 사용자가 어느 값을 믿을지 알 수 없음.
- **죽은/빈 표면 4곳이 트레이딩 동선의 요충지에 있음**: 엔캐리 게이지(상시 —/100), 어닝 캘린더(기본 경로 빈 화면), ticker 재무·차트 레이어(FMP 다운 시 침묵 숨김), breadth A-D·신고저 카드(—).
- **학습 실효성은 v52.39 교육 레이어(미배포)가 크게 개선할 예정**이나, 교육 콘텐츠가 "보라"고 가리키는 표면 2곳(엔캐리 게이지·어닝 캘린더)이 죽어 있어 **EF-08/EF-09를 함께 고쳐야 "가르친 대로 화면이 보인다"**.

---

## §1. 페이지별 진단 매트릭스 (라이브 실측)

판정: ✅ 충분 · ◐ 부분 · ❌ 미충족 · ⛔ 미점검. "실효성"은 트레이딩+학습 종합.

| 페이지 | 핵심 데이터 | 누락/죽은 표면 | 사용성 | 실효성 | 근거(실측) |
|---|---|---|---|---|---|
| home | ✅ 지수·VIX·금리·DXY·BTC 11슬롯 전부 라이브, 뉴스 하이라이트 최신 | ~~결론바 서브텍스트 21일 경과(EF-13)~~ **v52.40 해소**. **v52.43 EF-17(사용자 승인) 추가**: 글로벌 마켓 표에 ES=F/NQ=F 선물 2행(정규장 외 강조) | ✅ | **상** | 글로벌 표 라이브 값 대조 일치, T885 회귀 게이트 |
| signal | ✅ 65슬롯 라이브, 팩터분해·체크리스트·Exit·시나리오 완비 | ~~리스크 모니터링 2슬롯 대기 + 점수 서브라벨 대기(EF-11)~~ **v52.41 부분 해소** — 서브라벨 절반은 코드 추적 결과 미재현(같은 함수 3줄 간격, 분리 불가), rm-vixstr/rm-rspratio는 실제 갭이라 pending 상태 적용 | ✅ | **상** | T876으로 pending-state 회귀 게이트 신설 |
| breadth | ◐ 게이지는 있으나 스냅샷(기준일 미표시), A-D·신고저 "—" | ~~판정 상호모순(EF-02), 델타 전부 0pp~~ **v52.40 해소**(canonical 통일+기준일 배지+na-state+50sma 동기화) | ✅ | **상** | Batch 1 구현+T870-873/게이트 확인 |
| sentiment | ◐ F&G 47 라이브 ✓, VIX 기간구조 숫자는 시드 | ~~기간구조 시드 3개 시각 구분 없음(EF-06)~~ **v52.42 해소** — live/na 상태 분기 + 중복 렌더 블록 제거. ~~전일값-델타 라벨 불일치(EF-15)~~ **v52.42 해소** — CNN 라이브 fetch 성공 시 두 delta 표면 동시 재계산 | ✅ | **상** | T879 회귀 게이트, EF-15는 async라 정적 계약으로 커버 |
| briefing | ◐ 6축·~~일정(BOK 7/10, CPI 7/14) 정확~~ **BOK 날짜는 실제로 오류였음 — v52.43에서 7/16로 정정(EF-03)** | ~~자정~08시 미래시각 라벨+어제 수치(EF-04)~~ **v52.40 해소**, ~~번역실패 항목(EF-14)~~ **v52.42 해소** — 실제 원인은 제목이 아니라 미번역 소스명(출처) 노출, `_aioSafeSourceLabel()`로 정정 | ✅ | **상** | T881 회귀 게이트 |
| technical | ◐ RSI 56.0 등 지표 계산 ✓, 건강도 A | ~~S&P 값 0.5% stale(EF-01)~~ **코드 추적 결과 미재현** — technical 자체는 이미 canonical `data-live-price` 공유. 동일 패턴 실제 버그는 macro "지금·라이브" 카드에서 발견해 v52.40에서 해소. ~~TV차트 기본 미로드+대체카드 대시(EF-12)~~ **v52.41 해소** — 원인은 fallback sync가 "차트 로드" 버튼의 부수효과로만 실행되던 역설적 설계, `_aioSyncTvOhlcFallback()` 분리로 독립 실행 | ✅ | **상** | T870/T877 회귀 게이트 신설 |
| macro | ◐ 라이브 시세 34슬롯 ✓, ~~다음 발표(BOK 7/10) ✓~~ **BOK 날짜 자체가 오류였음(WebSearch로 발견) — v52.43에서 7/16로 정정**, 11개 섹션 완비 | 경제지표 내러티브·시계열 1~2개월 stale(EF-03) — `/data-refresh` 실행(quotes/F&G/news 갱신, FRED는 로컬 키 없어 BLOCKED), Fed/BOK 캘린더 날짜 오류 수정(v52.43). 나머지 시계열(CPI/PMI 등) 전면 재검증은 후속 필요 | ◐ | **중상** | T884 회귀 게이트, FOMC 캘린더도 6/17→lastRelease 승격+7/29 nextRelease로 정정 |
| fxbond | ✅ 67슬롯 라이브, 원/달러 1,508.65 ✓ | ~~엔캐리 게이지 —/100 상시 대기(EF-08)~~ **v52.41 해소** — 원인은 3/4축 부분스코어가 아니라 오펀 함수(P605 계열), 라이브 콘솔 수동호출로 증명 후 `_aioPageBus` 보조트리거 추가 | ✅ | **상** | 코드 자체는 항상 유효한 폴백 보유(T874 확인) |
| fundamental | ❌ 검색 전 어닝 캘린더·서프라이즈 "수집 대기"(5건) | ~~시장 레벨 어닝 정보 기본 경로 부재(EF-09)~~ **재검증 결과 미재현** — 진입만으로도(검색 없이) 3초 내 정상 렌더 확인, 최초 관찰은 로딩 스냅샷으로 판단. "실적 서프라이즈"만 실제 구조적 결함(FMP 전용 상시 403) — v52.41에서 정직화 | ◐ | **상** (진입 시) | 실측: `#fundamental` 진입 3초 후 "92건 어닝+5IPO" 자동 렌더 |
| themes | ✅ RRG 렌더·17타일·섹터 퍼포먼스·대기 0 | — | ✅ | **상** | showThemeDetail 크래시는 v52.27 해소됨 |
| portfolio | ✅ (입력형) 빈 상태 안내·R:R 계산기·리스크 카드 구조 ✓ | — | ✅ | **상** (입력 시) | 빈 상태 "추가하세요" CTA 확인 |
| ticker | ◐ NVDA 검색: 가격 $201.87·건강도 58·진입계산기 ✓ | ~~Key Metrics·Price Chart·Quarterly 통째 숨김(EF-10)~~ **v52.41 부분 해소** — Price Chart(`loadTickerChart`)는 이미 Stooq+시뮬레이션 폴백으로 정상 작동 확인(재현 안 됨), Key Metrics/Quarterly 9슬롯만 실제로 100% 미배선이라 na-state+펀더멘탈 포인터 적용 | ◐ | **중상** | 라이브 DOM 확인: Key Metrics는 visible(offsetParent 존재)인데 그냥 영구 공백이었음 |
| market-news | ◐ 피드·토픽·중요도 구조 ✓ | ~~최신 기사 17~20h 전(EF-05)~~ **재검증 결과 미재현** — 최신 기사 59분 전(서버 생성 14분 전) 확인, 갱신 cron도 30분 간격으로 건강. 감사 시점의 일시적 파이프라인 지연으로 추정, `data-watchdog.yml`이 이미 모니터링 중 | ✅ | **상** | 실측: 2026-07-10 08:50 KST 기준 최신 기사 age 59분 |
| options | ⛔ (nav 제거된 페이지 — 직접 라우트만 존재) | — | — | — | v50.35 폐기, v52.39 교육 블록만 추가됨 |
| screener | ✅ 873행 라이브·레짐 가중 작동 | FMP 컬럼 386슬롯 "—" (기존 확정 사항 — 현행 유지) | ✅ | **상** | 사용자 결정 존중, 재설계 안 함 |
| kr-home | ✅ 코스피 7,291.91(+0.62%)·코스닥·환율 7/9 종가 정확, VKOSPI 폴백 정직 라벨 ✓ | ~~수급 요약 라벨 모순(EF-07)~~ **v52.42 해소** — 실패 상태 진입 시 제목 날짜를 "폴백 데이터"로 override | ✅ | **상** | T880 회귀 게이트 |
| kr-supply | ◐ 실패 UI 계약(P643) 라이브 작동 ✓ | ~~수급 라이브 자체가 상시 실패 — 프록시 차단(EF-18)~~ **v52.43 근본원인 확정+수정** — curl 직접 검증 결과 프록시/차단이 아니라 순수 404(엔드포인트 경로 자체가 Naver에 없었음: `/investorTrend`→실제는 `/trend`). 경로+응답변환(`_aioAdaptKrTrendResponse`) 수정 | ◐ | **중상**(로직 검증됨, 실배포 후 최종 확인 필요) | T886 회귀 게이트, curl 200 확인. CORS 프록시 세션 내 rate-limit로 브라우저 종단 확인은 미완 |
| kr-themes | ✅ 28테마 카드+랭킹 라이브(394슬롯, "01:23 갱신"), 자동 분석 텍스트 ✓ | — | ✅ | **상** | 예상외로 건강 (v52.39 교육 블록이 유일 약점 보완) |
| kr-macro | ◐ 표 5개 구조 ✓ | 전량 스냅샷(22슬롯/라이브 0, Batch 4 EF-03 대상), ~~기준일 표시 산발(EF-16)~~ **v52.42 해소** — 기준금리/물가/경기 3카드에 `_fieldTs` 배지 신설, 수출입 라벨은 재확인 결과 이미 적절(EF-16 원 서술 일부 부정확) | ◐ | **중상** | T883 회귀 게이트 |
| kr-technical | ✅ 캔들차트(Naver 자체렌더·MA20·"TradingView 아님" 정직 라벨) ✓, KR건강도 62 B ✓, VKOSPI 폴백 라벨 정직 ✓, 금리스프레드 3종 ✓ | ~~KOSPI/KOSDAQ 기술분석 카드 상시 실패 + 새로고침 버튼 영구 고장(EF-19)~~ **v52.41 해소** — data-action `analyzeKrIndex`+올바른 targetId로 교체, `_fetchYahooChartData` codetabs.com 프록시 추가 | ✅ | **상** | T878 배선 확인 |
| guide | ⛔ (허브 — 이전 감사에서 충분 판정) | — | — | — | §4 참조 |
| theme-detail | — (P642에서 themes로 리다이렉트, 고아 라우트) | — | — | — | 감사 제외 |

---

## §2. 발견 대장 (EF-01 ~ EF-18)

우선순위: **P1**(현재 시장 판단을 직접 오도/차단) > **P2**(신뢰·사용성 저하) > **P3**(제안/운영).

### 정합성·신선도 계열

- **EF-01 (P1)** technical 페이지 "S&P 500 **실시간** 기술 지표" 헤더와 "지지·저항 & Weinstein" 카드가 **7,527.34** 표시 — 같은 시각 홈/시그널 라이브는 **7,490.7 (+0.11%)**. 0.5% 어긋난 stale 값이 '실시간' 라벨 아래 노출. 7,527은 어제 아침 KST 스냅샷 수준(브리핑의 "+0.60%"와 일치). R276(동일 지표 동일 소스) 위반 계열.
- **EF-02 (P1)** breadth 페이지 내부 모순 4중: ① 게이지 32/38/48%(스냅샷, **기준일 미표시** — fieldTs상 breadth_sma 2026-06-26, PUBLIC STATUS상 07-03) ② 같은 페이지 `breadth-signal-val`="BROAD RALLY"(RSP/SPY 라이브 파생) ③ 홈 스트립 "시장폭 52%" ④ 50일선 readout 텍스트 "50일선 52%"(게이지는 48%). 헤더 배지는 "약세 신호". **시장 폭 페이지의 존재 이유가 '지금의 참여도'인데 스냅샷 나이도 안 보여주고 판정들이 서로 싸움**. + A-D 비율·52주 신고/신저 카드 "—"(교차검증 카드 2개 사망) + 델타 전부 0pp.
- **EF-03 (P1)** macro 경제지표 레이어 1~2개월 stale: 내러티브 "고용(5월 NFP 172K로 금리인하 기대 후퇴)" — **6월 NFP는 7/2에 이미 발표됨**(리포 자체 캘린더 기준). 핵심 지표 시계열 4~5월 기준, 카드 1곳만 "36일 경과 ⚠️" 자체 경고. DATA_SNAPSHOT 수동 필드(`_fieldTs`: us_macro_manual 70일, fed_rate 23일, BOK 43일, BOE 71일 등 — 콘솔 경고 실측)가 UI에 안 보임. 시세는 초 단위인데 거시 판단 근거가 두 달 전.
- **EF-04 (P2)** briefing 자정~08시 구간에 "오늘의 브리핑 **2026-07-10 (Fri) · 08:00 KST**" — 아직 오지 않은 시각을 헤더로 표시하며 내용은 어제 아침 수치(S&P +0.60% vs 라이브 +0.11%). 사용자는 "오늘 아침 브리핑"으로 오독.
- **EF-05 (P2, 원인 미확정)** market-news 최신 기사가 17~20시간 전 — **미국 장중(목 10:40 ET)인데 최신이 어제 아침 KST 뉴스**. 서버 data.json은 87분 전 생성됐으므로 수집 주기 문제가 아니라 (a) 뉴스 소스 쿼리가 옛 기사만 반환 (b) 클라이언트 뉴스 캐시 TTL (c) 표시 나이 계산 중 하나. §4 조사 과제.
- **EF-06 (P2)** sentiment VIX 기간구조 카드: 판정은 "해당 없음"으로 게이트됐지만(P634 ✓) **시드 숫자 3개(VIX9D 18.80 / VIX3M 19.90 / VIX6M 20.40)가 시각 배지 없이 라이브 VIX(16.24)와 나란히 표시**. `data-source-kind="unavailable"` 속성만 있고 사용자 눈에 보이는 구분 없음 → 9D(18.80)>30D(16.24)를 보고 스스로 "역전(백워데이션)?"이라고 오독 가능. R282의 "숫자는 배지로 충분" 원칙이 "배지가 실제로 보여야 함"까지 못 감.
- **EF-07 (P2)** kr-home 수급 요약: "프록시 차단으로 수급 데이터 수집 실패 — 아래는 폴백 데이터입니다" 경고와 "최근 수급 (KOSPI) — **7/9 기준**" 최신 날짜 라벨이 **동시에** 표시(외국인 -1조7,939억 등 그럴듯한 수치). 실패인지 7/9 확정치인지 사용자가 판단 불가. 실제 출처 확인 후 라벨 한쪽으로 정리 필요.

### 죽은/빈 표면 계열

- **EF-08 (P1)** fxbond 엔캐리 언와인드 게이지: 4입력 중 USD/JPY 162.52·VIX 16.24·HYG 79.82 **3개 라이브인데 미일 금리차(일본 10Y 소스 부재) 1개 때문에 스코어 "—/100" + "데이터 수신 대기" 상시**. v52.39 교육 레이어가 "엔 급등이 보이면 이 게이지부터 확인하세요"라고 안내하는 바로 그 표면이 영구 사망 상태.
- **EF-09 (P1)** fundamental 검색 전 기본 화면: "어닝 캘린더 수집 대기…(Finnhub 우선, FMP 폴백)", "서프라이즈 데이터 수신 대기…" — **어닝 시즌에 '이번 주 누가 발표하나'를 보러 온 사용자가 빈 화면**. 시장 레벨 캘린더가 무키 기본 경로에서 채워지지 않음. v52.39 교육 레이어가 "어닝 캘린더에서 발표일을 미리 확인"하라고 안내하는 표면.
- **EF-10 (P1)** ticker NVDA 실검색 후: 가격·진입 적합성·진입 품질 계산기·캔들 갤러리만 렌더(전체 텍스트 838자). **Key Metrics·Price Chart·Quarterly Results 섹션이 통째로 숨겨짐**(offsetParent null) — FMP 다운을 안내 없이 침묵 처리. "기술·재무·뉴스·포트를 한 흐름으로"라는 페이지 선언 대비 재무 레이어 무표시. P643 실패 상태 계약 미적용 표면 + Price Chart는 FMP 무관하게 Yahoo OHLCV로 그릴 수 있는데 함께 숨겨짐.
- **EF-11 (P2)** signal 리스크 모니터링 카드: `rm-vixstr-status` "수집 대기"(VIX9D/3M 프록시 차단 계열), `rm-rspratio-status` "수신 대기" + `score-decision-sub` "점수 입력 수신 대기…"(점수 65는 이미 계산·표시 중인데 서브라벨만 영구 대기 — 배선 버그 의심).
- **EF-12 (P2)** technical TradingView 차트 기본 미로드(클릭 로드형) + "차트 대체 표시" 카드가 "종가: — 변동: —" 대시 (로컬 v52.39에서는 값이 채워졌음 — 라이브 v52.34와 코드 차이 또는 타이밍. 라이브 기준 기본 경로에서 차트도 대체값도 없음).

### 사용성·라벨 계열

- **EF-13 (P2)** 홈/시그널/브리핑 결론 바 서브텍스트 "최근 FOMC 결과는 이미 가격에 반영된 상태로 보고…" — `AIO_EVENT_RISK_CONTEXT`(asOf 2026-06-17/19, **21일 경과**)를 날짜 없이 표시. macro 페이지에서는 "2026-06-17:" 날짜가 붙는데 홈/시그널에서는 안 붙음. "데이터: 실시간·신뢰도 84%" 칩 옆에 있어 오늘의 판단 프레임으로 읽힘.
- **EF-14 (P2)** briefing 핵심 뉴스에 번역 실패 항목: "· 시장 · **Межа. Новини України.** 기사 · 중요도 64" — 우크라이나어 소스명만 노출, 제목 없음. P637(freeTranslateNews)의 적용 범위가 브리핑 핵심뉴스 표면까지 못 감(R206 계열 재발).
- **EF-15 (P3)** sentiment F&G 라벨: `fg-h1` "전일: 47" · 현재 47 · delta "+5" — 전일이 47이고 오늘 47이면 델타는 0이어야. 라벨-델타 소스 불일치 의심(조사 후 정합).
- **EF-16 (P3)** kr-macro 기준일 표시 산발: 페이지 전체가 스냅샷(22슬롯)인데 "2026-02" 기준 항목이 섞여 있고 카드별 기준일 노출이 불균일. "수출 +64.8% YoY (20일 기준)"이 몇 월분인지 화면에서 알 수 없음. + BOK 기준금리 fieldTs 43일 경과.

### 운영·구조 계열

- **EF-17 (P3, 신규 제안)** 야간(미국 장전·장후) 지수 선물 부재: 홈은 정규장 지수만. 한국 아침 사용자의 첫 질문("오늘 미장 분위기")에 답하는 ES/NQ 선물 또는 시간외 SPY 슬롯이 없음. Yahoo `ES=F`/`NQ=F`로 가능 — 스코프 확대라 제안으로만 기록.
- **EF-18 (P1, 구조)** KR 수급/VKOSPI/VIX9D 공통 프록시 SPOF(UX-03 재확인): kr-themes(394슬롯)는 잘 받는데 Naver JSON API 의존 표면만 상시 실패 — P643에서 Worker에 Naver 헤더 처리를 넣었지만 라이브에서 여전히 차단. **Worker 라우팅이 수급 엔드포인트에 실제 적용되는지**(클라이언트가 Worker 경유로 요청하는지, Worker가 차단당하는지) 라이브 네트워크 레벨 조사 필요.

### §4 보충 실측 발견 (2026-07-10 06:20~06:45 KST, 신규)

- **EF-19 (P2, 배선 버그, kr-technical)** "KOSPI/KOSDAQ 분석 새로고침" 버튼(index.html:11833/11843)의 `data-action="analyzeKrTickerDeep"`가 결과를 `#kr-ticker-analysis-result`(하단 "한국 종목 심화 분석" 박스)에 렌더 — 정작 자기 카드 `#kr-kospi-tech-result`/`#kr-kosdaq-tech-result`는 **절대 갱신되지 않음** (`analyzeKrIndex` vs `analyzeKrTickerDeep` 혼용, 코드 확인으로 100% 재현 확정). 실브라우저에서 버튼 클릭 → 카드는 그대로 "데이터를 불러올 수 없습니다", 대신 하단 박스에 "차트 데이터를 불러올 수 없습니다. 종목코드를 확인하세요." 표시로 실증. 부가 원인: 두 카드가 공유하는 `_fetchYahooChartData`(index.html:29552, 3-proxy 순차: cfWorker/corsproxy.io/allorigins.win)가 라이브 네트워크 실측상 KS11/KQ11/005930.KS 전부에서 allorigins·codetabs 503을 반복 — 같은 코드베이스에 이미 검증된 5-proxy 병렬 레이스(`dynamicTickerLookup`, index.html:21195-21201, v49.78/P415)가 더 안정적임이 네트워크 로그로 확인됨.
- **EF-20 (P1, 라이브 전용 회귀 — 운영자 확인 필요)** Worker `/anthropic` 라우트가 **403 "Request not allowed"**를 반복 반환 — AI 채팅(`chatSendUnified`) 실측 1문답 시도 15회 요청 중 대부분 403(1건 503), 콘솔에 동일 시각대 "AI 브리핑 생성 실패: API 403"·"번역 API 응답 에러: 403"도 함께 관측되어 **AI 채팅·브리핑 생성·번역 3개 기능이 동시에 실패 중**. 일일 쿼터는 "남은 횟수 50회"로 정상(쿼터 소진 아님). 응답 본문 문자열이 레포 `cloudflare-worker-proxy.js`의 어떤 에러 메시지와도 일치하지 않아 **배포된 Worker가 레포 코드와 다르거나(과거 C1/P638과 동일 패턴) Cloudflare 자체 정책(WAF/Access)이 원인**일 가능성. `_context/BUG-POSTMORTEM.md` C1/P638(2026-07-07)은 "Worker 재배포+ANTHROPIC_API_KEY 시크릿 추가로 완전 해소"로 기록돼 있었으나 **라이브에서 재발 확인** — R290(라이브 전용 회귀 재검증) 정확히 해당 사례. 레포 코드 수정으로 재현·수정 불가 — Cloudflare 대시보드 확인은 운영자 액션.

---

## §3. 보강 설계 (Sonnet 5 구현 방안)

> 공통 계약: R1(bump-version), R3(P번호 — 버그성 항목), 검증 = 로컬 게이트 전체 + `ci-headless-tests` + (연결 시) Chrome MCP 실브라우저. **R282/R291 준수**: 어떤 수정도 새 정적 단정 텍스트를 만들지 않는다.

### Batch 1 — 정합성 (v52.40 예정, EF-01/02/04/13)

1. **EF-01**: technical의 S&P 렌더 소스 추적(`지지·저항` 카드 + `실시간 기술 지표` 헤더 값) → `window._liveData['^GSPC']`(또는 SPY×배율 규칙이면 그 규칙의 라이브 값) 우선, 스냅샷은 폴백+배지로. 두 표면이 홈 글로벌 표와 같은 소스를 읽게 정합(R283 동일소스 패턴). **T-테스트**: 홈과 technical의 S&P 표시값이 라이브 주입 시 일치(±0.1%) assert.
2. **EF-02**: (a) breadth 게이지 3종에 기준일 배지 필수 표시(`DATA_SNAPSHOT._fieldTs.breadth_sma` → "기준: MM-DD (N일 전)" — 7일 초과 시 경고색), (b) 헤더 배지·`breadth-signal-val`·홈 스트립을 canonical `NARRATIVE_ENGINE.getBreadthRegime()` 단일 소스로(R287 적용 누락 표면 정리 — "BROAD RALLY"가 별도 RSP/SPY 파생이면 라벨에 "RSP/SPY 기준" 명시하고 종합 판정과 시각 분리), (c) A-D·신고저 카드에 P643 4상태 실패 렌더 적용(무한 "—" 금지), (d) 50일선 readout 텍스트가 게이지와 같은 값 소스를 읽게 수정(48 vs 52 불일치 해소).
3. **EF-04**: 브리핑 날짜 라벨 로직 — 생성 컷오프(08:00 KST) 이전이면 "MM-DD 08:00 KST 기준(어제)" 표기 또는 실제 생성에 쓰인 데이터 시각을 그대로 표기. 미래 시각 표기 금지. **T-테스트**: Date 모킹으로 00:30 KST 시뮬레이션 시 라벨이 미래 시각을 포함하지 않음 assert.
4. **EF-13**: `AIO_EVENT_RISK_CONTEXT` 렌더 시 홈/시그널/브리핑에서도 macro처럼 `asOf` 날짜 병기 + 21일 초과 시 "(오래된 컨텍스트)" 자동 배지. 30일 초과 시 서브텍스트 자체를 접기(기본 숨김). `ci-runtime-contract-check`에 asOf 노출 정적 계약 추가.

### Batch 2 — 죽은 표면 복구 (v52.41 예정, EF-08/09/10/11/12)

5. **EF-08**: 엔캐리 게이지를 **3/4 입력 부분 스코어**로 전환 — 미일 금리차 입력 결측 시 나머지 3축 가중 재정규화 + 게이지에 "금리차 제외(3/4축)" 명시 배지. 일본 10Y 라이브 소스 신설은 하지 않는다(Yahoo 심볼 부재, 스크랩 과잉 — R282 정신대로 결측을 정직하게 표기하고 부분값 제공). **T-테스트**: JGB 입력 null 시 스코어가 숫자이고 배지 문자열 포함 assert.
6. **EF-09**: 어닝 캘린더 — (a) 1차: 페이지 진입 시 시장 레벨 캘린더 fetch가 트리거되는지 확인(검색 시에만 트리거면 진입 시로 확장), (b) 2차: 서버 파이프라인(`fetch-data.mjs`)에 주간 어닝 캘린더 수집을 추가해 `public-data/`로 공급(무키 클라이언트도 채워지도록; Yahoo earnings 캘린더 스크랩 또는 기존 스냅샷 갱신 자동화 — 소스 실효성은 구현 세션이 확인), (c) 최소 폴백: P643 실패 상태 + "종목 검색 시 해당 종목 일정 표시" 안내로 무한 "수집 대기" 제거.
7. **EF-10**: ticker — (a) FMP 실패 시 Key Metrics/Quarterly 섹션을 숨기지 말고 P643 실패 상태로 렌더("FMP 미수신 — 기업 분석 페이지(SEC 경로) 이용" 포인터), (b) **Price Chart를 FMP와 분리**: `fetchYahooChartData`(기존 함수) + Chart.js로 자체 렌더 — kr-technical P610 캔들 패턴의 US 재사용(신규 CDN 없음). **T-테스트**: FMP 차단 모킹 시 ticker 검색 후 실패 상태 텍스트 존재+차트 캔버스 렌더 assert.
8. **EF-11**: `score-decision-sub` 배선 조사(점수 존재 시 대기 문구가 남는 조건) 후 수정. `rm-vixstr`/`rm-rspratio`는 서버 스냅샷 폴백 + P643 실패 상태(무한 대기 금지).
9. **EF-12**: TV 대체 카드가 라이브 SPY 종가/변동으로 채워지지 않는 원인 확인(v52.39 로컬에서는 채워짐 — 배포로 해소되는지 라이브 재확인 먼저, 잔존 시 수정).
9b. **EF-19(신규, §4 보충실측 발견)**: kr-technical "KOSPI/KOSDAQ 분석 새로고침" 버튼의 `data-action`을 `analyzeKrTickerDeep`(오배선)에서 `analyzeKrIndex`로 교체 + `data-arg2`/`data-arg3`로 정확한 대상(`kr-kospi-tech-result`/`KOSPI`, `kr-kosdaq-tech-result`/`KOSDAQ`) 전달(기존 `data-action="foo" data-arg="a" data-arg2="b" data-arg3="c"` 델리게이션 패턴 그대로 재사용, index.html:11833/11843). 부가로 `_fetchYahooChartData`(index.html:29552)의 3-proxy 순차 체인에 `api.codetabs.com`을 추가하거나 `dynamicTickerLookup` 스타일 병렬 race로 강화(라이브 네트워크 실측상 codetabs/cfWorker가 corsproxy.io/allorigins.win보다 KR 티커에 안정적). **T-테스트**: 두 버튼 클릭 시 `analyzeKrIndex`가 올바른 targetId로 호출되는지 registry 검증(R283 패턴) assert.

### Batch 3 — 라벨·번역 정직화 (v52.42 예정, EF-06/07/14/15/16)

10. **EF-06**: VIX 기간구조 시드 슬롯에 기존 4상태 value-slot 렌더 적용 — 시드값은 흐림 처리+«정적» 배지 또는 "—"+툴팁("라이브 미수신 — 시드값 숨김"). R282 Validation에 "verdict뿐 아니라 인접 숫자의 시각 구분까지" 각주 추가.
11. **EF-07**: kr-home 수급 요약 데이터의 실제 출처 확인(서버 `public-data` 확정치면 경고 문구를 "서버 스냅샷 · 7/9 확정치"로 교체, 정적 시드면 날짜 라벨 제거+"참고용" 강등) — kr-supply 동일 원칙. 모순 라벨(실패 경고+최신 날짜) 동시 표시 금지를 QA 항목화.
12. **EF-14**: 브리핑 핵심뉴스 표면에 P637 `freeTranslateNews` 경로 적용 확장 + 번역 실패 시 원문 헤드라인(음차 소스명이 아니라 기사 제목) 표시. **T-테스트**: 키릴 문자만으로 구성된 제목이 브리핑 카드에 그대로 노출되지 않음 assert(기존 R206 게이트 패턴 재사용).
13. **EF-15**: F&G "전일" 라벨과 델타의 소스 추적 → 같은 히스토리 소스로 정합.
14. **EF-16**: kr-macro 카드 공통 "기준일 배지" 일관화(스냅샷 카드 전수에 `_fieldTs` 노출) + "수출 YoY"에 대상 월 명시. (데이터 자체 갱신은 Batch 4의 /data-refresh.)

### Batch 4 — 데이터·운영 (코드+운영 혼합, EF-03/05/17/18)

15. **EF-03**: (운영) `/data-refresh` 실행으로 6월 NFP·CPI·금리 스냅샷 반영(fieldTs 8종 콘솔 경고 해소). (구조) `fetch-data.mjs`에 FRED 무키 CSV(CPIAUCSL, UNRATE, PAYEMS 등 헤드라인 수치)를 주 1회 수집해 `public-data/`로 공급하는 파이프라인 검토 — 내러티브 문장은 자동 생성하지 않고(R282/R291) 수치+발표일만 갱신. UI에는 `_fieldTs` 경과일을 카드 배지로 노출(콘솔 전용 경고 졸업).
16. **EF-05**: (조사) 라이브 `data.json`의 `news[].publishedAt` 분포 vs 화면 나이 표시 대조 → 수집 쿼리/캐시 TTL/나이 계산 중 원인 확정 후 수정. 미국 장중에 17h 지연이 재현되면 뉴스 수집 쿼리에 최근성 파라미터 보강.
17. **EF-18**: (조사→수정) kr-supply 요청이 Cloudflare Worker를 실제 경유하는지 라이브 네트워크 로그로 확인 → 미경유면 클라이언트 프록시 체인에서 Naver JSON 계열을 Worker 우선으로 고정, 경유하는데 차단이면 Worker 헤더/캐시 조정. VKOSPI·VIX9D 동일 뿌리. (해소 시 EF-11의 vixstr, kr-home VKOSPI 폴백도 연쇄 해소.)
18. **EF-17**: (제안 — 사용자 승인 후) 홈 글로벌 표에 ES=F/NQ=F 선물 2행 추가(정규장 외 시간에만 강조). 승인 없으면 착수하지 않는다.
19. **EF-20 (신규, 최우선 — 운영자 액션)**: Worker `/anthropic` 라우트 403 회귀. **레포 코드 수정으로는 해결 불가** — (a) Cloudflare 대시보드에서 `aio-proxy` Worker의 배포 버전이 레포 `cloudflare-worker-proxy.js`와 일치하는지 확인(diff), (b) `ANTHROPIC_API_KEY` 시크릿 유효성/만료 확인, (c) Cloudflare WAF/Access/Rate-Limiting Rules에 최근 추가된 차단 규칙이 있는지 확인. 해소 후 `curl -X POST https://aio-proxy.zmfhd007.workers.dev/anthropic`로 200 확인. **R290 반영**: 해소 후 `scripts/ci-live-invariant-check.mjs`에 `/anthropic` 라우트 최소 헬스체크(예: 401/403 없이 4xx 이외 응답 또는 명확한 quota-exceeded 응답인지)를 추가해 `data-watchdog.yml` 시간별 실행으로 재발을 자동 포착하게 한다 — 이번처럼 사람이 수동으로 Chrome MCP를 켜야만 발견되는 구조를 반복하지 않기 위함.

---

## §4. 미점검 잔여 (이번 진단에서 못 본 것 — 명시)

**2026-07-10 06:20~06:45 KST 보충 실측으로 4건 해소** (kr-technical/fundamental 실검색/AI 채팅/kr-macro 세부) — 결과는 §1·§2(EF-19/20) 반영 완료. 아래는 여전히 미점검인 잔여.

| 항목 | 사유 | 후속 |
|---|---|---|
| ~~kr-technical 페이지~~ | ✅ 2026-07-10 보충 실측 완료 | 캔들차트/KR건강도/VKOSPI 폴백 라벨 정상, KOSPI/KOSDAQ 기술분석 새로고침 버튼 배선 버그 발견(EF-19) |
| **guide 페이지** | 허브 성격 — 이전 감사(v48.45~)에서 충분 판정 이력 | 저우선 |
| **options 직접 라우트** | nav 제거된 고아 페이지(v50.35) | 저우선 — v52.39 교육 블록 렌더만 확인하면 됨 |
| ~~fundamental 실검색 플로우~~ | ✅ 2026-07-10 보충 실측 완료(NVDA) | 7/7 소스 성공, 재무·애널리스트·어닝캘린더·리스크레이더 정상 — "실적 서프라이즈"만 FMP 전용이라 지속 실패 |
| ~~AI 채팅 실문답~~ | ✅ 2026-07-10 보충 실측 완료 | **실패 확인** — Worker `/anthropic` 403 회귀(EF-20, 운영자 확인 필요) |
| ~~kr-macro 세부 2건~~ | ✅ 2026-07-10 보충 실측 완료 | "5월 수출 +64.8%"는 실측 화면에 "5월"/"최신" 라벨 이미 존재(원 서술 일부 부정확) · "2026-02" 항목은 "수출입 동향"의 품목별 표(반도체/자동차 등)에 "📦 2026-02 아카이브"로 위치 확인 |
| **브리핑 08:00 이후 상태** | 실측 시각이 자정 직후 | EF-04 수정 후 08:00 전/후 두 시점 재확인 |
| **모바일 실기기** | 데스크톱 뷰포트만 실측 | CI 뷰포트 매트릭스(88/88)가 커버하나 실기기 터치는 미검증 |
| **EF-05/07/15/18 원인 확정** | 화면 실측만으로 원인 특정 불가 | §3 Batch 3~4의 조사 단계 |

---

## §5. 실행 순서 (Sonnet 5 체크리스트)

1. `_context/RULES.md` R276/R282/R283/R287/R291 + 이 문서 §2~§3 정독. 라이브 v52.34 기준 실측이므로 **v52.39 배포 후에는 EF-12 등 일부가 이미 해소됐는지 라이브 재확인 먼저**.
2. ~~§4의 미점검 4건(kr-technical, fundamental 실검색, kr-macro 세부, EF 원인조사)을 Batch 착수 전 보충 실측~~ **완료(2026-07-10)** — EF-19(kr-technical 배선 버그)/EF-20(AI 라우트 403 회귀, 운영자 확인 필요) 추가됨.
3. ~~Batch 1(EF-01/02/04/13) → bump v52.40 + P655(버그성 묶음 postmortem) + T-테스트/계약 추가 → 게이트 전체.~~ **구현 완료(2026-07-10)** — EF-01(macro "지금·라이브" 카드로 재스코프, technical 자체는 미재현), EF-02(6개 하위항목: canonical 통일/기준일 배지/na-state/50sma 동기화/RSP-SPY 라벨 분리), EF-04(미래시각 라벨 제거), EF-13(FOMC footnote asOf+staleness, 실제 코드는 `AIO_EVENT_FRESHNESS_REGISTRY.fomc` — 설계 문서의 `AIO_EVENT_RISK_CONTEXT` 지목은 부정확했음, 코드 추적으로 정정) 전부 반영. 부수 발견: kr-technical 배선 버그(EF-19, Batch 2로 이월), AI `/anthropic` 403 회귀(EF-20, 운영자 확인 필요 — 레포 코드로 해결 불가), Chart.js 오프라인 폴백 스텁 `.defaults` 누락(테스트 작성 중 발견, 즉시 수정). T870~873 신설 + `ci-runtime-contract-check.mjs` 8건 + QA-CHECKLIST P655-Q1~9. **게이트: `node --check` 3파일 green, 헤드리스 936/936 PASS, structural/runtime-contract/ux-default-path/viewport-matrix 전부 PASS.** 버전 bump(v52.40)는 이 세션 마지막에 일괄 실행 예정. 상세: `_context/BUG-POSTMORTEM.md` P655.
4. ~~Batch 2(EF-08/09/10/11/12 + EF-19) → bump v52.41 + P656 → 동일.~~ **구현 완료(2026-07-10)** — 라이브 재검증 결과 5개 중 3개(EF-08/09/EF-11 절반)가 설계 가정과 다른 실제 원인으로 확인됨(EF-08: 3/4 부분스코어 아닌 오펀함수, EF-09: P1 우려 미재현, EF-11: 서브라벨 절반 미재현). EF-10은 Price Chart 이미 정상 확인+Key Metrics/Quarterly만 na-state 적용. EF-12/EF-19는 설계대로 재현·해소. T874~878 + 계약 7건 + QA P656-Q1~7. 게이트: 헤드리스 941/941, 로컬 9종+viewport 88/88 전부 PASS. 상세: `_context/BUG-POSTMORTEM.md` P656.
5. ~~Batch 3(EF-06/07/14/15/16) → bump v52.42 + P657 → 동일.~~ **구현 완료(2026-07-10)** — 5건 전부 코드 추적으로 근본원인 확정 후 좁게 수정(설계 가정 반전 없음, Batch 1/2와 대조적). T879~883 + 계약 6건 + QA P657-Q1~6. 게이트: 헤드리스 945/945(1차부터 클린 통과), 로컬 9종 전부 PASS. 상세: `_context/BUG-POSTMORTEM.md` P657.
6. ~~Batch 4(EF-03/05/18 — 운영+조사+구조) → /data-refresh 실행 포함, 필요 시 개별 버전. EF-17은 사용자 승인 완료(2026-07-10) — 포함해서 진행.~~ **구현 완료(2026-07-10)** — `/data-refresh` 실행(quotes 77/77·F&G·news 갱신, FRED BLOCKED 명시). WebSearch로 실제 캘린더 재확인 중 **BOK 다음 금통위 날짜 자체가 오류(7/10→실제 7/16)임을 발견·수정** — 이번 감사 전체에서 가장 파급력 큰 발견. EF-05는 재검증 결과 미재현(최신 기사 59분 전, cron 30분 간격 건강). EF-18은 근본원인이 "프록시 차단"이 아니라 **순수 404**(Naver 엔드포인트 경로 자체 소멸)임을 curl로 확정 후 올바른 경로(`/trend`)+응답변환으로 수정. EF-17(승인됨) GMO 표에 ES=F/NQ=F 추가. T884~886 + 계약 5건 + QA P658-Q1~5. 게이트: 헤드리스 948/948, 로컬 9종+viewport 88/88 전부 PASS. 상세: `_context/BUG-POSTMORTEM.md` P658.
7. 각 Batch 후 QA-CHECKLIST에 P65x-Q 블록, 이 문서 §1 매트릭스의 해당 셀 갱신(✅ 처리) — **완료**.

**Binary 완료 기준 재평가** (원 EF-01/08/09/11 절반이 재검증 결과 미재현으로 판명돼 완료 기준 자체를 실측 기반으로 재해석): (a) 홈-technical S&P 표시값 일치 — ✅ 이미 canonical 공유 확인(T870). (b) breadth 페이지 내 판정·수치 단일 소스+기준일 배지 — ✅ v52.40. (c) 엔캐리 게이지 숫자 스코어 상시 표시 — ✅ v52.41(3/4축 부분스코어 대신 오펀함수 트리거 수정으로 달성, 계산 자체는 항상 4/4 유효했음). (d) ticker 검색 후 재무 실패 상태 텍스트+자체 차트 렌더 — ✅ v52.41(차트는 이미 있었음 확인, 재무만 na-state 추가). (e) 어닝 캘린더 기본 경로 무한 대기 0 — ✅ 재검증 결과 애초에 무한 대기가 아니었음(진입 시 자동 트리거, 서프라이즈만 정직화). (f) 브리핑 미래 시각 라벨 0 — ✅ v52.40. (g) 이벤트 컨텍스트 asOf 전 표면 노출 — ✅ v52.40(실제 코드는 AIO_EVENT_FRESHNESS_REGISTRY). (h) 각 Batch 게이트 green — ✅ 전 배치 로컬 9종+헤드리스+viewport 전부 PASS. **전 항목 충족.**

**스코프 제외(사용자 기확정)**: 스크리너 FMP 컬럼 현행 유지, Telegram 페이지간 중복 현행 유지, options 페이지 재구성 안 함.

**세션 종료 시점 잔여 항목(후속 세션 권장)**:
- kr-supply `/trend` 경로 수정은 curl(200)+Node 격리 테스트로 로직 검증됐으나, 이 세션 내 CORS 프록시가 과도한 사용으로 rate-limit돼 브라우저 종단 실 fetch 성공까지는 확인 못 함 — 배포 후 라이브 재확인 권장.
- kr-macro의 CPI/PMI/GDP 등 나머지 시계열 지표(BOJ/BOE/PBOC 금리 포함)는 BOK 날짜처럼 개별 WebSearch 재검증을 하지 못함 — EF-03 범위를 벗어나 전체 재확인이 필요하면 별도 /data-refresh 세션 권장.
- EF-20(AI `/anthropic` 403 회귀)은 운영자의 Cloudflare 대시보드 확인이 여전히 필요(레포 코드로 해결 불가, 미해소 상태 유지).
