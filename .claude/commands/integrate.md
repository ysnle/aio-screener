# /integrate

## AIO Skill Operating Contract v51.73

This command is a thin wrapper. Before executing, read `_context/WORKFLOW-GOVERNANCE.md`, `_context/INDEX.md`, `.claude/skills/_shared/operating-contract.md`, and `.claude/skills/integrate/SKILL.md` completely.

Use `.claude/skills` as the active skill directory. Keep R1 as 7 surfaces when a version bump is required, and run `node scripts/ci-skill-contract-check.mjs` after skill or wrapper edits.

## Route

Run `.claude/skills/integrate/SKILL.md`.

## Final Output

Separate verified, blocked, and unverified surfaces. Do not claim browser, network, or live validation unless it actually ran.
