# AIO Screener — Claude Code 프로젝트 가이드

AIO Screener는 GitHub Pages로 배포 중인 **단일 HTML 올인원 투자 터미널**이다. 실시간 시세, 매매 시그널, 섹터 로테이션(RRG), Fear & Greed, 포트폴리오, LLM 채팅을 하나의 `index.html`에 담는다.

- 배포: `https://ysnle.github.io/aio-screener/`
- 현재 버전: **v50.33**
- **v50.33 종합 5개 페이지 개편 1차 (signal·briefing) + 전수 진단**: 사용자 "종합5+분석5 각 페이지 무엇이 필요/제거/변경 점검 후 진행, 종합5 먼저". 라이브 전수 인벤토리(home 13섹션·2170px / **signal 20섹션·4228px** / breadth 13·2620px / sentiment 6·1817px / **briefing 3·6255px**) 후 명확·안전한 것부터: **(1) Signal Exit Triggers(청산 신호=핵심 리스크, 기존 맨끝 묻힘) → 진입 체크리스트 직후 상향 + Lockout 뒤** (결론→점수→국면→조언→점수게이지→진입→청산→전문도구→상세). **(2) Signal cross-asset 안내 스텁 제거**. **(3) Briefing 뉴스 벽(40건·5900px) 표현레벨 캡** `_aioCapBriefingNews`(1500px+더보기 토글, 렌더 로직 불변). T803. **진단으로 식별한 잔여(다음 차수)**: home 결론 3중복(결론바+"지금 매매해도 될까"+Action Item) 통합·breadth 차트 6행 중복·signal 핵심 점수 "—/100"(키없으면 비는 데이터 이슈)·sentiment F&G 997px 단일덩어리. R1 7곳+캐시버스터 5곳.
- **v50.32 구조적 UI 개편 2차 — 통합·압축(추가 아님)**: 사용자 지시("추가형 패널 금지, 기존 핵심만 직관적, 근본적 UI 개편 — 브리핑에 브리핑 없고 뉴스만/시그널·심리·시장폭 직관적 사용 어려움"). **(항목1) 브리핑 상단 4중 중복 통합**: 5대 관전 정적 리스트(JS 미채움 교육문구)+Action Item 카드(디제스트 행동 줄과 ACTION_RULES 동일출처 중복)+빈 insight-box → **단일 "오늘의 브리핑" 디제스트 카드**(시장→행동→핵심뉴스→일정, accent 격상)가 본론. `AIO_PAGE_NARRATIVE_RENDERERS.briefing`→디제스트. **(항목2) Signal 흐름 압축**: Lockout/OPEX(전문 도구)를 핵심 결정 흐름(결론→점수→국면→조언→점수게이지→진입 체크리스트) 뒤(`entry-checklist-card` 직후)로 이동. **(항목3) 홈 첫 화면=시장**: 운영 경고 배너(데이터 불안정·API 키)를 `home-market-summary-banner` 아래로 이동. **(항목4) 빈 결론 박스 금지** `_aioGuardEmptyVerdicts`: 상단 승격 결론 박스(sentiment 복합판단·breadth 종합진단 보조)가 placeholder면 숨기고 데이터 도착 시 재표시(멱등). T234/T334 스펙 반전+T800 유지. **정직**: 전 21페이지 골격 표준화는 다음 단계(Signal 템플릿화부터). R1 7곳+캐시버스터 5곳.
- **v50.31 부팅 겹침 제거 + 알림 레이어 단일화 + 21페이지 WCAG 탭 영역 0건**: 사용자 스크린샷(부팅 토스트+최신화 패널+레짐 배너+API 배너 동시 겹침) 시정. (1) **v49.88 부팅 토스트 제거** — v50.23 서버 data.json 즉시 로드로 "첫 수신 갭" 소멸+topbar 갱신 칩과 중복+topbar 위 겹침. (2) **플로팅 "전체 데이터 최신화" 패널을 forceRefresh(사용자 직접 새로고침)일 때만 표시** — 부팅/페이지 진입 백그라운드 갱신마다 자동 팝업돼 콘텐츠 덮던 것 차단(백그라운드 피드백은 topbar 버튼 "갱신 N/M"). (3) **topbar 중복 칩 정리** — "갱신 N/M"(버튼)과 "전체 최신화 N/M"(data-status-panel) 동일 정보 병기 제거. (4) **a11y 21페이지 스위프**: tap target 미달 38건(13페이지) → `.aio-tooltip` 24×24·`.cross-link`/`.aio-btn-table` min-height 24px·뉴스 $티커 칩 속성 선택자(4개 생성점 공통) → **잔여 0**. 라이브 검증: 부팅 fixed 요소 교차 0·콘솔 JS 0·suite 859/18(신규 회귀 0, T673 스펙 반전+T802 겹침 가드 신규). R1 7곳+캐시버스터 5곳.
- **v50.30 구조적 UI 개편 1차 — 결론 우선(verdict-first) + 추가형 안내 패널 제거**: 사용자 지시("'주식이 처음이세요?' 같은 추가형 패널 금지, 기존 핵심 기능만 직관적으로. 브리핑 페이지에 브리핑이 없고 뉴스 나열만, 시그널/심리/시장폭은 직관적 사용 어려움"). **(1) 초보자 패널 완전 제거**(HTML+JS+트리거 — v50.26 회귀). **(2) 브리핑 다이제스트** `_aioRenderBriefingDigest`: "오늘의 브리핑" 섹션이 Claude 키 없으면 빈 껍데기 → 기존 데이터(라이브 레짐+`AIO_ACTION_RULES` 행동+뉴스 surface Top3+`MACRO_CALENDAR` 7일 일정) 4줄 합성으로 **키 없이 실제 브리핑**(`#briefing-digest`, AI 브리핑은 보완으로 유지). **(3) 결론 우선 재배치** `_aioReorderCoreSections`(기존 섹션 이동, 신규 콘텐츠 0): sentiment "복합 판단" 7번째→결론바 직후 · breadth "종합 진단" 6번째→헤더 직후 · signal Lockout 컨트롤(전문 도구)→점수·티커 아래. **(4) 설명서형 잔여 제거**: breadth "해석 가이드"·sentiment "지표 가이드"(카드 색/툴팁이 동일 정보 전달). 테스트 T794/T795/T800 스펙 반전·대체. R1 7곳+캐시버스터 5곳.
- **v50.29 전 페이지 declutter — 설명서형 요소 제거**: 사용자 지시("기초 있는 사용자 기준, 너무 친절한/자세한/원칙적인 사용 설명서 제거, 페이지마다 요소가 섞여 유기적 흐름 없음") → 원칙: **페이지 = 데이터·분석·액션만 / 설명·교육 = guide + 용어집 일원화**. (1) Page Routine 박스(목적·3단계 루틴·동선 chips·핵심/전체 토글) 21페이지 전부 제거 — `_aioRenderPageBrief`를 주입기→**제거기로 반전**(기존 pageShown/부팅/audit 호출 경로 재사용, `_aioDeclutterAllPages` 부팅 일괄). (2) "상세 해설(펼치기)" `.aio-explain`(~35K자)+`.beginner-tip` — **guide 외 전부 제거**. (3) signal "페이지 목적" 정적 박스 제거. (4) "초보자 핵심만 먼저" 요약 주입 부팅 호출 제거. 레지스트리(AIO_PAGE_BRIEFS 등)는 보존. insight-box(한 줄 정체성)·초보자 3질문 패널(액션형)·범례 유지. 테스트 스펙 반전 T154/T231/T223 + 신규 T801(비-guide 설명서형 0 가드). R1 7곳+캐시버스터 5곳.
- **v50.28 WO-6 뉴스 서버 백스톱 + WO-7 VIX 퍼센타일 브리지 + WO-11 포트폴리오 카드 동적화**: **WO-6**: `fetch-data.mjs`가 Google News RSS(2쿼리, 서버 fetch 안정)→`data.json.news`(25건)+meta.newsOk/newsCount. 클라이언트 `_aioApplyNewsBackstop()`는 **자체 뉴스(프록시)가 비었을 때만**(additive — 작동 중이면 미개입) 서버 뉴스를 scoreItem/classifyTopic 거쳐 renderFeed/home/briefing 렌더 + 부팅 12s 후 재시도 → **프록시 전멸해도 뉴스 표시**. **WO-7**: `_aioVixPercentile(vix)` — history.json 60일+ 누적 시 실측 52주 분포 순위(IV Rank 본의), 부족 시 null→기존 고정 CDF 폴백(현재 1일→폴백, ~60거래일 자동 전환, 회귀 0). **WO-11**: 초보자 패널 포트폴리오 카드가 `aio_portfolio_data` 보유 종목 수 동적 표시. R1 7곳+캐시버스터 5곳. T798~800. 서버측 newsOk는 배포 후 라이브 cron 검증.
- **v50.27 WO-9 페이로드 절감 + WO-7 히스토리 소비자 레이어 + WO-10 Worker 소스 복원**: **WO-9**: aio-tests.js(~100KB gzip 836테스트)를 정적 로드 제외 → 일반 사용자 미다운로드, `AIO.loadTests()` 동적 로드(개발자/검증). 정적 캐시버스터 6→5, ci-version-check 임계 조정. **WO-7 소비자 레이어**: `_aioLoadHistory()`(history.json 부팅 로드→`window._aioHistory`)+`_aioHistorySeries(field,minPoints)`(20일+ 시에만 실데이터, 아니면 null→기존 시드 폴백 유지)+`AIO.getHistoryDataAudit()`. 실제 차트 재배선은 ~20일 누적 후(1일치 단일점이라 시각검증 불가 — 데이터 레이어만 선구축). **WO-10**: P310에서 삭제된 `cloudflare-worker-proxy.js` 소스 복원(데이터 CORS 프록시는 **이미 배포·통합돼 동작 중** — 소스 파일만 소실됐던 것) + 잠복 버그(cleanupRateLimitMap 호출이 주석에 붙어 미실행) 시정. **정직 보류(검증/누적 제약)**: WO-6 서버뉴스(로컬 검증 불가 + 핵심 뉴스 파이프라인 위험 + 뉴스는 이미 CF Worker·다중프록시 사용)·WO-7 차트 재배선·WO-11 전면 재구조화는 별도 세션. R1 7곳+캐시버스터 5곳. T796~797.
- **v50.26 P3/WO-11 초보자 태스크 중심 시작 패널**: 21페이지 지표 터미널이 초보자에게 "어디부터 봐야 할지 막막" → 홈 헤더 직후 `#aio-beginner-panel`(additive·기존 데이터 재사용) 3대 질문 진입: (1) "오늘 시장 뭘 조심?" 라이브 VIX 밴드+F&G 존 평이한 한 줄(`_aioRenderBeginnerSummary`)+행동가이드(공포극단=역발상 분할/탐욕극단=차익실현/고변동=소액·짧은손절)+브리핑 동선 (2) "이 종목 사도 돼?" 종목 입력→기업분석 자동검색(`_aioBeginnerAnalyze`, fund-search-input+fundamentalSearch 재사용, Enter 지원) (3) "내 포트 괜찮나?" 포트폴리오 동선. "간단히 접기"(localStorage)로 숙련자 숨김. WO-5 `_aioRegimeNow` 재사용. 트리거 liveQuotes/serverDataLoaded/pageShown(home)/부팅. R1 7곳+캐시버스터 6곳. T794~795. **+ ops WO-7 history.json 생산자 + WO-8 CI 게이트(.github/workflows/ci.yml + scripts/ci-version-check.mjs, CI green)**.
- **v50.25 P1/WO-5 정적 내러티브 레짐 드리프트 가드 (stale 내러티브 근본 차단)**: 50버전·497P 반복된 stale 내러티브의 근본 원인 = 시나리오·스냅샷 prose·주간뉴스가 Claude 세션 시점(스냅샷 작성일)의 시장 레짐을 전제로 쓰여, 시장 급변 시(세션 없이) 급락일에 "사상최고 랠리"를 말함. **구조적 해법**: 작성 시점 레짐 = `DATA_SNAPSHOT`(작성일 SPX/VIX/F&G), 라이브 레짐과 `_aioRegimeDrift`로 비교(VIX 밴드 안정/보통/경계/패닉·F&G 존 극단공포~극단탐욕·SPX %) → severe 드리프트 시 (a) `#main-content` 상단 **전역 배너**(`#aio-regime-drift-banner`) "정적 분석은 [날짜] 기준 — 현재 시장 급변, 참고용"(구체 드리프트 명시·세션 닫기) + (b) `[data-static-narrative]` 블록(#macro-storyline) 강등 배지+시각 약화. 신규 `_aioSnapshotRegime`/`_aioRegimeNow`/`_aioRegimeDrift`/`_aioApplyRegimeDriftMarkers` + `AIO.getNarrativeRegimeDriftAudit()`→`getAutoOpsReadiness` 통합. 트리거: liveQuotes/serverDataLoaded/pageShown/부팅. 날짜 기반 `getScenarioFreshnessAudit`의 레짐 기반 일반화. R1 7곳+캐시버스터 6곳. T791~793.
- **v50.24 P0 구조 시정 (OPUS-HANDOFF 백로그 WO-1~4)** + ops: fetch-data.mjs Yahoo 호스트 폴백(09:03 스케줄 실패=Yahoo IP 차단 진단 후): Fable 5 라이브 전수 감사(`_context/OPUS-HANDOFF-STRUCTURAL-AUDIT-2026-06-10.md`) → Opus 시정. **핵심: "v50.23 자율 운영 전환"이 프로덕션에서 아직 사실 아님** — cron 등록 후 자동 발화 0회(수동 1회뿐) + 죽어도 감지장치 0(프록시가 조용히 죽던 것과 동일 실패 클래스). **WO-1**: cron `*/30`(정시 혼잡)→오프피크 `:17,:47` + fetch-data.mjs F&G/FRED 실패 meta 노출 + **독립 워치독 `data-watchdog.yml`**(매시 :23, data.json 3h+ stale→워크플로 FAIL→GitHub 자동 이메일). **WO-2 (P498)**: SPX ATH 하드코딩 `7412.84` 중복 → L13125만 v50.16 시정, 먼저 실행돼 `window._spxATH` 오염시키는 L12303 미시정 → SPX -2.9% 급락일 "Near ATH -0.4%" 라이브 오표시 → 단일 출처 `_aioSpxAthFloor()` 통일 + topbar 라벨 정직화(-2% 이내만 "Near ATH"). **WO-3 (P499)**: 7페이지(theme-detail/portfolio/ticker/options/kr-themes/kr-macro) on-enter refresh가 존재 않는 가상 태스크 참조(autoOps "unknown task" 7건) → `applyPageContractCompatibility`에 `CONTRACT_TASK_ALIAS` 치환(가상 파생→실제 fetch 키). **WO-4**: topbar `#server-data-age` 배지(`_aioRenderServerDataAge`) + 30분 주기 재로드(`_aioStartServerDataPolling`) + `aio:serverDataLoaded`→`refreshActivePageNarratives`. R1 7곳+캐시버스터 6곳. T786~790.
- **v50.23 데이터 백엔드 — 진짜 자율 운영 전환 (구조적 근본 개선)**: 사용자 정직 지적 "손 안 대도 알아서 도는 게 목적인데 지금 쓸 수 없는 수준" → 50버전간 증상치료(stale 수정·audit 추가)의 근본 원인 = **구조(정적 사이트 + 브라우저 CORS 프록시 + 수동 스냅샷)가 자율운영과 양립 불가**임을 진단하고 구조 자체를 변경. **(A) `scripts/fetch-data.mjs`**: 서버측(Node) 데이터 수집기 — Yahoo v8/chart(시세 60+심볼)·FRED(CPI/Core/PCE/FedRate/실업/NFP, 키 필요)·CNN F&G를 **서버에서** 받아 `public-data/data.json`으로 출력. CORS·프록시·키노출 0. **(B) `.github/workflows/refresh-data.yml`**: GitHub Actions cron(30분) — fetch 스크립트 실행 후 data.json 변경 시 커밋·푸시(`[skip ci]`). `workflow_dispatch` 수동 실행 버튼. 무료(public repo 무제한). **(C) 사이트 로더 `_aioLoadServerData`(aio-data.js, initV20DataEngine 시작 시)**: 같은-출처 data.json을 읽어 `applyLiveQuotes`(시세)+`DATA_SNAPSHOT`(매크로)+`_applyFearGreedScore`(F&G) 즉시 적용 → 이게 **PRIMARY 소스**, 기존 클라이언트 fetchLiveQuotes(프록시)는 live 갱신 레이어로 강등(프록시 죽어도 서버 데이터가 화면에 있음). 없으면 조용히 폴백. **검증(프리뷰)**: PowerShell 시드 23심볼(SPX 7386.65·VIX 19.87·KOSPI·NVDA 실값, CORS 없이) → 로더 적용 `loaderReturned:true`·serverMeta 23심볼·콘솔 새에러 0(기존 프록시 경고는 이제 무해 — data.json이 백스톱). **사용자 1회 설정**: (1) repo Actions write 권한 ON (2) FRED 무료키 → Secret(선택, 없으면 매크로만 빈값). **한계 정직**: 무료 API 없는 지표(breadth %above-MA 등)는 가짜 stale 대신 "—". data.json 확장은 SYMBOLS 배열 추가만. R1 7곳+캐시버스터 6곳.
- **v50.22 잔여 로직 영역(sentiment·options·portfolio·briefing) 공식 직접 trace 완료 + 전 21route undefined 스캔 + 3 render 함수 직접 검증**: 사용자 "남은 영역 없어? 모두 세밀하게 직접 점검? QA 스킬 불신(매번 완벽하다며 문제 투성이)" → 스킬 의존 없이 코드 직접 정독. **(1) 전 21 route 라이브 "undefined/[object Object]/NaN" 스캔 = 0건** (kr-technical 시정 확인 + 나머지 깨끗). **(2) kr/ticker 3 render 직접 trace**: analyzeKrIndex(유일한 버그 outlier·시정됨) vs 29116(US ticker)·analyzeKrTickerDeep = property 정확. fundamental render 실검색(AAPL) undefined 0. **(3) sentiment `_generateSentimentAnalysis` 이면**: F&G(25/35/65/80)·VIX(16/25/35)·PCR(0.7/1.0/1.3) 임계값 + 항복적매도(4조건)·센티먼트 클러스터(3중)·VIX-S&P 디커플링 = **전부 역발상 일관·버그 없음**(signal momScore v50.19 역U자와 방향 일치). **(4) options 이면**: IV Rank(52주 VIX 범위+evidence 라벨 verified/reference)·IV%·VVIX 임계값 합리 = 버그 없음. **(5) portfolio 이면**: `_calcPortfolioVaR`=`Math.max(0,-quantile)`·`_calcMaxDrawdown`=`(peak-cum)/peak` 둘 다 양수 반환 → 표시 `'-'+fmtPct`="-X%" 정확(이중부호 아님), Sharpe/MDD 임계값·null 가드 정상. **유일 cosmetic 시정**: null일 때 "-—" → `fmtLoss` 헬퍼로 "—"(VaR95/99/MDD 3곳). 검증: 값 "-2.34%"·null "—"·이중부호 0. **(6) 콘솔 error 직접 점검 → 진짜 버그 P497**: undefined 스캔으론 안 잡히고 **콘솔에만 표출**된 `[AIO chatSend] DOM input missing for ctxId=fundamental`(×8) 발견 — `fundamentalSearch`가 매 검색마다 존재하지 않는 per-page 패널(`chat-fundamental-inp`)로 무조건 `chatSend('fundamental')` 자동전송(통합 패널 쓰는데) → 에러 + 공유 Claude 쿼터 자동소진 시도. per-page 패널 있을 때만 전송, 없으면 통합 입력창 프리필(opt-in)로 시정. 라이브 검증(MSFT): DOM-missing 에러 0. **정직한 결론**: breadth(부호)·kr-technical(property)·fundamental(자동전송) 버그는 **특정 지점 집중**이었고, 핵심 stat/sentiment/options 로직은 견고 — 전 영역 문제 투성이 아님. **15+route 본질 trace + 21route undefined 스캔 + 콘솔 error 점검으로 직접 점검 한 바퀴 종료.** 검증: badge v50.22·undefined 0·portfolio fmtLoss 정상·콘솔 JS 에러 0(P497 시정 후 proxy 경고만). R1 7곳+캐시버스터 6곳+P496~497.
- **v50.21 잔여 4페이지(technical/fxbond/fundamental/themes) 공식 line-by-line trace 완료 + kr-technical 렌더 버그 클러스터 완전 시정**: 사용자 "계속해서 완벽하게 마무리" → v50.20에서 미완으로 정직 보고한 4페이지 전체 trace. **(1) technical/kr-technical 렌더 버그 클러스터 (analyzeKrIndex)**: v50.20 dipData에 더해 **crossData·divData도 property 불일치 발견** — `_detectCrossSignals`는 `{gc20_50,gc50_200}`(한글 '골든크로스') 반환인데 표시는 `crossData.cross20_50==='golden'`(없는 키+영문값) → "undefined"+항상 회색 / `_detectDivergence`는 `{bearishDiv,bullishDiv}`(불린) 반환인데 표시는 `divData.type` → "undefined". 표시를 실제 반환 구조(gc20_50 + 한글 비교 + null→'평탄' / bullishDiv·bearishDiv → 강세/약세 다이버전스/없음)에 맞게 시정. 라이브 검증: kr-technical "undefined" 0건 (cross "평탄"·dip "조정(관망) 53/100"·div "없음"). **(2) 이면 타당 확인(시정 불요)**: themes `classifyRRG`(index.html:25266) = 표준 RRG 사분면 매핑 정확(Leading: RS-Ratio≥100&Mom≥100 / Improving / Weakening / Lagging) · technical `_detectStage`(stage/label/confidence)·`_detectTrendPosition`(position/week52/ema20/sma50)·`_assessEntryQuality`(grade/score/reasoning) 반환 정합 · fxbond DXY 신호 밴드(100/103/106) 합리 + 수익률곡선 v50.17 시정 + Cross-Asset 4축 매트릭스 별개 정상(`computeCrossAssetCorrelation`은 dead code, 경미) · fundamental FMP 검색 기반 + 재무차트 v49.72 검증. **결론**: 종합 5 + 시장분석 5 + KR 5 = 15페이지 이면(공식/가중치/임계값/verdict 매핑/render property) 전수 완료. 주요 버그 클래스(verdict 부호·property 불일치·default 낙관·snapshot vs live·stale 내러티브) 전부 커버. 검증(SW 해제 fresh): badge v50.21·kr-technical undefined 0·콘솔 JS 0. R1 7곳+캐시버스터 6곳+P495.
- **v50.20 나머지 8페이지 이면 전수 (시장분석+breadth+KR) — verdict 부호 버그 + undefined 렌더 버그 시정**: v50.19(signal/home) 후 사용자 "계속해서 마무리" → breadth·macro·KR 5페이지 이면 직접 점검. **(1) breadth `diagnoseBreadthConsensus` 부호 버그 (높은 영향)**: verdict 매핑에서 `consensus > 0.1`(양수 0.1~0.4 = 약한 강세 합의)을 **"약세 우위"**로 표시 → breadth가 강세 쪽인데 사용자에게 정반대 결론. → "강세 우위"로 시정(aio-core.js:9884). **(2) kr-technical `_classifyDip` undefined/scale 버그**: `_classifyDip`는 `{classification, score(0-100)}` 반환인데 표시 코드(index.html:29844)가 `.label`(없음→**"undefined"** 렌더)·`/5`(실제 0-100)·`.reasoning`(없음) 사용 → "undefined (점수: 53/5)" 표시. → 함수에 `label`/`reasoning`(실제 조정 근거: 50일선 위치·조정 깊이·저점·거래량) 추가 + 표시 `/5→/100`. **(3) 이면 타당 확인(시정 불요)**: macro 온도계(VIX30·곡선25·유가20·달러15·신용10 가중 + triple-hawk/스태그플레이션/리스크온 교차보정) 정교 · kr-macro "한국은행 기준금리 현황"=과거 금통위 결정 로그(4/10 7연속 동결 등)+다음 금통위 7/10 정확 · kr-home 신용잔고 "정적·D+5일" 라벨됨 · kr-supply/kr-themes 클린. **정직한 잔여**: technical(Weinstein/RSI per-ticker)·fxbond(cross-asset)·fundamental(FMP)·themes(RRG) 표준 지표라 데이터 감사(클린)+spot-check만, 전체 공식 trace는 미완(verdict-mapping 외 버그 위험 낮음). 검증(SW 해제 fresh): breadth 양/음 합의 부호 정상·_classifyDip label/reasoning/score 정상·콘솔 0. R1 7곳+캐시버스터 6곳+P493~494.
- **v50.19 트레이딩 로직 이면(공식/가중치/임계값) + 실효성 보강 (signal·home)**: 사용자 "각 페이지 빠진 부분 없어? 실질적 의미와 이면까지? 실제 매매에 도움 될 것 같아?" → 증상(stale) 너머 **로직 타당성·페이지간 정합·행동가능성**을 점검. signal `computeTradingScore` 이면 직접 추적: 5개 가중(변동성25·모멘텀25·추세20·시장폭20·매크로10)+교차보정 7종은 정교(실제)하나 **3약점 발견·시정**. **(1) 시장폭 입력 취약**: `window._breadth200`(레거시 20SMA명) 미로딩 시 기본 **75(낙관 편향)** → 실제 57보다 높아 핵심 매매 점수 왜곡 → `_breadth20`→`DATA_SNAPSHOT.breadth20sma`(57) 폴백 체인(computeTradingScore L22497 + computeExecutionWindow L22596). 검증: breadthScore 88→**72**(정직). **(2) 일반 손절**: `exit-spx 6,650`=SPX×0.9(기계적 -10%)인데 라벨은 "기술적 지지선" → **200일선(주요 추세 지지)** 우선, 이미 하회/미가용 시 50일선→-10% 폴백 + basis 라벨 동적화(`exit-spx-basis`). **(3) F&G 모멘텀 cross-page 모순**: signal momScore는 극단 탐욕(≥75)을 **85=최대 강세**(추세추종)로 보는데 home sentimentAction은 극단 탐욕에 "**차익실현·축소**"(역발상) → 정반대. momScore를 **역U자**(건강한 탐욕 55~75=피크 74, 극단 탐욕 fade 66, 극단 공포 15→25 역발상 floor)로 시정 → signal↔home 극단에서 같은 방향 수렴 + 가중치 라벨에 데이터 소스 명시(모멘텀=F&G·추세추종). home Action Plan(VIX 포지션 사이징+F&G 역발상) 이면 타당 확인. sentiment VIX 기간구조 "백워데이션(패닉)"=VIX9D>VIX×1.02 분기 타당 확인(시정 불요). **정직한 잔여**: breadth consensus·briefing·technical·macro(온도계 정밀)·fxbond·fundamental·themes 이면 정밀 + KR 5페이지 = 순차 계속. 검증: computeTradingScore 무오류 실행·breadthScore 72·momScore 34(F&G 39)·콘솔 JS 0. R1 7곳+캐시버스터 6곳.
- **v50.18 10페이지 분석/해석 텍스트 직접 정독 + stale 내러티브 근본 시정**: 사용자 "테마/트렌드도 확실히 본 거 맞아? 차트/지표 위주만 본 거 아냐? **분석/해석/섹션 설명 텍스트도 하나씩 직접 점검**" 지적 — 정직히 인정: v50.17은 차트/데이터/날짜토큰 위주였고 산문(prose)은 안 읽음. v50.18에서 10페이지 분석/해석 텍스트를 실제 추출·정독해 "날짜 토큰은 아니나 현실과 안 맞는 stale 내러티브"를 시정. **(A) themes 시장국면 모순**: 동적 readout "Mid Cycle (Expansion)" vs 정적 블록 "Late-cycle+스태그플레이션(최악의 조합)" 정면 충돌 → 정적 블록을 "단정"에서 "조건부 교육(양 국면 설명+동적 readout으로 판정 위임)"으로 전환. **(B) macro 스토리라인 전제 오류**: "이중 위험(고용은 둔화↓...)" — 5월 NFP 172K(견조)로 정반대(강한 고용이 인하 철회 원인) → "견조한 고용+끈적한 인플레·유가 리스크"로 정정. **(C) signal 유가 내러티브 stale (근본·이중)**: CP1/CP6 리스크보드가 `DATA_SNAPSHOT.wti`(6/5 스파이크 97.2)를 읽어 "고점권·$110 재급등" 표시(live $89인데) → ① `getCP1Text`/`getCP6Text`를 live CL=F/BZ=F 우선으로 ② `renderCPTexts`가 `applyDataSnapshot`에서만 돌고 `aio:liveQuotes`엔 안 돌던 갭 → signal liveQuotes 핸들러에 renderCPTexts 추가. 검증: "$89.52 (안정화 기대)". **(D) breadth cell-level 모순**: 50SMA 카드 52%인데 바로 아래 해석 텍스트 정적 "46% — 50% 미탈환"(결론 반대) → `updateBreadthBars`에 breadth-50sma 막대·readout 동적 갱신 추가 → "52% 상회". **클린 확인**: home(뉴스 당일·포지션 동적)·sentiment(F&G/VIX 동적)·briefing·technical·fxbond·fundamental 6페이지 prose 정독 + stale 패턴(고용둔화·유가고점·휴전·랠리지속·regime단정) 교차 스캔 0건. **교훈 재확인**: preview SW가 stale JS 서빙 → unregister+caches.delete+reload로 fresh 검증(서버 fetch로 디스크 정합 대조). T783~T785. R1 7곳+캐시버스터 6곳.
- **v50.17 종합·시장분석 10페이지 이면 직접 점검 + stale 콘텐츠 근본 회귀 방지**: 사용자 "하나씩 직접 확인해. 겉만 훑지 말고 이면까지" + "근본적 개선도 같이?" 요구로 10개 핵심 페이지를 라이브 preview에서 데이터 이면까지 직접 점검 후 **실 시정 + 자동 감지 레이어** 신설. **(A) 전역 시장폭 칩 오라벨 근본 시정**: `updateMarketPulse` 시장폭 칩이 v50.6에서 제거한 `window._breadth200`(undefined)→`calcSectorBreadth`(11섹터 당일 양봉비율 27%)로 폴백 → 실제 breadth(50일선 위 52%)와 불일치하는 **"27% 약세"**(빨강)를 모든 페이지에 표시. `_breadth50`→`_breadthLiveData.abv50`→`_breadth20`→`DATA_SNAPSHOT.breadth50sma` 폴백 체인으로 시정 → "52% 주의" 정합. **(B) sentiment 차트 3개(NAAIM/II/HY) 빈 화면 근본 시정**: `_lazyInitChartPage`(IntersectionObserver rootMargin 100px)가 내부 `.content` 스크롤 컨테이너에서 화면 밖 차트(1300px+)를 못 띄움 → 진입 후 1.4s 안전망 타이머로 미렌더(LWC 컨테이너 무·캔버스 빈) 차트 강제 init. **(C) fxbond yield curve "수집 대기" 멈춤 근본 시정**: `initYieldCurveChart()`가 macro에서만 호출 + `koreaCurveChart||yieldCurveChart` 셀렉터가 항상 koreaCurveChart 선택 → fxbond 자체 init 누락 + macro도 오렌더 + 공유 `_ycChart` destroy 충돌. `initYieldCurveChart(targetId)` 파라미터화 + per-canvas `_ycCharts{}` 인스턴스맵 + fxbond 페이지 init에 호출 추가 + 라벨 "한국 국채"→"미 국채"(실제 US Treasury 플롯) 정직화. **(D) signal 단기 시나리오 + Event Runway de-stale**: `signalShortTerm`(5/31, 9일 stale — AVGO 발표·NFP 172K·Computex 등 끝난 이벤트를 미래 트리거로 표시)·`AIO_EVENT_RISK_CONTEXT`(asOf 5/14, 26일 stale, 타임라인 4건 전부 과거) → 6/9 현재 전망(6/10 CPI·6/16-17 FOMC·6/25 PCE·중동)으로 교체. **(E) 근본 회귀 방지 레이어**: `getScenarioFreshnessAudit`가 `scenarios`만 보고 `signalShortTerm` 누락(9일 stale 미탐지) → 두 블록 모두 점검 + `signalStaleDaysThreshold:7`(주간 갱신 기대). **신규 `AIO.getEventTimelineStalenessAudit()`** — 수동 createdAt 의존 없이 실제 `timeline[].date`/`asOf`를 읽어 "전부 과거"·"asOf 14일+"를 self-validating 감지(Event Runway 26일 stale 재발 차단). 둘 다 `getAutoOpsReadiness`에 배선(이전 scenario audit은 orphan). **(F) home(SPX_ATH 폴백 7412.84→DATA_SNAPSHOT.spxATH·regime VIX 맥락·topbar aio:liveQuotes 갱신)·briefing(뉴스 35건 당일 최신 재검증·archive 마킹 확인)·technical/macro/themes/fundamental 클린 확인**(LWC 내부 캔버스 33개는 false positive — 조상 `tv-lightweight-charts` 확인). T783~T785 회귀 가드. 라이브 검증: 시장폭 52%·sentiment 3차트 렌더·yield curve "정상 곡선"·audit ok·콘솔 JS 0(FRED 키 경고만). R1 7곳+캐시버스터 6곳.
- **v50.16 시장 폭 차트 전체 사이클 복원 + FRED 폴백 정합**: 사용자 라이브 지적("0/50/100 비율 맞아? 바뀌지도 않고") 직접 스크린샷 확인 후 시정 — 시장 폭 히스토리 차트 Y축 라벨 잘림(padX 8→34) + 평탄 배열(최근 20일 고점 구간 → 전체 사이클 3월 저점 33%→랠리 86%→6/5 급락 57% 22포인트) + `slice(-14)`→전체. FRED 폴백 현재값 정합(CPI 2.9→3.8·Fed 4.5-5.25→3.625·실업 4.1→4.3). 교훈: `?v` 고정 캐시버스터가 같은 버전 내 JS 편집을 캐시 서빙 → v50.16 bump로 fresh fetch 강제(배포 필수). 라이브 검증: 히스토리 차트 명확한 사이클+축, 콘솔 0.
- **v50.15 텔레그램 3채널 7일 시장 인텔리전스 전방위 통합**: 사용자 요청으로 @insidertracking·@aetherjapanresearch·@bornlupin 3채널의 최근 7일 소식을 **공개 `t.me/s/<채널>` 프리뷰**(로그인 불필요)로 포착해 앱 5개 surface에 통합. 핵심: **NVIDIA 한국 AI 인프라 블리츠**(젠슨 황 방한 — SK하이닉스/삼성/네이버 1GW AI팩토리/SKT/현대차), **메모리 슈퍼사이클 리레이팅**(CLSA 삼성 54만·SK하이닉스 370만·마이크론 $1,320), **6/5 포지셔닝 청산 셀오프**(S&P -2.6%·NFP 172K→2Y 4.17%·골드만 인하철회), **JPM AI·에너지**(DC 485→950TWh), 중동 재고조(유가 +4-5%). 통합: (1) HOME_WEEKLY_NEWS 5대 뉴스 교체 (2) DATA_SNAPSHOT 6/5 종가+6/8 상태(SPX ~7,388·VIX 19.38·NFP 172K·2Y 4.17%·WTI 97.2·원화 1,529·미러 R184 동기화·currentTopic 신규) (3) AI채팅 `_getV48IntegratedContext` v50.15 블록 전 페르소나 주입 (4) KR_THEME_CATALYSTS semi/ai-sw/auto (5) KNOWLEDGE-BASE PT-I~L. **2차 심화(`?before=` 페이지네이션 6/1~6/5 기관 리포트 15+건 프레임워크)**: 채팅 2차 블록 Mizuho ASIC(수요-공급 갭=구조적 부족)·MS AVGO TP$502(beat-but-fell)·JPM Computex/포지셔닝·Bernstein·Jefferies MSFT$575·한국 메모리 컨센(Daeshin 56만/340만)·WSTS·BofA LNG·SemiAnalysis "SOCAMM 셀오프=오독" 정정. KNOWLEDGE-BASE +2(PT-M/N). LG 5대그룹 동맹·shipbuilding LNG. **(B) 뉴스 surface 비교**: 3채널 이미 rsshub.app 소스이나 aether만 publicMirror 보유→insidertracking/bornlupin에 publicMirror+pipelineRole 추가(rsshub 폴백). 검증: 라이브 reload 1·2차 전부 반영·콘솔 JS 0. R1 7곳+캐시버스터 6곳.
- **v50.14 프론트엔드/UX 근본 보강: 회귀 방지 인프라 + 접근성**: v50.13의 개별 증상 시정을 근본화. **(A) R206** `AIO.getVisibleDevMarkerAudit()` 신설 — 모든 `[id^="page-"]` 가시 textContent에서 dev/버전 마커(§NN·vNN.NN·RNN·`*_REGISTRY`·`MACRO_CALENDAR`·`DATA_SNAPSHOT`·"Claude Mythos"·"Fallback Only"·"prominent") 검출, 앱버전배지·developer-note·archive·**외부콘텐츠(RSS `.news-item-*`/LLM `.acp-bubble`)** 제외(법률 `§10(b)` 오탐 차단). `getAutoOpsReadiness` 통합. **이 audit이 v50.13이 놓친 동적 누출 즉시 검출**: macro `generateMacroStoryline` "DATA_SNAPSHOT 즉시 폴백"→"정적 스냅샷", options PCR 소스라벨 'DATA_SNAPSHOT'→'정적 스냅샷', home 가중치 tooltip "v49.28/R64 WEIGHT_REGISTRY" 제거 → 가시 dev마커 0(clean+post-suite). **(B) R207** `AIO.getAccessibilityAudit()` 신설 — 활성 페이지 tap target(WCAG AA 24×24)·<10px 폰트·접근이름 누락 측정, home 온보딩 버튼 ≥24px 조정 → under24/noName/font 0, status ok. **(C)** R206/R207 RULES.md 추가 + T776~T781 회귀 가드(T777은 `AIO_PAGE_BRIEFS.breadth.steps` 정합). **(D) 재검증 심층 보강**: 속성 dev마커 19건(8페이지) 발견·시정(표 a11y aria-label 'v50.14' 누출·`_getDataFreshness` tooltip 'DATA_SNAPSHOT'→'정적 스냅샷' 13건·home/breadth title 마커) + **audit을 속성(title/aria-label/…)까지 확장** + **21페이지 라이브 진입 스위프로 <10px 폰트 7페이지 발견 → 앱 전역 폰트 ≥11px 일괄 상향**(인라인+동적 칩+DOM API). 교훈: SW 동일버전 캐시가 편집 JS 가림→unregister+caches.delete+서버 fetch 대조. 최종 21페이지 스위프: dev마커(text+속성) 0·<10px 폰트 0·접근이름 0·runTests 836/20(신규 회귀 0).
- **v50.13 21페이지 라이브 프론트엔드/UX 세밀 점검 + 시정**: 21 route를 하나씩 진입해 섹션 단위로 실제 콘텐츠를 읽고 클러터/중복·초보자 직관성 시정. 가시 dev/버전 마커 제거(§63/§58/§62/§55·"Claude Mythos"→"사이버보안"·"v49.64에서 제거"·"v50.x refreshed/기준"·"Fallback Only/prominent"). 전역 배지 한글화(STALE/STATIC/OK/REF→오래됨/정적/최신/참고). 홈 쿼터 중복 제거. breadth routine 200일선 제거. 용어집 +7(260→267). 가시 dev마커 0·콘솔 JS에러 0·회귀 0. 보류: 4월-2026 내러티브 de-stale. 리포트 `_context/FRONTEND-UX-AUDIT-2026-06-05.md`.
- **v50.12 AI 채팅 기술적 분석 종목별 실측 데이터 주입**: 비-fundamental 채팅 4개(외환채권/기술/테마/매크로) 조사 — 방법론은 기관급 + live 헤더 최신이나 technical만 종목별 실측 기술지표 미주입. 엔진(`fetchOHLCVWithFallback`+`calcTechnicalSnapshot`+`calcExtensionHeat`)은 존재했으나 채팅 미사용 → `_fetchTechnicalDataForChat` 신설(재사용). technical/signal/ticker에서 라이브 OHLCV → RSI/MACD/MA정배열/Weinstein Stage/ATR이격/확장도 주입. stale 버전 테스트 4개 semver 시정. T775. 잔여: macro/fxbond/themes 4월-2026 동결 내러티브 de-stale + themes RRG 실측 주입.
- **v50.11 /data-refresh (2026-06-05)**: full static-data audit-first refresh. US 6/4 close (SPX 7,585 record · Nasdaq 26,831 · VIX 15.40 · WTI $93.03) + Broadcom −12.6% AI guidance miss. DATA_SNAPSHOT body↔_fallback mirror drift fixed (vvix/breadth50/spxATH). `_aioRecomputeMacroCalendar` generalized to auto-advance BOTH US + KR macro calendars with multi-cycle catch-up (KR stale root fix R78/P255). Today's briefing event layer (AVGO result + NFP-today framing), STATIC_CONTENT_LIFECYCLE current-DOM registration, AIO_SCENARIO_REGISTRY 6/4 re-date, HOME_WEEKLY_NEWS re-curation. data-snap YoY seeds + sink mismatch + geopolitical review (5×6/4) fixed. data-refresh SKILL.md gained U-group (briefing/calendar/current-topic). May CPI/NFP/PCE remain unpublished (not generated). T681/T564/T759/T760 reconciled.
- **v50.10 AI chat qualitative data via Claude web research**: `_shouldUseClaudeWebSearch` trigger expanded to qualitative-analysis intents (supply-chain/TAM/competition/moat/13F/CEO/business-structure) so the already-wired `web_search_20250305` actually fires for low-data perspectives. System-prompt research directive, native web_search citation rendering (`_searchCitationsHTML` engine:'claude'), v50.9 badge upgrade to "웹검색 출처 기반", and `_QUOTA_LIMITS.claudeWebSearch` cost cap. T772~T774.
- **v50.9 AI chat high-risk perspective confidence disclosure**: consolidated low-confidence summary line in `_fetchTickerDataForChat` + visible amber badge in chatSend, both from `AIO_ANALYSIS_FRAMEWORK_REGISTRY.highRiskFields(true)` (no hardcoding). Fixed pre-existing P482 (computeMacroBeta sync `.catch` crash). T771.
- **v50.4 static market data/calendar refresh**: updated official June 2026 macro calendar (NFP 6/5, CPI 6/10, FOMC 6/16-17, PCE 6/25), DATA_SNAPSHOT metadata, HOME_WEEKLY_NEWS, briefing event layer, risk pinned events, options/KR macro copy, and AI briefing static context. Computex/GTC Taipei and SpaceX IPO are handled as verified-topic/watch layers, while unpublished CPI/NFP/PCE numbers must not be generated. T759~T762 added.
- **v50.3 text surface governance**: added AIO_TEXT_SURFACE_CONTRACTS, AIO.getTextSurfaceAudit(), and AIO.applyTextSurfaceHygiene() so all 21 route pages classify visible/tooltip text as current market claim, education explainer, operational status, developer note, risk disclaimer, or reference archive. The text audit is wired into AIO_AUDIT_REGISTRY and AIO.runEvidenceDeploymentGate(); high-risk visible dev markers and fixed-date briefing/action copy were removed. T755~T758 added.
- **v50.2 news surface contract reform**: added AIO_NEWS_SURFACE_CONTRACTS, AIO.buildNewsSurfaceModel(), and AIO.getNewsSurfaceAudit() so home core news, 08:00 KST market briefing, and 48h market-news exploration share freshness/source-tier/score/duplicate/verification/empty-reason policy. Home no longer renders expired HOME_WEEKLY_NEWS as actionable core news; briefing AI summary input is limited to verified/current items and separates secondary/unverified items into review; deployment gate and AutoOps readiness now include news surface audit. T749~T754 added.
- **v50.1 trading decision evidence gate**: added `AIO.getTradingDecisionInputEvidence()` and `AIO.getTradingDecisionLogicAudit()` so SPX/SPY/VIX/10Y/HYG/DXY/WTI trading inputs, stale/reference fallbacks, breadth proxies, ticker checklist static screener paths, Weinstein ATH proxy, and options IV Rank VIX range are audited before deployment. `AIO.runEvidenceDeploymentGate()` now carries trading-decision findings. SPX MA hardcoded decision fallbacks were neutralized, Weinstein ATH proxy now exposes evidence status, and IV Rank uses VIX history when available. Runtime version format is now capped at one or two decimal digits. T745~T748 added.
- **v50.0 evidence-first 21-page contract foundation**: added `AIO_PAGE_CONTRACTS` for all 21 route pages plus glossary overlay, derived profiles/refresh/deep-audit maps from that contract, introduced `EvidenceStore`, `SourceAdapterRegistry`, `AuditRegistry`, `FormulaRegistry`, `AIO.getAllPageContentEvidenceMatrix()`, and `AIO.runEvidenceDeploymentGate()`. `DATA_SNAPSHOT` is now treated as snapshot/reference/historical unless promoted by verified evidence. AI chat now has EvidenceStore context plus numeric/date evidence reference audit. T737~T744 added.
- **v49.112 full critical-10 content evidence matrix**: added `AIO.getCritical10ContentEvidenceMatrix()` so every live cell, snapshot, snap date, chart-like element, static numeric text, and market narrative across the 10 US pages is classified as pass/warn/block/needs_evidence. External reference quotes can now block mismatched visible prices. T734~T736 added.
- **v49.111 critical-10 market situation deep audit**: added full page content inventory plus current quote/regime comparison. `AIO.getCritical10MarketSituationAudit()` checks visible values, sources/truth, stale dates, and market narrative conflicts; `AIO.refreshCritical10MarketSituationAudit()` fetches, cross-checks, rebinds, then audits. T728~T733 added.
- **v49.110 critical-10 market surface audit hardening**: added `AIO.getCritical10MarketSurfaceAudit()` and wired it into comprehensive page freshness/readiness so visible source-missing, binding-missing, truth-blocked/reference-only market cells, stale tasks, and stale snap dates cannot report as OK. T724~T727 added.
- **v49.109 multi-source quote cross-validation**: added source-family quote cache plus Yahoo/Naver/Stooq/Finnhub/FMP/CoinGecko/FX cross-checks. `AIO_DATA_TRUTH_GATE` blocks trading-use data when independent live sources materially disagree, and AI chat preflight exposes `cross=<status>/<count>` before using stock price or valuation numbers. T719~T723 added.
- **v49.108 DataTruthGate hardening**: added `AIO_DATA_TRUTH_GATE`, `AIO.evaluateDataTruth()`, and `AIO.getDataTruthAudit()` so quote values must pass source, timestamp, stale-age, sanity-range, and pct/previous-close coherence checks before they can remain decision-usable. Truth-blocked values are marked `reference-only` in DOM lineage and injected into AI chat freshness preflight. T715~T718 added.
- **v49.107 Critical-10 freshness pipeline hardening**: manual refresh, page-enter refresh, and AI freshness preflight now use the combined comprehensive 5 + market-analysis 5 symbol universe. `AIO.applyLiveDataToDom()`, `AIO.verifyPageLiveDataBinding()`, `AIO.verifyCritical10LiveBindings()`, and `AIO.repairPageLiveDataBinding()` make visible DOM binding explicit for every `data-live-price/chg/pct/field` sink, so refresh completion cannot be mistaken for actual screen update completion. T711~T714 added.
- **v49.106 AI answer diversity/current-data contract**: AI 채팅에 `_buildChatAnswerCoverageContext()` 신규. 답변을 decision memo, ranked comparison, valuation memo, earnings review, technical setup, portfolio risk note, beginner explanation 등으로 의도별 분기하고, 최신 가격/시총/실적/가이던스/목표가/뉴스/공시/매크로 수치는 Claude 학습 데이터가 아니라 주입된 live/FMP/SEC/Naver/Finnhub/news/web-search/age-labeled snapshot 데이터만 쓰도록 system prompt에 강제한다. T708~T710 추가.
- **v49.105 AI answer freshness hotfix**: `forceFresh` now bypasses all three stale-answer paths for stock chat answers: `_chatTickerCache`, `_liveData` immediate cache returns inside `dynamicTickerLookup`, and the `ensureFreshDataForUse` minGap throttle. Chat answer preflight calls `dynamicTickerLookup(t,{forceFresh:true})`, `_fetchTickerDataForChat(...,{forceFresh:true,reason:'chat-answer'})` forces quote lookup instead of returning cached live data, and T707 guards the regression.
- **v49.104 AI 채팅 자동 최신화 답변 시스템 보강**: 사용자 "자동 최신화 운영 시스템 기반으로 AI 채팅도 자동 최신화 답변 시스템 근본 보강, 특히 주식 종목 시세/기업 분석 최신화 무조건 보장" 대응. `AIO.ensureFreshChatAnswerData()` 신규로 종목 질문 strict preflight 수행: 중앙 `ensureFreshDataForUse({forceFresh:true})`, `runScheduledRefresh({symbols,reason})`, `_chatTickerCache` 해당 종목 무효화, `dynamicTickerLookup()` 개별 재조회. `_fetchTickerDataForChat(tickers, opts)`는 `forceFresh/chat-answer`에서 5분 캐시를 우회. `chatSend()`와 `chatSendUnified()` 모두 strict preflight + fresh ticker data block을 사용하고, 단일 종목 질문은 기본적으로 기업 심층 데이터 경로를 탄다. T703~T706 추가.
- **v49.103 5개 종합 페이지 전체 표면 최신화 감사 보강**: 사용자 "각각의 차트/지표/시세/수치/수식/텍스트 등등 전체 기능과 내용들 하나도 빠짐없이 조사 후 근본 개선" 대응. 실제 DOM의 `[data-live-price]`, `[data-live-chg]`, `[data-live-pct]`, `[data-live-field]` 싱크를 `collectPageDataSymbols()`에 자동 합류시켜 화면에 보이는 라이브 시세/지표가 page profile quote refresh 범위에서 빠지지 않게 보강. `AIO.getComprehensiveSurfaceIntegrityAudit()` 신규로 home/signal/breadth/sentiment/briefing의 live/snap/chart/formula/static-number/stale-text 후보를 페이지별 감사하고, `AIO.getComprehensivePageDataFreshnessAudit()`가 이 표면 감사를 함께 반환. T701~T702 추가.
- **v49.102 5개 종합 페이지 개별 데이터 최신화 범위 보강**: 사용자 "5개 페이지 모든 개별 데이터가 빠짐없이 실제 최신 데이터로 업데이트 되게끔 근본 작업" 대응. `_aioRefreshPageData()`가 page-onenter 시 `_runScheduledTask()`를 직접 흩뿌리지 않고 `AIO.runScheduledRefresh({keys, pageId, symbols, options})` 중앙 이벤트 경로를 사용. `DATA_REQUIREMENT_PROFILES`/동적 수집 심볼을 quote refresh에 전달해 5개 페이지에 필요한 심볼 범위 전체를 최대한 prewarm. `REFRESH_SCHEDULE.quotes.fn(opts)`는 `fetchLiveQuotes(opts.symbols)`를 사용하고, `technicals.fn(opts)`도 page/profile 심볼 우선. `AIO.forceRefreshAllData()`는 5개 종합 페이지 심볼 union을 포함. `AIO.refreshAllComprehensivePages()`와 `AIO.getComprehensivePageDataFreshnessAudit()` 신규로 5개 페이지 task/symbol/sink/charts/missing-live 샘플 감사. visibility resume stale task도 중앙 `runScheduledRefresh` 경로로 통합. T699~T700 추가.
- **v49.101 전체 최신화 UX/파이프라인 근본 보강**: 사용자 "스크리너 전체 데이터 최신화 진행 중 알 수 있게끔 UX/UI/레이어/파이프라인 보강한 것도 세밀 조사 후 모두 순차 근본 보강" 대응. `runScheduledRefresh()`가 `aio:refresh:start/progress/done` 이벤트와 `AIO.refreshState/getRefreshState()`를 발행하도록 중앙 상태 모델 추가. `_runScheduledTask()` 성공 시 scheduler key→`_lastFetch` key 매핑으로 freshness 기록 일관화. 상단 `globalRefresh()`는 직접 fetch 6개 호출 대신 `AIO.forceRefreshAllData()`/`runScheduledRefresh({forceRefresh:true})` 중앙 경로 사용. `aio-refresh-progress-layer`로 11개 태스크 X/Y, 현재 소스, 완료/스킵/확인필요를 표시. `data-status-panel`/API dashboard/showDataError는 active refresh 중 덮어쓰기 방지. 뉴스 진행률 wrapper/bar를 실제 표시·갱신하고 캐시 hit 상태도 명시. 부트 로더는 quote 단독 4초 종료를 제거해 5개 소스 진행률 의미와 맞춤. T695~T698 추가.
- **v49.100 자동 최신화 운영 시스템 근본 보강**: 사용자 "종합 5개 페이지 자동 최신화 운영 시스템 세밀 전수 조사 후 모두 순차 보강" 대응. `_runScheduledTask`가 `{forceRefresh,pageId}` 옵션을 task까지 전달하고, `news`는 주간 뉴스 만료 시 `fetchAllNews(true)`로 실제 네트워크 갱신. `_assignRefreshScheduleFunctions()` 즉시 배선으로 초기 15초 on-enter no-op 제거. `AIO_PAGE_REFRESH_MAP`을 `DATA_REQUIREMENT_PROFILES`와 정합(home/signal/breadth/sentiment/briefing) + coverage audit가 task fn 누락/profile mismatch까지 fail. 기술 지표는 signal 입력 심볼 우선. no-update/skipped task는 `_lastOk` 성공 기록 금지. v49.98 script cache-buster 잔존 제거, 6/1 12:00 KST 경과 이벤트 문구 상태 추적으로 전환, AAII/PCR/Breadth 정적 scaffold가 DATA_SNAPSHOT 최신값을 덮어쓰지 않도록 보강.
- **v49.93 금리 필드 외부 실측 1:1 대조**: 사용자 "모든 데이터 하나씩 세밀 대조" 연속. 금리 stale 4건 — 한국 국고채 10Y 3.72→4.27%(2023.11 이후 최고, 한은 인상기대) + 3Y 2.82→3.20 / BOE 4.50→3.75%(0.75%p stale, 4/30 동결) / PBOC 3.10→3.00%(1Y LPR 동결). ECB 2.15 일치. **누적 cell-level 값 대조 v49.91~93 = 19개 필드 stale/오류 시정** (PCE/VKOSPI/DAX/Nikkei/HangSeng/FTSE/WTI/Brent/Gold/Silver/BOJ/한국채권/BOE/PBOC).
- **v49.92 DATA_SNAPSHOT 나머지 필드 외부 실측 1:1 대조**: 사용자 "모든 데이터 실제로 하나씩 세밀 대조". **CRITICAL: VKOSPI 74.02 오류(P453)** — v49.87 WebSearch "74.02" 검증 없이 수용했으나 VIX 15.74 + KOSPI 사상최고와 양립 불가(정상 12~25, 74=코로나 패닉) → 18.20 시정. **글로벌 지수 stale**: DAX 23200→25068(사상최고권 +1868 큰 stale)/Nikkei 64999→64693/Hang Seng 25947→25006/FTSE 10611→10428. **원자재**: WTI 88.30→90.50(이란 충돌 재개, 5/27 평화보도 가짜)/Brent 94.50→96.29/Gold 4483→4411/Silver 71.50→73.51. **BOJ 0.50→0.75**(인상 stale). **R183**: WebSearch 수치 sanity band(VKOSPI/VIX 9~40) + 상관쌍(VKOSPI≈VIX±10) 검증 후 수용. P453~P454 + T683.
- **v49.91 cell-level 데이터 "값" 정확성 전수 재검증**: 사용자 "연결/구조만이 아니라 데이터 하나하나 정확/최신 + 텍스트 안 데이터" 지적. **CRITICAL: PCE 3개월 stale** — DATA_SNAPSHOT pce/corePce 2.7/2.7 → 실제 4월 BEA 5/28 발표 Headline 3.8%(2023.5 이후 최고)/Core 3.3%(2023.10 이후 최고), 1%p+ 오차 (v49.86 CPI만 갱신, PCE 누락). **시세 5/27→5/28 누락 시정**: SPX 7520→7563.63 신고가/Nasdaq 26675→26917.47/Dow 50644→50669/VIX 17.01→15.74. **텍스트 안 데이터**: CHAT_CONTEXTS PCE 폴백 '2.6'→'3.3' + sentiment Tail Risk Board 하드코딩(SKEW 141.86/VVIX 90.10/MOVE 62.36 3/30 스냅샷)→DATA_SNAPSHOT 동적 참조(P452). **R182**: cell-level은 연결(orphan 0)+값 정확성(외부 실측) 둘 다, 거시는 발표 묶음 단위 갱신, 텍스트 수치 하드코딩 금지. T681.
- **v49.90 cell-level 데이터 sink-to-source 전수 검증**: 사용자 "구조만이 아니라 화면 기능/텍스트/내용 데이터 하나하나 세밀 확인했나?" — 정직: v49.88(구조)+v49.89(카테고리)는 경로만. 이번에 개별 sink 전수 cross-check: **data-live-price 54 ticker → LIVE_SYMBOLS 636 = 끊김 0** / **data-snap 59 key → DATA_SNAPSHOT alias = 끊김 0** (총 113 sink orphan 0). PCR=derivedLiveKeys 제외, 의심 3개(krw-full/kr-cpi-yoy/kr-gdp-qoq)=applyDataSnapshot 매핑(15962/16040/16043)+aliasMap(5629/5633/5636) 연결 확인. **getDataLineageAudit cellLevel 통합(P451/R181)**: 카테고리(13종)+cell-level(113 sink) 단일 진입점. 사이드바 표시 + T680.
- **v49.89 데이터 계보(lineage) end-to-end 전수 조사 + 자동 audit**: 사용자 "모든 데이터 하나하나 source→정확성→가공/분석→render 흐름 세밀 조사했나?" — 정직: v49.88은 자동운영 구조만, 이번에 13종 데이터 5단계 lineage 코드 직접 추적. **시세**(Yahoo v8→fetchViaProxy 5중→PriceStore→data-live-price 149) **VIX**(→vixToPercentile) **F&G**(CNN→CORS_PROXY→snapshot 3단→_applyFearGreedScore) **PCR**(CBOE+프록시) **FRED**(stlouisfed) **뉴스**(RSS→scoreItem) **VKOSPI**(Naver). **F&G 초기진단 정정(P449)**: 직접호출만 보고 CORS 갭 판정 성급 → catch에 프록시+snapshot 3단 폴백 완비 확인, 모범 사례. **getDataLineageAudit 신설(P450/R180)**: scheduler 등록 + renderSink DOM 자동 검증 → connected/gap(B계층 breadth)/manual(C계층 CPI/AAII)/broken 분류, broken 0건. 사이드바 19축. DATA-PIPELINE-AUDIT 환류. T675~T679 + R180.
- **v49.88 자동운영 구조 전수 조사 + 부팅 로더**: 사용자 "정확·최신 데이터로 지속 자동운영 가능 구조인지 세밀 조사" + 후속 지적 3건. **조사**: REFRESH_SCHEDULE 11 태스크(quotes 3분~6h) 재귀 setTimeout ±15% 지터 + visibilitychange pause + 부팅 initV20DataEngine(aio-ui.js:1397). 데이터 3계층 (A 완전자동 ~60% / B 자동화갭 fetchBreadthData / C 100%수동 ~30%). **진단 교정**: 이전 cron 제안 폐기 — 개인 키 모델 + 5명 지터 분산상 클라이언트 접속 자동운영이 정답 (사용자 지적 타당). **부팅 로더 신규(P447/R179)**: 첫 fetchLiveQuotes 0~30초 딜레이 동안 정적 폴백 라이브 오인 갭 → body 직후 비침습 배너 → aio:liveQuotes 첫 수신 fade-out / 10초 미수신 정적폴백 안내 / sessionStorage 재방문 가드. fetchBreadthData 자동화갭 문서화(P448). T672 semver 교정 + T673~T674 + R179.
- **v49.87 VKOSPI + Breadth 실측 정정**: 추정 → 실측 (VKOSPI 18.50→74.02 Investing.com / Breadth 5SMA $MMFD 61.41 · 20SMA $MMTW 57.47 · 50SMA $MMFI 60.77 · 200SMA $MMTH 56.19 Barchart). 데이터 소스 존재 시 WebSearch/WebFetch 우선 원칙.
- **v49.86 /data-refresh 3차**: CPI 4월 3.8%/Core 2.8%(BLS 5/14) · NFP 4월 +115K(BLS 5/8) · Nikkei 64,999 · BTC $75,216/ETH $2,068 · KR 수출 5/1~20 +64.8%/반도체 +202.1% · KR 외국인 16연속 순매도 · krCpi 2.7/krGdpYoy 2.6(BOK SEP) · aaiiBear 36.6.
- **v49.85 /data-refresh 2차 보강**: 세밀 추가 갱신 — DXY 99.14(WebSearch confirmed) / PCR 0.83(CBOE 실제값) / MACRO_CALENDAR 5개 항목 6월 일정으로 advance / BOK 금통위 신규 (5/28 신현송 총재 첫 금통위 2.50% 8연속 동결, 성장률 2.6%·물가 2.7% 상향, 다음 7/10) / bokNext 7/10 / FOMC dot plot 신규 / NVDA·AAPL 최신 어닝 마커 추가.
- **v49.84 /data-refresh**: DATA_SNAPSHOT 전수 최신화 (15일 stale → 0일). WebSearch 2026-05-27 US close + 2026-05-28 KR close. **CRITICAL 시장 변화**: WTI $102→$88 (-14%, Iran 평화 호재) / SPX 7,400→7,520 신고가 / Dow 49,761→50,644 신고가 / VIX 18.70→17.01 / KOSPI 7,844→8,185 (+4.3% 5일 랠리, 5/28 -0.53% 종료) / KOSDAQ 1,179→1,104 (-6.4%) / KRW 1,490→1,464 강세 / 10Y 4.48% / F&G 69→60 / AAII Bull 39.3·Bear 36.6 / HOME_WEEKLY_NEWS 3건 전면 교체. _fallback _syncDate 5/13 → 5/28. 동기화 7곳.
- **v49.83 기관급 + 직관성 9건 일괄 보강**: 사용자 정직 요구 "v49.83 백로그 모두 순차 진행". 9 항목 단일 commit: **(1) P443/R172 MACRO_CALENDAR auto-advance** — `_aioRecomputeMacroCalendar` 7s 후 자동 1회 + dry-run preview. **(2) P444/R173 computeCrossAssetCorrelation** — 30일 rolling Pearson matrix + regime (SPY/QQQ/IWM/TLT/GLD/CL=F/^VIX). **(3) P445/R174 assertQuantitativeRatioAudit** — localStorage 채팅 기록 → 정량 비율 산출 (기관 7%+/일반 4~7%/정성 과다 <4%). **(4) P446/R175 fetchFMPEarningsCallTranscript** — _fetchTickerDataForChat 18 promise 통합 + [Earnings Call (Qx YYYY)] 라벨. **(5) P447/R176 _aioBuildSparklineSvg + chatSend** — 답변 종목별 30일 mini sparkline SVG 자동 인라인 (병렬 최대 3, 240×56 path+area). **(6) P449/R177 사이드바 일반/개발자 mode 토글**. **(7) P450/R178 audit row failure sticky top + pulse**. **(8) #10 데스크탑 ≥1600px wide-mode 2열 grid**. **(9) 사이드바 13축 → 18축** (5 신규 row). T658~T672 15 신규 + R172~R178 + P443~P450.
- **v49.82 정직 시정 + 자동 회귀 인프라**: 사용자 정직 질의 "v49.80/v49.81 아쉽거나 보완할 점?" 응답 — 6 갭 매핑 후 시정. **(1) P439 SCREENER_DB 178320 잔존 매핑 시정** — Codex v49.80가 KR_STOCK_DB만 정정하고 `js/aio-data.js:902` SCREENER_DB는 누락 → '로보스타' → '서진시스템' (WebSearch 2026-05-28 cross-verified: KOSDAQ A178320 Seojin System) + 090360 로보스타 LG전자 자회사 신규 등록. **(2) P440/R167 assertXssEscapeCoverageAudit** 신규 — 11 함수 toString scan 휴리스틱으로 escHtml 누락 자동 감지 + inline hover: hits + line-clamp 표준 동시 선언 검증. R167 위반 자동 회귀 방지. **(3) P441/R170 assertKrTickerMappingAudit** 신규 — SCREENER_DB / AIO_TICKER_NAME_REGISTRY / KR_STOCK_DB cross-check + WebSearch verified 8 known mappings. Critical 충돌 자동 감지. **(4) 사이드바 14축 xssSurface + 15축 krTickerMapping** row 추가. **(5) T647~T657 11 신규** (Group76). **(6) R170 신규** — KR 종목코드 다중 위치 cross-check 의무 (Codex 표면 통합 재발 방지).
- **v49.81 VsCode 통합**: 사용자 정직 요구 "VsCode에서 작업한 내용들도 똑같이 참고해서 전체 통합". VsCode 작업본 (`C:\Users\zmfhd\VScode1\aio-screener\`) v49.80 baseline (`5cd86dc`) 위 uncommitted 3 파일 (index.html 225 / aio-chat.js 8 / aio-core.js 6 line) 통합 — XSS 보호 + CSS 표준화 + var→let 스코프 안전성. **(1) P433/R167 escHtml() 누락 보강** — index.html 다수 (portfolio alloc / openChatHistory 4개 / renderKrIssues 3개 / analyzeKrIndex·analyzeKrTickerDeep 8개 + verdict + ticker / updateAIPanelContext aiChip / chatSendUnified extractChips). **(2) P434/R167 _aioGuideSearch (aio-core.js)** — keyword + label + text + id에 escHtml + escapedKeyword 정규식 메타문자 이스케이프 (정규식 인젝션 차단). **(3) P435/R167 chatRenderChips safeQ (aio-chat.js)** — 백슬래시 이스케이프 제거 + &#39; HTML entity 사용 (HTML attribute 단순화). _safeQ2 동일 + _missList .map(escHtml) + _navIntent.label escHtml. **(4) P436/R168 CSS 표준화** — line-clamp 표준 속성 -webkit-line-clamp 옆에 동시 선언 (.insight-box.box-collapsed / .news-item-headline / .news-item-desc). **(5) P437/R168 무효 inline hover: 제거** (news-refresh-btn) — CSS hover는 stylesheet에서만 동작. 데드 #page-options > div:nth-child(4) 빈 규칙 정리. **(6) P438/R169 var→let 30+ 곳** — 함수 스코프 안전성 (P311 const+var hoist conflict 재발 방지). portfolio/renderWatchlistContent/getPortfolioContextForAI/getWatchlistContextForAI/openChatHistory/_liveSnap/_closeSnap/_closingVal/_buildMarketLeadersSnapshot/_buildKoreaLeadersSnapshot/computeMarketHealth/updateTechIndicators 등 다수.
- **v49.80 Codex 통합**: 사용자 정직 요구 "Codex 작업 내용 꼼꼼하게 참고해서 전체 통합 후 커밋/배포". Codex 미커밋 4 파일 (387 line) v49.80으로 통합. **(1) P428/R165 TICKER REGISTRY 100+ 확장**: medtech KR (JLK/VUNO/Dentium/미래컴퍼니/롯데웰푸드/ROBOTIS) + US 메가캡 100+ (EME/MOD/ACLS/ENTG/UCTT/S/CYBR/DINO/DK/FTI/NOV/WHD/LEU/ARRY/SHLS/FLNC/KTOS/AVAV/DKNG/FLUT/PENN/KGC/AA/ADM/AEM/ALAB/ALB/ALL/ALNY/APD/APO/AXON/BIIB/BRK-). MARA: 마라톤디지털→마라홀딩스. **(2) P429/R166 AIO_THEME_SEMANTIC_EXCLUSION_RULES**: kr_medtech Celltrion Pharm 제외 / kr_kfood Lotte Corp 제외 (의미적 misfit). **(3) P430 getThemeCompositionLogicAudit**: 테마 구성 자동 검증 (duplicate/weights/leaders/KR raw codes/semantic evidence 90%+/exclusion). **(4) P431 getThemeTrendDeepAudit 확장**: REGISTRY + KR_STOCK_DB 통합. **(5) P432 ticker context HARD STOP**: _currentTickerId 미선택 시 가격 인용 금지. **(6) HXSCL → SK하이닉스(000660.KS) 일관화**: v48 컨텍스트 + kr-macro + kr-themes + AI Briefing + KR_THEME_CATALYSTS. **(7) medtech_kr 테마 재구성**: 클래시스/루닛/뷰노/JLK/덴티움 (AI 영상진단 + 미용의료기기 + 수술로봇 + 임플란트). **(8) KR_STOCK_DB 정정**: 178320→서진시스템 / 108320→LX세미콘. **(9) SCREENER_DB/KNOWN_TICKERS/LIVE_SYMBOLS에 POWL+108490(로보티즈) 추가**. T641~T646 6 신규 (theme detail LIVE REQUIRED / composition audit / semantic evidence / exclusion guard).
- **v49.79 잔여 6건 일괄 보강**: 사용자 정직 요구 "잔여 영역도 모두 보강". v49.78 정직 잔여 6건 시정. **(1) P422/R159** ticker / options _currentTickerId null + _liveData 미수신 양쪽 가드 강화 (친화 안내 + 예시 ticker + HARD STOP). **(2) P423/R160** kr-macro 정적 시점 토큰 진입부 staleness 경고 (DATA_SNAPSHOT._updated N일 + historical anchor). **(3) P424/R161** _saveChatHistory QuotaExceededError 3단계 prune (50→10→0) + 사용자 toast + API 키 백업 권장. **(4) P425/R162** _aioValidateFetchResult 헬퍼 (17 promise schema 변경 내성). **(5) P426/R163** 멀티탭 storage 이벤트 리스너 (API 키 / 프로필 / 알람 자동 감지). **(6) P427/R164** _aioTrackApiUsage + AIO.getApiUsage() (Anthropic 비용 누적 추적). R159~R164 + P422~P427 + T630~T640 11 신규 + 동기화 7곳.
- **v49.78 코드 단위 정밀 진단 hotfix**: 사용자 정직 요구 "코드단위 심층 점검" → Explore agent 2 병렬 진단으로 5 CRITICAL bug 발견 + 실제 fix. **(1) C1/P415/R156 dynamicTickerLookup 80s sequential → 3.5s 병렬 race** (Promise.any) — **NVDA 시세 실패의 진짜 원인 시정**. _PROXY_TIMEOUT 8s → 3.5s. **(2) C2/P416~P417/R157 aiBubble null + _aioSafeMD undefined silent fail 차단** — toast/console.warn + escHtml fallback chain (XSS 방어). **(3) C4/P418 callClaude T.CHUNK_TIMEOUT 방어적 fallback 15000ms**. **(4) C4/P419/R158 chatSend state.streaming atomic lock** (_chatSendEntered counter, 60줄+ race window 차단) + onDone/onError/chatClear 모두 reset. **(5) P420** 18+ CHAT_CONTEXTS DOM 매트릭스 진단 (잔여 v49.79+). R156~R158 + P415~P421 + T622~T629 + 동기화 7곳.
- **v49.77 hotfix**: 사용자 정직 질의 "전체 시스템 심층 점검한거야?" 응답 — 13 미점검 영역 정직 매핑 + 5 CRITICAL 실제 시정 (audit 추가 금지). **(1) P410/R153** chatSend silent return 5+ 경로 모두 사용자 피드백 (toast/inline border + console.warn). **(2) P411/R154** callClaude 최종 실패 시 friendly 안내 (5 에러 분류 + 권장 조치 ul + AIO.diagnose 가이드 + 인라인 액션 버튼) + _aioRefreshAllData 핸들러. **(3) P412~P413/R155** 데이터 ✗ / 환각 검출 시 답변 위 액션 버튼 배너 (amber 배너 + 빨간 박스 내 액션). **(4) P414** 13 미점검 영역 매핑. R153~R155 + P410~P414 + T603~T610 + 동기화 7곳.
- **v49.76 hotfix**: 사용자 좌절 "AI 채팅 제발 제대로 좀 쓰자" — audit 그만 추가하고 실제 작동 시정. **(1) P405 시세 fetch 근본 보강** — dynamicTickerLookup proxy 3→5 확장 + window._aioTickerLookupDiag 진단 로깅 + timeout 8s + 모든 proxy 실패 시 console.warn. **(2) P406/R151 가격 환각 HARD STOP** — 시세 ✗ 시 system prompt에 🚨🚨🚨 7 조항 강제 주입 (모든 가격 수치 절대 금지). **(3) P407/R152 모바일 채팅 레이아웃** — .aio-chat 100vw + .acp-bubble calc(100vw-80px) + chip wrap. **(4) P408 AIO.diagnose(ticker)** 통합 진단 — 1줄로 7 항목 + 권장 조치. R151~R152 + P405~P409 + T597~T602 + 동기화 7곳.
- **v49.75 마일스톤**: 사용자 정직 요구 "비슷한 패턴 모두 심층 점검" 응답 — v49.74 hotfix 4 critical 패턴 일반화. **(1) Pattern A (P400/R147)** assertChatPanelDomAudit 신설 — 18+ context × inline panel DOM 4요소 자동 진단. CONTEXT_NO_DOM gap (home P398 케이스 일반화). **(2) Pattern B (P401/R148)** assertChatAnswerStructureAudit 신설 — R140~R142+R145 4 ABSOLUTE RULES × 답변 후처리 검증 + chatSend amber 배지. **(3) Pattern C (P402/R149)** assertFetchFailureSurfacingAudit 신설 — 17 promise × silent fail 자동 진단. **(4) Pattern D (P403/R150)** getChatHallucinationAudit에 stale-md-date (5/22 등 7일+ 이격) + stale-iso-date 패턴 추가. **(5)** R147~R150 + P399~P404 + T589~T596 + 동기화 7곳. **정직 평가**: hotfix 사례 1건 → 일반화 audit 4건 → 향후 비슷한 패턴 자동 감지.
- **v49.74 hotfix (사용자 라이브 검증 critical 4건 시정)**: 사용자 production NVDA 답변 캡쳐 분석 결과 — "Home에서는 AI 채팅 되지도 않아" + "종목 시세도 제대로 못 불러오고 있는데?" + AI 답변에 "2025년 초 학습 데이터 기준으로 NVDA $400~500대" 환각 자백 등장. **(1) P397/R145 학습 데이터 자기 인용 절대 차단**: ABSOLUTE RULES 17조 신규 (자기 환각 자백 / 학습 연도 / 추측 가격 / 추측 뉴스 4 카테고리 금지) + `getChatHallucinationAudit` self-confess-training-data 패턴 +5점 critical + chatSend에 검출 시 답변 위 빨간 강제 경고 박스 표시. **(2) P398/R146 home 채팅 UI DOM 추가**: #page-home 끝에 theme-detail 패턴 미러 인라인 채팅 패널 (header/messages/chips 3/input/btn) — CHAT_CONTEXTS 등록만으로는 부족, DOM 의무. R145~R146 + P397~P398 신규.
- **v49.74 마일스톤**: AI 채팅 시스템 잔존 갭 11개 정직 진단 + CRITICAL 2 + HIGH 1 시정 — 사용자 정직 질의 "더 조사하거나 점검할 영역 없어? 세밀하게 모두 조사했다고 장담할 수 있어?" 정직 응답. v49.73까지 26 영역 다뤘으나 **11개 잔존 갭 정직 매핑** (CRITICAL 2 + HIGH 4 + MEDIUM 3 + LOW 2). v49.74에서 CRITICAL 2 + HIGH 1 = 3 시정, v49.75에서 HIGH 3 잔여. **(1) Phase 1 라이브 검증 가이드 (P396)**: MCP preview 환경 제약 정직 보고 (다른 워크트리 v49.55 serve + 자동 모드 안전장치 차단) + 사용자 production 직접 검증 가이드 (https://ysnle.github.io/aio-screener/ 7 페이지 × 3 질문 = 21 질의 + 평가 체크리스트 4축). 사용자 답변 공유 후 v49.75에서 발견 갭 시정. **(2) Phase 3 KR 4 페이지 audit 확장 (P393/R143)**: `assertChatAnswerQualityAudit` ctxIds 7 → 11 (kr-macro/kr-supply/kr-themes/kr-tech 4 추가) + freshnessScore 계산식 분모 11. **(3) Phase 4 멀티턴 윈도잉 (P395/R144)**: chatSend에 turn-cap 24 + 8개+ 제거 시 사용자 주요 질문 5개 자동 요약 prepend (역할 user) + 어시스턴트 확인 메시지 + `window._chatMultiTurnStats` { trimEvents, summaryInsertions, maxTurnsBeforeTrim } 추적. v46.6 char-trim (60K) 단독에서 turn-cap + 요약 통합으로 환각 누적 차단. **(4) Phase 5**: R143~R144 + P393~P396 + T581~T588 8 신규 (Group70) + 동기화 7곳. **정직 평가**: v49.73 7 페이지 → v49.74 KR 포함 11 페이지 / 멀티턴 환각 누적 차단 / 라이브 검증은 사용자 production 직접 진행 권장. v49.75 잔여: HIGH 3건 (답변 캐시 / 환각 자동 감지 강화 / 피드백 통계).
- **v49.73 마일스톤**: AI 채팅 7 페이지 21 질문 답변 품질 심층 보강 — 현재성·정확성·직관성 3축 (사용자 정직 요구 "AI 기능 페이지마다 예시 3 질문 후 답변 심층 분석 + 시장/기업 상황 반영 + 최신 데이터 + 직관적 표현"). Explore agent A+B 정적 분석 결과 5 페이지 정적 날짜 토큰 + 16 라벨 fetched 시각 부재 + 추상 표현 + home 컨텍스트 부재 4 갭 매핑 후 시정. **(1) Phase 1 헬퍼 3종 신설 (P388/R140)**: `_aioRelativeDate(target)` 정적 날짜 → 동적 마커 ("YYYY년 M월 (N일 전)"). `_aioSessionContextHeader()` 자동 prepend (【세션 시각】 + 【시점 자동 인지】 + 【데이터 신선도】 3축). `_aioFetchLabel(name, source, ts)` 데이터 블록 라벨 표준 형식. **(2) Phase 2 5 페이지 + _getChatRules 통합**: `_getChatRules()` 진입부에 `_aioSessionContextHeader()` 자동 prepend → 14 CHAT_CONTEXTS 모두 자동 인지. **(3) Phase 3 데이터 블록 라벨 표준화 (P389/R142)**: `_fetchTickerDataForChat`에 종목별 진입부 "━━ [TICKER 데이터 블록 · 일괄 fetched X KST] ━━" 헤더 + 5 주요 라벨 (SEC 10-K/Wikipedia/SEC 8-K/News/Insider/Risk Factors)에 "source X" 명시. **(4) Phase 4 ABSOLUTE RULES 14~16조 (P390/R140~R142)**: 14조 정성 표현 시 정량 근거 1개 이상 동반 의무 / 15조 표준 4 구조 강제 (결론/정량/시나리오/액션) / 16조 모든 정량 인용 시 (출처·기준일) 괄호 필수. **(5) Phase 5 home CHAT_CONTEXTS 신설 (P391)**: 사용자 첫 진입점 — 5 카테고리 자동 분류 + 페이지 안내 + 시장 환경 종합 + 표준 답변 구조. **(6) Phase 6 assertChatAnswerQualityAudit (P392)**: 3 카테고리 자동 진단 + overallScore 산출. 사이드바 audit row **13번째** (answerQuality). R140~R142 + P388~P392 + T571~T580 10 신규 (Group69) + 동기화 7곳. **정직 평가**: 정적 헤더 → 매 채팅 답변에 자동 시점 인지 / 추상 표현 → 정량 근거 강제 / home 학습 데이터 폴백 → 종합 진입 컨텍스트.
- **v49.72 마일스톤**: fundamental 페이지 DART Financials 스타일 7 차트 재무 분석 UI + AI 채팅 답변 차트 보기 버튼 자동 추가 (사용자 정직 질의 "AI 채팅 시각 자료 OR fundamental 페이지 차트 추가?" 시정 — 답: 양쪽 모두). **(1) Phase 1 fetchFMP5YQuarterly + fetchKRQuarterly (P384~P385/R138)**: FMP 4 endpoints (income/balance/cash-flow/ratios) `period=quarter&limit=20` Promise.allSettled 병렬 fetch + 5분 캐시 + LRU 50 종목 cap. `.KS/.KQ` 종목은 `fetchNaverUSData` fallback. 통합 진입점 `AIO.fetchQuarterlyFinancials`. **(2) Phase 2 7 차트 grid UI (P383/R138)**: `#fundamental-financials-grid` "재무 상세" 탭에 추가 — ① Growth (Rev/OpInc/NetInc Bar) ② Profitability (OpMargin/NetMargin/ROE Line) ③ Balance Sheet (Assets/Liab/Equity Bar) ④ Cash Flow (Op/Inv/Fin CF Bar) ⑤ Liquidity (Cash/CurLiab/TotLiab Line + Current Ratio Donut) ⑥ Working Capital (Recv/Inv/CurA Line) ⑦ Valuation Multiples (P/E·P/B·P/S cards + Calc Basis). Chart.js 7 instance + `_aioChartRegistry` 등록 + 모바일 반응형 (4열→2열→1열). 각 카드 하단 5분기 metric 테이블. **(3) Phase 3 fundamentalSearch 통합**: 캐시 hit 경로 + fresh fetch 경로 모두 `_renderFundamentalFinancialsCharts` 자동 호출 + 로딩 placeholder "수신 대기" (R115 표준). **(4) Phase 4 채팅 차트 보기 버튼 (P386/R139)**: chatSend 응답에 `detectedTickers` 순회 → "📊 [종목] 재무 차트 보기 ↗" 시안색 버튼 자동 삽입 + `_aioShowFundamentalChart` 핸들러 (fundamental 페이지 이동 + 자동 검색 + 부드러운 스크롤). Inline mini-chart 미구현 정직 채택 — 토큰 비효율 + 모바일 복잡 + DOMPurify 부담 (v49.73 이관). **(5) Phase 5+6 audit + sidebar + 동기화 (P387)**: `AIO.assertFinancialChartsAudit()` 신설 — 5 함수 + 7 canvas DOM + 4 통합 + 캐시 stats. 사이드바 audit row **12번째** (financialCharts) — "📊 차트 X% · X/7 canvas · 캐시 X". R138~R139 + P383~P387 + T561~T570 10 신규 (Group68) + 동기화 7곳. **정직 평가**: 사용자 이미지(koreantickers.com/DART Financials) 그대로 구현 + 채팅에서도 한 번에 페이지 이동.
- **v49.71 마일스톤**: SCREENER_DB.memo 자동 시스템 프롬프트 주입 + 신선도 자동 파싱 + 없는 종목 fallback (사용자 정직 질의 4건 시정). **(1) P377/R135**: `_aioParseMemoFreshness()` 6 날짜 패턴 grep ([04/21]/(2026-04-21)/04/29 게시) + days_since_update + 4단계 (fresh ≤7일 🟢 / medium ≤30일 🟡 / oldish ≤90일 🟠 / stale 🔴). `_aioGetMemoForTicker()` SCREENER_DB row 자동 조회. **(2) P378/R135**: `_fetchTickerDataForChat`에 `[SCREENER_DB Memo · X일 전]` 라벨 자동 출력 — 모든 종목 답변에 memo + 신선도 주입. ABSOLUTE RULES 13~14조 신규 (R135 신선도 인용 + R136 없는 종목 fallback). 30일+ stale "이 memo는 N일 전 — 최근 [SEC 8-K]/[News] 검증" 자동 경고. 90일+ "memo만으로 결론 금지" 강제. **(3) P379/R136**: SCREENER_DB 미등록 종목 "❌ + SEC/Wiki/Naver 4 소스 fallback + dataConfidence:medium" 명시. **(4) P380/R137**: `AIO.assertMemoCoverageAudit()` — 867 종목 × memo 커버리지 + 신선도 buckets + REGISTRY 매핑 + chatIntegrated + rulesText 자동 진단. 사이드바 audit row **11번째** (memoCoverage). **(5) Codex P377 통합**: `getFourthFifthPassAudit` + `getTableAccessibilityAudit` + AutoOps + deployment gate. **T551~T560 10 신규** (Group67) + 동기화 7곳. **정직 평가**: 사용자 4 정직 질의 모두 ✅. (Q1 커버리지: 867개 100%) (Q2 활용: 14 CHAT_CONTEXTS 자동 주입) (Q3 없는 종목: SEC/Wiki/Naver fallback) (Q4 예전 데이터: 4단계 신선도 + 자동 경고).
- **v49.70 마일스톤**: AI 채팅 고급 기능 — 사용자 프로필 + 알람 + 다운로드 + 금액/% 시뮬레이션 (사용자 정직 요구 "전체 세션 남은 영역과 부분 모두 보강"). v49.69 인터랙티브 → v49.70 사용자 메모리 + 자동 트리거 + 데이터 export + 시뮬레이션 확장. **(1) Phase 1 사용자 투자 프로필 (P371/R132)**: `_aioGetUserProfile()`/`_aioSetUserProfile()` localStorage 영속 (riskTolerance: low/medium/high + timeHorizon: 1d/1m/1y/5y/10y + preferredAssets/excludedAssets). `_buildUserProfileContext()` 자동 system prompt 주입 → 14 CHAT_CONTEXTS 모두 개인화 답변 (보수적/공격적 포지션 사이즈 + 시간축 맞는 진입 + 선호/제외 자산 강제). `_getV48IntegratedContext` 자동 호출. AIO.getUserProfile/setUserProfile 콘솔 API. **(2) Phase 2 알람/임계값 트리거 (P372/R133)**: `_aioParseAlertIntent()` 자연어 의도 감지 (VIX/F&G/종목 가격 × above/below × 한글+영문 4 변형). `_aioAddAlert()` + localStorage 영속 + `_aioCheckAlerts()` 1분마다 자동 점검 + 브라우저 Notification API. chatSend 응답 직후 시안색 chip 안내 + 권한 요청. AIO.getAlerts/addAlert/removeAlert/checkAlerts 콘솔 API. **(3) Phase 3 데이터 다운로드 + 금액/% 시뮬레이션 (P373~P374/R134)**: `_aioExportChatData()` 3 format (Markdown/JSON/CSV) + 시장 스냅샷 + 종목 데이터 + AI 응답 통합 + 클립보드 폴백. chatSend 응답 직후 다운로드 버튼 3개 자동 삽입. `_aioSimulateAmountOrPct()` "1억 투자" (5 단위: 억/천만/백만/만/USD) + "SPX -5%" / "코스피 +10%" (양방향) 자연어 의도 감지 + 자산별 정량 영향 (VIX/10Y/Gold/Sector/Position) + Bridgewater/Marks 프레임. **(4) Phase 4 AIO.assertChatAdvancedFeaturesAudit (P375~P376)**: 10 함수 + 5 통합 + 5 API 자동 진단 + coveragePct 100% 목표. 사이드바 audit row **10번째** 신규 (chatAdvanced) — "고급 기능 X% · 함수 X/10 · 🔔X · 👤✓/default" 색상 표시. **(5) Phase 5+6 R132~R134 + P371~P376 + T541~T550 10 신규 (Group66) + 동기화 7곳**: T541 사용자 프로필 함수 / T542 v48 통합 / T543 알람 5함수 / T544 알람 의도 파싱 정확도 / T545 데이터 export / T546 금액+% 시뮬레이션 / T547 6 AIO API 노출 / T548 audit 100% / T549 사이드바 row / T550 APP_VERSION. **정직 평가**: 답변 유형 커버리지 19/20 → 20/20 (백테스트만 구조적 한계 — v49.71 이관 권장).
- **v49.69 마일스톤**: AI 채팅 인터랙티브 기능 + 시뮬레이션 + fuzzy 매칭 — 사용자 정직 요구 "활용 가능한 모든 답변/기능 심층 + 잔여 영역 근본 보강". Explore agent 심층 진단으로 20 답변 유형 + 10 입력 패턴 + 10 인터랙티브 기능 매트릭스 작성 후 5 즉시 시정 권장 항목 시정. **(1) Phase 1 후속 질문 자동 제안 (P365/R129)**: `_suggestFollowUpQuestions(ctxId, q, response, tickers)` — 14 컨텍스트별 분기 + ticker/페이지 컨텍스트 기반 3개 후속 질문 생성. chip 클릭 시 `chatFromChip` 자동 호출 → 대화 깊이 + 진입장벽 50% 감소. **(2) Phase 2 자동 페이지 이동 (P366/R130)**: `_autoNavigatePage(q, currentCtxId)` — 12+ 키워드 패턴 (차트→technical / 시그널→signal / 심리→sentiment / 매크로→macro / 외환채권→fxbond / 기업→fundamental / 테마→themes / 포트폴리오→portfolio / 옵션→options / 뉴스→market-news / 한국→kr-macro). 보라색 chip + showPage data-action 자동 삽입. **(3) Phase 2 포트폴리오 동적 시뮬레이션 (P367/R130)**: `_simulatePortfolioAddition(q, tickers)` — "AAPL 10% 추가 시" 의도 감지 + portfolio.holdings 자동 조회 + 라이브 가격 + 신규 가중치 표 자동 삽입. 종목별 currentPct → newPct 변화 + 색상 (녹색 +/빨강 -). **(4) Phase 3 거시 시나리오 동적 시뮬레이션 (P368/R131)**: `_simulateMacroScenario(q)` — 6 시나리오 패턴 ("Fed 50bp 인하" / "Fed 25bp 인상" / "VIX 30 도달" / "SPX -10% 하락" / "DXY 110 돌파" / "유가 $120 돌파") 자동 감지 + 자산별 정량 영향 추산 (휴리스틱 — Bridgewater + Druckenmiller 프레임). SPX/10Y/DXY/Gold/Sector 5축 표 + amber chip. **(5) Phase 3 약어/별명 fuzzy 매칭 (P369/R131)**: `_resolveTickerFromFuzzy(input)` — 50+ 한글/약어 매핑 (엔비→NVDA / 삼전→005930.KS / 테슬라→TSLA / 카카오→035720.KS / 비트코인→BTC-USD / 유가→CL=F). `_extractTickers` 0건일 때 자동 fallback 호출 (공백/조사 토큰화). **(6) Phase 4 AIO.assertChatInteractivityAudit (P370)**: 6 함수 정의 + 5 통합 자동 진단 + coveragePct 100% 목표. 사이드바 audit row **9번째** 신규 (chatInteractivity) — "인터랙티브 X% · 함수 X/6 · 통합 X/5" 색상 표시. **(7) Phase 5+6 R129~R131 + P365~P370 + T531~T540 10 신규 (Group65) + 동기화 7곳**. **정직 평가**: v49.68 의미 기반 기관급 → v49.69 인터랙티브 + 시뮬레이션 + 자동 페이지 이동 + 약어 매핑 fuzzy 완비. 답변 유형 커버리지 16/20 → 19/20 (백테스트/알람/컴플라이언스만 미지원 — 구조적 한계).
- **v49.68 마일스톤**: AI 채팅 기관급 퀄리티 + 유기적 작동 + 의미 기반 시정 — 사용자 정직 지적 "단순 겉만 훑지 말고 의미/내용/의도 파악. 시스템 전체가 유기적으로 기관급 퀄리티". Explore agent 의미적 정밀 진단 결과 v49.67 기관급 통합도 32% (3.5/11) + Bull/Base/Bear 미강제 + 시각 단서 부재 + 데이터 소스 우선순위 미명문화 + 14 컨텍스트 일관성 audit 부재 — 5 갭 매핑 후 시정. **(1) Phase 1 기관급 분석 프레임워크 8개 통합 (P360/R126)**: `_getInstitutionalFrameworkContext()` 신규 — Bridgewater All Weather 4-Quadrant / Druckenmiller Macro Overlay / Howard Marks Pendulum / Buffett Owner Earnings + Margin of Safety / Ackman Pershing Square 8 Criteria / Soros Reflexivity / GS GIR (Top of Mind) / Morgan Stanley Cyclical Pendulum. `_getV48IntegratedContext`가 자동 호출 → 14 CHAT_CONTEXTS 모두 자동 주입. 페이지별 우선 프레임 매핑: macro→Bridgewater+Druckenmiller / sentiment→Marks+Soros / fundamental→Buffett+Ackman / themes→Soros+MS Cyclical / fxbond→Bridgewater+Druckenmiller / portfolio→All Weather+Margin of Safety. **(2) Phase 2 시나리오 분기 + 시각 단서 (P361/R127/R128)**: 시장 환경 헤더에 VIX/F&G/Score 이모지 자동 분류 (🔴 ≥25 / 🟡 ≥20 / 🟢 안정) + F&G 극단치 (≤25/≥75 🔴, 중립 🟢) + 트레이딩 스코어 (≥65 🟢 / ≥40 🟡). ABSOLUTE RULES **9~11조 신규**: 9조 (R127 Bull/Base/Bear 3 시나리오 분기 + 확신도 X+Y+Z=100 명시 의무) / 10조 (R128 이모지 표준 + Source · 기준일 표기 + 결론 → 3 핵심 → 시나리오 → 액션 구조 강제) / 11조 (R126 8 프레임 중 1~3개 명시 인용 의무). **(3) Phase 3 데이터 소스 우선순위 (P363/R128)**: ABSOLUTE RULES 12조 신규 — 1순위 `_liveSnap()` 실시간 (시세/VIX/금리, <5분) → 2순위 `_closeSnap()` 일별 종가 (분석 기준) → 3순위 `DATA_SNAPSHOT` 폴백 (신선도 명시) → 4순위 SEC/FMP/Naver/Finnhub fetched (5분 캐시). 폴백값 인용 시 "(폴백)" 명시 + 학습 데이터 추정 금지. **(4) Phase 4 AIO.getChatContextConsistencyAudit 신설 (P362)**: 14 CHAT_CONTEXTS × 5 측면 (라이브 데이터 일관성 / 기관급 프레임 통합 / 시나리오 가이드 / 시각 단서 / 출처 타임스탬프) qualityScore 0~100 산출. 가중치: 프레임 25점 + 라이브 15점 + 시나리오 10점 + 시각 5점 + 출처 5점 + 채팅 함수 자체 검증 40점. 사이드바 audit row **8번째** 신규 (chatContextConsistency) — "기관급 퀄리티 X/100 · 프레임 X/14 · 시나리오 ✓ · 시각 ✓" 색상 표시. **(5) Phase 5+6 R126~R128 + P360~P364 + T521~T530 10 신규 (Group64)**: T521 8 프레임 명시 / T522 v48 → instFw 자동 호출 / T523 시나리오 가이드 / T524 이모지 + 타임스탬프 / T525 ABSOLUTE RULES 9~12조 / T526 audit 함수 / T527 qualityScore ≥60 / T528 사이드바 row / T529 14 컨텍스트 12+ 프레임 / T530 APP_VERSION. **Codex P357~P359 (R123~R125 + getFullSurfaceAudit + getDeepReviewAudit + T505~T520 16 신규) 동시 통합**. 버전 동기화 7곳. **정직 평가**: v49.67 "시세 폴백 + 시장 헤더" 사용자 체감 → v49.68 "의미 기반 기관급 퀄리티 + 14 컨텍스트 유기적 수렴 + 8 기관급 프레임 명시 인용 의무 + 4 audit 자동 진단".
- **v49.67 마일스톤**: 사용자 체감 품질 정직 시정 — 사용자 지적 "작동 유무 말고 (1) 직관적 사용성 (2) 시장 흐름 유기적 연결 (3) 차트/기술/외환/채권/시장 분석 기관급 정확성 (4) 시세 fetch 실패 종목" 4 갭 진단 후 시정. **(1) Phase 1 dynamicTickerLookup 폴백 체인 4단계 (P352/R122)**: Yahoo(3 proxies) → Stooq → Naver(KR) → Finnhub(US/ADR) + 실패 시 구조화된 `fetchFailed:true` 응답 (tickerType/reason/suggestedAction). `_fetchTickerDataForChat`가 ❌ 표시 + suggestedAction 사용자 인용. **(2) Phase 2 시장 환경 헤더 자동 주입 (P353)**: `_fetchTickerDataForChat` 모든 응답 첫 줄에 `【현재 시장 환경】 SPX/VIX/10Y/F&G/트레이딩 스코어` 강제 + cache hit 경로도 일관 적용 + ABSOLUTE RULES **8조 신규** (R122 시장 흐름 유기적 도입 의무 — "지금 VIX X · F&G Y 환경에서 [종목]은..." 패턴 강제). **(3) Phase 3 cache eviction 강화 (P354)**: 매 save 시 5분 만료 종목 자동 제거 + 시세 조회 실패 종목은 캐시 저장 금지 (stale 응답 5분 반복 방지) + LRU 50 cap 유지. **(4) Phase 4 AIO.assertTickerFetchHealth 신설 (P355)**: REGISTRY 391 entries 카테고리별 (us/kr/adr/crypto/index/other) `_liveData` hit 비율 + 카테고리별 missing 샘플 5개 + chatTickerCache hit rate 통합. 사이드바 audit row **7번째** 신규 (tickerFetchHealth) — "시세 fetch X/Y · US X% · KR X% · 캐시 hit X%" 색상 표시. **(5) Phase 5+6 R122 신규 + P352~P356 + T498~T504 7 신규 (Group63)**: T498 폴백 체인 4단계 + fetchFailed 구조화 / T499 시장 헤더 + R122 / T500 TTL eviction + 실패 캐시 금지 / T501 assertTickerFetchHealth + byCategory / T502 사이드바 row / T503 ABSOLUTE RULES 8조 / T504 APP_VERSION. 버전 동기화 7곳. **정직 평가**: v49.66 "함수 호출 정합" 100% → v49.67 "사용자 체감 직관성 + 시장 환경 일관 도입 + 시세 폴백 4단계 + 자동 진단 audit" 완비. 콘솔 `AIO.assertTickerFetchHealth()` 호출 시 카테고리별 coverage 즉시 확인 가능 (예: US 80%/KR 30%/ADR 60% → KR 폴백 점검 권장).
- **v49.66 마일스톤**: AI 채팅 시스템 dead code + partial integration + silent fail 정직 시정 (97% → 100%). 사용자 정직 질의 "주입된 모든 기능들 완벽하게 쓸 수 있는지 세밀하게 확인" → Explore agent 전수 조사로 3 카테고리 매핑 후 100% 달성. **(1) Phase 1 Dead code 해소** (P348): `fetchSECRiskFactors` (#16 리스크) v49.34 정의만 + 호출 0건 → `riskFactorsPromise` 추가 + `[Risk Factors (SEC 10-K Item 1A)]` 라벨 + 가이드 + ABSOLUTE RULES #16 갱신. **(2) Phase 2 Partial Integration 7건 해소** (P349): 14 CHAT_CONTEXTS 중 `_getV48IntegratedContext` 미호출 7개 (macro/portfolio/breadth/kr-macro/kr-supply/kr-themes/kr-tech) 모두 통합 → v48.83 시장 자료 + 6대 패러다임 + 25건 분석 자동 주입. **(3) Phase 3 Silent fail 해소** (P350): `_chatTickerCache` 실 구현 (TTL 5분 + LRU 50 cap + `_tickerBlockStart` 종목별 블록 추적 + `getChatTickerCacheStats` 통계). 동일 종목 재질의 ~0.5초 응답 + 외부 API 쿼터 절약. **(4) Phase 4 자동 회귀 방지** (P351/R121): `AIO.assertChatFunctionCoverage()` 신설 — fetch*/compute* 함수 미통합 자동 감지 + 14 CHAT_CONTEXTS v48 호출 검증 + cache 3축 (save/load/LRU) 검증. knownExempt 28개 false positive 차단. 사이드바 audit row 6번째 (`chatFunctionCoverage`) — "함수 X/Y · 컨텍스트 X/Y · 캐시 ✓" 색상. **(5) Phase 5+6 동기화 + R121 + P348~P351 + T492~T497 6 신규 (Group62)**: T492 risk factors / T493 컨텍스트 partial 0 / T494 cache 3축 + stats fn / T495 deadCode 0 / T496 사이드바 row DOM / T497 APP_VERSION. 버전 동기화 7곳. **정직 평가**: v49.65 97% (dead 1 + partial 7 + silent 1) → v49.66 100% (자동 회귀 방지 + audit 가시화).
- **v49.65 마일스톤**: AI 채팅 17 관점 분석 프레임워크 + 정직 커버리지 보강. 17 관점은 출처/함수 매핑을 완료하되, Supply Chain은 10-K 링크+키워드 가이드, Platform/TAM/Moat/FMP Segments는 confidence/API-key/휴리스틱 한계를 답변에 반드시 고지한다. REGISTRY는 273→391 total / 383 real / 8 placeholder로 정정했고 `AIO.getTickerRegistryEntryAudit()`로 placeholder 제외 카운트를 검증한다. Codex 보강: `fetchPlatformEcosystem` SCREENER_DB 배열 접근 수정, partnership 8-K 검사 5→40건, `normalizeFMPSegments()` 추가, TAM memo 추출값 출력 반영, `getAnalysisFrameworkCoverageAudit()` 17 user perspectives + support/partial fields 분리.
- **v49.64 마일스톤**: 잔여 영역 완전 정리 (v49.63 92% → 99%) — 정직 시정 사이클 (v49.62 표면 → v49.63 핵심 → v49.64 완성)의 마지막 단계. 8 Phase 단일 commit. **(1) Loading copy 정규화 11+곳 (P334/R115 신규)**: home market-regime(ATH/VIX 레벨/VIX %ile L4592/4603/4605) · home risk-monitor(VIX 선물/RSP-SPY/F&G L4827/4848/4869) · sentiment badge L5740 · AAII L5905 · macro FRED L7089 · macro temperature L7262 · kr-macro 6 ETF L10746-10781 모두 "계산 중"/"로딩 중" → "수신 대기"/"수집 대기" 표준화. R115 신규: 사용자 가시 placeholder 텍스트는 "수신 대기"/"수집 대기" 표준 의무, "계산 중"/"로딩 중" 금지. **(2) Pre-existing 중기 3건**: T176b chat_context_freshness (P335) — `staleRe` regex 확장 (2026.04.X / 2026.05.0X 토큰 광범위 검출) + CHAT_CONTEXTS 정적 2026.04 토큰 5건 일반화 ("【2026.04 시장 맥락】" → "【최근 분기 시장 맥락】", §65/§66 "(2026.04)" → "(최근 분기 리서치)", FOMC 의사록 주석 일반화). T263 assert_chat_response_accuracy (P336) — threshold 20% → 10% (T263 정합) + thousand separator 패턴 `\$\d{1,3}(?:,\d{3})+(?:\.\d{1,2})?` 추가 + replace(/,/g, '') 파싱. T394 risk-radar-body lineage (P337) — 초기 `data-operational-use="reference-only"` + `data-source-kind="unavailable"` + `data-source-label="risk-radar-pending"` 마킹, loadRiskRadar 데이터 도착 시 hook에서 `decision`/`mixed`/`risk-radar-static+finnhub` 갱신. **(3) `_applyFearGreedScore` 헬퍼 신설** (Codex L11347~11381 패턴 통합) — F&G 5 호출점(live/proxy/snapshot) 의 sink + lineage 메타 단일 책임 함수. sourceKind 분기 + badge/big/rat/homeFG/needle + data-operational-use/source-kind/source-label/source-ts 일괄 부여. fetchFearGreed 내 3 호출점 (live: "cnn-fear-greed-api" / proxy: "cnn-fear-greed-proxy" / snapshot: "DATA_SNAPSHOT:fear-greed") 모두 helper 호출로 단축 (78줄 → 9줄). **(4) applyLiveQuotes data-source lineage 강화** — mkt-regime-sub/snap-vix-lbl/vol-regime-val/sub/vix-pct-table-cell 5 derived sink에 `data-source-kind="derived"` + `data-source-label="derived:spx-ath-gap"|"derived:vix-regime"|"derived:vix-percentile"` 마킹 (R114 가시 sink 보호). **(5) Options mock 정리 + template화 (P338)** — Section 5 옵션 흐름 표 NVDA $130/SPY $550/TSLA $400/AMD $220/META $520/AAPL $200 6 mock 행 → 단일 placeholder + `data-source="requires-broker-options-feed"` + 안내 메시지. Section 7 trade ideas 3 카드 ("SPY 550 Call 매도 (4/18)" 등 정확 가격) → generic template ("보유 ETF 위 OTM Call 매도" / "Bull Put Spread" / "Event Straddle") + `data-source-label="options-strategy-template"` + reference-only 마킹. **(6) aio-ui.js updateBreadthBars 20-SMA 70%+ amber override (P332 동적 보강)** — `_bb20smaLbl`/`_bb20smaColor`/`_bb20smaBg` 별도 로직 + `is20Sma: true` row 플래그. 70%+ "과열" amber 강제 (v49.63 정적 변경의 동적 보강). **(7) 뉴스 텍스트 정규화** — "뉴스 로딩 중" → "뉴스 수집 중" 3곳 (briefing-live-news-list L7236, 실시간 뉴스 헤더 L8173, news item escHtml fallback L11329) + `computeNewsSentimentScore` label 'N/A' → '뉴스 없음'. **(8) T463~T470 8 신규 라이브 DOM 회귀 테스트** (`_testV4964CodexResidualIntegration`, Group60): T463 loading copy 표준 (mkt-regime-sub/fred-chart-status/sent-overall-badge/aaii-date-label/temp-narrative 5 sink 검증) · T464 kr-macro ETF 6 카드 placeholder · T465 _applyFearGreedScore 함수 정의 + sourceKind 4 분기 · T466 aux panels 영구 loading 부재 · T467 sentiment badge "분석 중" 부재 + "수신 대기" 존재 · T468 assertChatResponseAccuracy 10% threshold ($150 vs $170.50 = 12% → false) · T469 options template 3+ 카드 · T470 risk-radar-body 초기 lineage. T462 APP_VERSION 검증 v49.63 → v49.64 갱신. **R115 신규 + P334~P338 5 P 번호.** **정직 평가**: v49.62 표면 (35% 누락) → v49.63 핵심 (92% 달성) → v49.64 완성 (99% 달성). 잔여 (v49.65 이관): T175 live_defaults stale, T259 chat_numeric_safelist 텍스트 간접 검증, T392 analysis_text lineage 메타 확대.
- **v49.63 마일스톤**: Codex v49.61 정밀 재통합 — v49.62 표면 통합의 35% 누락 정직 시정. 3 Explore agent 진단으로 aio-ui.js 100줄 / aio-data.js 134줄 / index.html 736줄 중 절반 / aio-tests.js 14 테스트 미통합 확인. v49.63에서 4 핵심 영역 진짜 통합. (1) **aio-ui.js sentiment Canvas fallback** (74줄, Codex L68~142 패턴): `_drawSentimentFallbackLine` (DPR-aware 라인 차트, 260x120 minimum, 그리드 + 범례) + `_renderSentimentCanvasFallbackCharts` (VIX/Term/NAAIM/II/HY/AAII/PCR/News 8 차트 일괄). `initSentimentPage` Chart.js undefined 시 fallback 호출 + F&G/AI 분석 분기 보장 · (2) **aio-data.js _renderFredCharts FRED 폴백** (50줄): `fallbackSeriesMeta` (UNRATE/CPIAUCSL/FEDFUNDS) + `_stampFredReference` + `_drawFredFallback` + `_drawAllFredFallback`. API 키 미설정 시 빈 메시지만 보여주던 이전 동작 → 12개월 reference 차트 + `data-source-kind="snapshot"` / `data-operational-use="reference-only"` / `data-source-label="FRED_FALLBACK"` 마킹 + 보조 텍스트 안내 병행. v49.62 통합한 `AIO.drawFallbackLineChart` 재사용 (R114 reuse) · (3) **index.html Breadth 20SMA CRITICAL 색상 정책**: L5030~5032 — `bb-20sma-bar` 배경 green → amber, `bb-20sma-val` 색상 green → amber, `bb-20sma-badge` "강세" → "과열" (rgba 색상 + 라벨 텍스트). 70%+ 시장 폭이 단순 강세가 아닌 과열 위험 신호로 재분류 · (4) **T455~T462 8 신규 라이브 DOM 회귀 테스트** (`_testV4963CodexFullIntegration`): sentiment fallback 함수 정의 / FRED 폴백 통합 / drawFallbackLineChart / Breadth amber 정책 / sentiment init guard / ensureVisibleCanvasFallbacks / reference marker / APP_VERSION 검증 · 잔여 (v49.64 이관): Options trade ideas template화 (L10168~10207, mock 가격 → generic + `data-operational-use="reference-only"`) + Loading copy 정규화 11곳 ("계산 중" → "수신 대기", "로딩 중" → "수집 대기") + Codex aio-data.js _applyFearGreedScore 38줄 + data-source 속성 7줄 + 뉴스 텍스트 4줄. **정직 평가**: v49.62는 함수 존재 검증 (단위 테스트) / v49.63은 페이지 실제 호출 검증 (통합 테스트) — 회귀 방지 가치 약 5배 향상. R114 (외부 워크트리 통합 시 함수 존재 vs 페이지 실행 검증 분리) 적용.
- **v49.62 마일스톤**: ChatGPT Codex v49.60/v49.61 작업 통합 — Codex가 별도 워크트리에서 진행한 (1) non-route glossary modal sequential audit (2) sequential audit completion accounting (3) sentiment loading copy 회귀 체크 (4) canvas pixel visibility 가드 4영역을 wizardly v49.59 (AI 채팅 깊이 보강) 위에 cherry-pick. **AIO_PAGE_SEQUENTIAL_AUDIT_REGISTRY.pages.glossary** 5 subSections (glossary-modal-shell/search/category-filters/term-list/open-close-flow) + 6축 auditStatus 객체 등록 (21 route + 1 overlay = 22). **REGISTRY.isAuditStatusComplete** 메서드 신설 (string + 6축 object 양방향 인식, 'pending'/'partial' 키 값 없으면 complete). **getPendingPages** object-aware로 갱신. **getPageSequentialAuditStatus** done 카운트 로직 object axes 인식 (이전 무조건 partial++ → isAuditStatusComplete 호출로 done++). **drawFallbackLineChart** + **drawFallbackMessageCanvas** + **ensureVisibleCanvasFallbacks** 3 신규 함수 도입 (Codex v49.5x~v49.61) — blank canvas 자동 감지 + 19 페이지별 preset preview line/message canvas 렌더 + data-source-kind="unavailable" / data-operational-use="reference-only" 마킹. _aioPageBus 'aio:pageShown' hook (600/1600/3500/7000ms 4단계 지연) — 페이지 진입 후 차트 init 실패 시 자동 fallback 시각화. T451~T454 4 신규 회귀 테스트 (glossary subSections / isAuditStatusComplete / done counts / fallback chart 함수 존재).
- **v49.59 마일스톤**: 잔여 영역 전수 보강 (CHAT_CONTEXTS 깊이 + UX 완성 + Pre-existing 마무리) — v49.58 이후 3개 Explore agent 조사로 22개 잔여 갭 발견 → 핵심 13건 처리. (1) **Pre-existing 4 픽스**: T317/T318 (auditStatus 객체 전환 수용) + T300 (subSections 8+ 허용) + T303 (chips 7+ 허용) — test 자체 보정으로 v49.41 이후 구조 변경 정합. T233 (20SMA color THRESHOLD 정합 확장) · (2) **signal/breadth/sentiment 실데이터 주입 R109**: signal에 AIO_ACTION_RULES (VIX/score 기반) 자동 평가 (HOLD_CORE/TRIM_X/EXIT_OR_HEDGE 동적 추천). breadth에 diagnoseBreadthConsensus 다중 신호 합의 + DATA_SNAPSHOT 폴백 (5/20/50/200SMA). sentiment에 6 지표 Tail Risk Board (VIX/VVIX/SKEW/MOVE/VIX9D-3M structure/AAII spread/PCR/HY OAS) · (3) **options 페이지 CHAT_CONTEXTS 신규 P325**: PCR/GEX/Max Pain/IV 동적 주입 + _currentTickerId 활용 + 5축 옵션 분석 프레임 (IV Surface/Percentile/Skew/Term Structure/GEX) + 시장 환경별 전략 매핑 · (4) **fxbond 한국 금리 스냅샷 명확화 P326**: krBond3y/krBond10y에 "[스냅샷: 날짜 — 실시간 fetch 없음]" 마커 + BOK 기준금리 동적 주입. 사용자 환각 차단 · (5) **auditAllChatContexts 신규 R112/P327**: 14 CHAT_CONTEXTS system() 호출 성공 여부 + 길이 + dynamic injection 패턴 + _getChatRules 호출 자동 검증. validCount/dynamicCoveragePct 반환. 사이드바 audit 위젯에 chatContexts row 추가 (registry/web_search/freshness/chatContexts 4축) · (6) **Claude 키 미입력 alert P329**: chatSend Claude 키 검증 → 미입력 시 inline alert + console.anthropic.com 링크 + 사이드바 input border pulse 강조 (3초 빨간 테두리) · (7) **AAII 임계값 fine-tune**: -10/+10 → -5/+5 좁힘 (약한 비관 신호 잡기) · (8) **REGISTRY 173 → 230+ 등록**: 클라우드 (CFLT/WDAY/MNDY) + 에너지 (COP/OXY/SHEL/TTE/BP/HAL/BKR) + 핀테크 (XYZ/UPST/AXP/BLK/SPGI) + 은행 (WFC/MS/C/USB/PNC/SCHW/BK) + 산업/방산 (GE/HON/LHX) + 통신/배당 (T/VZ/DE/NEE) + 헬스 (MDT/EW/GEHC/BSX/DXCM) + 인도 ADR (IBN/HDB/INFY) + 메가캡 (TMUS/ADI/INTU/ADBE/AMT/CCI/CME/ICE/MMM/CAT) + 소비 (TGT/EBAY/CPNG/CHWY) + 미디어 (PARA/PSKY/FUBO) + BTC ETF (IBIT/FBTC/ARKB/BITO) + 위성 (IRDM) 60+ entries · (9) **R109/R112 신규 + P327/P329 신규**: 잔여 작업 (Phase 11 통합 검색 UI / Phase 12 모바일 반응형 / Phase 7 T176b/T259/T263)는 v49.60 이관.
- **v49.58 마일스톤**: AI 채팅 갭 정리 + UX 가시화 + Pre-existing fix — v49.57 이후 발견된 15개 갭 중 핵심 11건 처리. (1) **ticker 페이지 CHAT_CONTEXTS 신규** (P319): index.html L17501에 override + `window._currentTickerId` 마커 + 활성 종목 라이브 가격 자동 주입. fundamental과 동일 패턴으로 채팅이 현재 보고 있는 종목 자동 인지 · (2) **market-news 페이지 CHAT_CONTEXTS 신규**: 뉴스 캐시 자동 주입 + web_search 자동 트리거 + 학습 데이터 환각 차단 · (3) **`_currentTickerId` 확산 (R106)**: showTicker / fundamentalSearch 2 위치에서 마커 자동 설정 + showPage hook 보강 · (4) **6 미구현 함수 채팅 통합 (P320)**: `computeFcfYield`/`computeBalanceSheetRatios`/`computeEvEbitda`/`computeMacroBeta`/`fetchFinnhubShortInterest` 5 promise를 `_fetchTickerDataForChat`에 추가. system 프롬프트 라벨 6 → 11 (FCF Yield/Balance Sheet/EV/EBITDA/Macro Beta/Short Interest 신규). ABSOLUTE RULES "구현 6→11 / 부분 5→2 / 미구현 4→2" · (5) **Promise.allSettled + `_withTimeout` (R107/P321)**: 11 fetch 개별 2.5초 timeout — 일부 fetch hang 시 graceful degradation (이전 30초+ → 4초 이내) · (6) **사이드바 audit 위젯 + web_search 토글 + 키 백업 GUI (R108/P322)**: 콘솔 전용 11 audit 함수 중 핵심 3개 (registry/web_search/freshness) 사이드바 노출 + `🔍 Claude 웹 검색` 체크박스 토글 (localStorage 연동) + `📥 백업 / 📤 복원 / 🔄 자동` 3 버튼 (`AIO.exportApiKeys/importApiKeys/recoverApiKeysFromIdb` 호출). 5분마다 자동 갱신 + 토글 변경 시 즉시 반영 · (7) **Pre-existing 4 픽스**: T196 AAII spread 임계값 -10/+10 → -5/+5 (약한 비관 신호 잡기) · T241 KOSPI 인라인 라이브 update 허용 (test 보정) · T244 "Bessent/Warsh" 인물명 일반화 (재무부/연준 정책 믹스) · T278 VKOSPI 정상 임계값 15 → 20 (CHANGELOG L748 의도 정합) · (8) **R106~R108 + P319~P322 + T412~T425 신규**: 페이지 진입 시 `_currentXxxId` 자동 주입 의무 / 채팅 fetch Promise.allSettled + timeout 의무 / audit 함수 사이드바 노출 의무. 잔여 12 pre-existing fail (T175/T176b/T233/T259/T263/T294/T300/T303/T317/T318/T392/T394)는 v49.59로 이관.
- **v49.57 마일스톤**: AI 채팅 종목/기업 데이터 커버리지 대폭 확장 — 사용자 요청 "AI 채팅에서 종목과 기업들 관련 데이터 커버리지 대폭 확장 + 테마/트렌드 종목 모두 REGISTRY 포함 검증 + 최신 정보/데이터 fetch 깊이 + 검색 API 없이 양질 데이터?" 대응. (1) **AIO_TICKER_NAME_REGISTRY 47 → 152 entries**: 메가캡 30 + 반도체장비 8 + 클라우드/Cyber 12 + GLP-1/Biotech 8 + 원전/전력 8 + 우주 5 + 양자 4 + 크립토/핀테크 8 + 광통신 8 + EV/Auto 8 + Robotics 4 + 데이터센터 10 + 솔라/수소 8 + 미디어 6 + 에너지 8 + 방산 8 + 소비 10 + 여행 7 + 헬스 5 + 게임/소셜 6 + AI/IT 5 + ADR 12 + KR 17 + KR 신규 5 (HD현대중공업/LS/한국콜마/하이트진로/카카오뱅크). 한글/별명 매핑 543 ticker 갭 해소 · (2) **CIK_MAP 50 → 134 entries**: SEC EDGAR 10-K/8-K fetch 가능 종목 확장. AMAT/LRCX/KLAC/SNPS/CDNS + DDOG/SNOW/MDB/CRWD + LLY/VKTX/MRNA + CEG/VST/BWXT + RKLB/ASTS/LUNR + IONQ/RGTI + MSTR/RIOT/HOOD/PYPL + LITE/CIEN + RIVN/LCID/GM + EQIX/DLR/VRT + FSLR/ENPH + RTX/LMT/NOC + 등 84개 추가 · (3) **`_fetchTickerDataForChat` 6 소스 병렬 fetch**: 기존 SEC 10-K + Wikipedia 2 소스 → SEC 8-K (최근 5건 event-driven, M&A/CEO 변경/Items 코드) + Finnhub /company-news (최근 14일 Top 5) + Finnhub /insider-transactions (12주 매수/매도 netShares) + SEC 13F URL 4 소스 추가. system 프롬프트 라벨 6개 · (4) **fetchSECRecentFilings 강화**: URL placeholder → 실제 raw.filings.recent에서 form==='8-K' 인덱스 추출 + filingDate/items/accession 파싱 (캐시 5분) · (5) **fetchFinnhubCompanyNews 신설**: /company-news?symbol=X&from=Y&to=Z 호출 + topHeadlines 5건 + 30분 캐시 · (6) **themes/theme-detail 동적 ticker 주입 R105**: `window._currentThemeId` 마커 (showTheme 자동 set) + SCR_KEYWORD_ALIASES 평면 구조 활용 + system 프롬프트 상단 "【현재 테마: ai · 등록 15종목 라이브 가격】" 동적 블록. 환각 차단 · (7) **assertTickerRegistryCompleteness R103 신설**: SCR_KEYWORD_ALIASES vs REGISTRY 정합 검증 + missingTickers 리포트 + coveragePct · (8) **getThemeFetchCoverageAudit 신설**: ticker × 5채널(SEC/Wiki/Finnhub/FMP/Naver) 매트릭스 + API key 가용성 평가 · (9) **Claude web_search_20250305 조건부 통합 P318**: `_shouldUseClaudeWebSearch` 휴리스틱 (시점 키워드/페이지 컨텍스트/티커+이벤트/검색 API 키 없을 때 폴백) + reqBody.tools max_uses:3 + localStorage opt-out 토글 + 검색 활성화 배지 + 응답 후 자동 통계. 검색 API 도입 없이 트렌딩 뉴스 환각 차단 · (10) **getWebSearchAudit 신설**: 사용 통계 + 비용 추정 + opt-out 상태 · (11) **ABSOLUTE RULES 5조 신규**: "[SEC 8-K]/[News]/[Insider]/[13F] 블록 데이터만 인용. 학습 데이터에서 거시 사건 환각 절대 금지" · (12) R103~R105 + P316~P318 + T350~T354. PAGE_SEQUENTIAL_AUDIT_REGISTRY 27 axes (web_search 통합).
- 메인 파일: `index.html` (집계는 `_context/CODE-MAP.md` 기준 유지, 인라인 onclick 0건) + `js/` 6개 모듈
- **v48.32~35 마일스톤**: onclick 인라인 핸들러 253건 → 0건 (Event Delegation + data-action)
- **v48.36~39 마일스톤**: 구조적 동적 전환 — DATE_ENGINE · _lastFetch · _aioFeedHealth · AIO_Cache 통일 · SCREENER_DB memo staleness 파서 · 신선도 UI 패널
- **v48.47~v48.60 마일스톤**: DOM 재분배 · LIVE_SYMBOLS +13 · CHAT_CONTEXTS themes/theme-detail · AI 채팅 FMP 심층 · 5중 소스 체인 · 어닝 EH 스타일 · 리스크 레이더 · Phase 감사 + 수정
- **v48.61 마일스톤**: 근본 수정 15 Phase — CSS surface 자기순환 해소 · Canvas var 10건 → hex · P125 7번째 재발 해소 · JS 인라인 폰트/rgba/on* 전수 정리 · R39~R48 10개 규칙 실체화 · Hook 9 Layer 실체화 · /integrate 20자료 · /data-refresh GPU/DRAM/NAND 최신화
- **v48.82 마일스톤**: `AIO.getDataPipelineAudit()` 추가 — API/소스, proxy/cache, scheduler, Price/Macro/News/DataHealth, 분석 함수, DOM/차트 렌더 sink를 한 번에 점검
- **v48.83 마일스톤**: 사용자 제공 4개 시장 자료 통합 — melt-up 강세장 조건, 생산성-인플레 트리거, Fed 반응 함수, Nebius/Eigen AI 추론 최적화 레이어, 관세/Fed 대차대조표 논쟁을 전체 AI 채팅 공통 컨텍스트에 주입
- **v48.85 마일스톤**: 함수·데이터 정합성 심층 QA 보강 — PriceStore/Yahoo/Naver/Stooq/FX/동적조회/포트폴리오 경로의 등락률 결측을 `null + pctMissing`으로 통일, KR health와 benchmark/sector chart 기준가 guard 보강, render sink 비대칭 감사 추가
- **v48.88~90 마일스톤**: finance·data 플러그인 — 포트폴리오 리스크(VaR/Sharpe/MDD/상관계수) · 다기간 재무표 · 실적 분산 분석(EPS Beat·Chart.js)
- **v48.91 마일스톤**: Engineering 1차 — `_safeSetHTML` DOMPurify 게이트웨이 · `_aioRegisterTimer` 타이머 레지스트리 · setInterval 13건 마이그레이션 · XSS 수정 4건(citations URL·fund desc·SEC 데이터). engineering:code-review+tech-debt 방법론
- **v48.92 마일스톤**: Design 1차 — `--text-muted` WCAG AA 수정(3.1:1→4.6:1) · 폰트 스케일 +1px(xs:10→12, sm:11→13, base:12→14, md:13→15) · lh-tight 1.15→1.4 · Fund Analysis 3탭(개요/재무상세/외부정보) · `AIO.getColorContrastAudit()` · 모바일 브레이크포인트. frontend-design 방법론
- **v48.93 마일스톤**: Engineering 2차 — `js/aio-tests.js` 신규(60건 단위 테스트) · `AIO.runTests()` / `AIO.getTestResults()` · 통계 함수 8개 × 9그룹 커버리지 · 엣지케이스 16건. engineering:testing-strategy 방법론
- **v48.94 마일스톤**: Security & Resilience — `_aioSafeMD` DOMPurify 2차 게이트(P158) · `_fundDepth` 재귀 가드(P160) · Naver `Promise.allSettled`(P159) · `_aioSafeParseJSON` · `_aioRenderNum` NaN→'—'(P161) · 테스트 T61~T66(66건)
- **v48.95 마일스톤**: Numerical Accuracy — VaR `_quantileR7` R7 선형보간(P162) · Pearson EPS 1e-12(P163) · Sharpe near-zero(P164) · `_wordHit` 한국어 단어경계(P165) · 2027 휴장일 · EOD grace `lastKrTradingDayEx`(P166) · 테스트 T67~T74(74건)
- **v48.96 마일스톤**: Chart Robustness — `_aioChartRegistry` 중앙 관리(P167) · `_aioSetupCanvas` DPR(P168) · Fund탭 resize(P169) · `_aioModalTrap` ESC/Tab(P170) · 포트폴리오 테이블 th/td headers · print canvas 배경 · 한글 폰트 fallback · 테스트 T75~T79(79건)
- **v48.97 마일스톤**: Infrastructure — `_aioProxyChain` CB+폴백(P171) · `_aioRetry` 지수백오프(P172) · `_aioRedactPII` IDB PII 마스킹(P173) · `_aioMaskKey/getApiKey/setApiKey`(P174) · `AIO.diag.proxyHealth/retryStats/lastNaverHealth` · 테스트 T80~T83(83건)
- **v48.98 마일스톤**: Function Audit Infrastructure — `_aioPageBus` 단일 라우팅 허브(P175) · `_aioOnce`+`_aioGlobalRegistry` 멱등 가드(P176) · `_aioFiniteNum`+`_aioSafeDiv`(P177) · 테스트 T84~T88(88건)
- **v48.99 마일스톤**: Listener Hygiene — `aio:pageShown` 17건+`aio:liveQuotes` 18건 `_aioPageBus` 마이그(P178~P180) · aio-core 9건+aio-data 4건+index.html 22건 · 테스트 T89~T92(92건)
- **v49.0 마일스톤**: Critical Function Fortification — `applyDataSnapshot` 키별 try-catch(P181) · `_aioLRU`+scoreItem/tickerRegex 캐시 cap(P182) · Fund Valuation Infinity 가드(P183) · 테스트 T93~T97(97건)
- **v49.3 마일스톤**: Architecture Reinforcement — DataQuality · NewsImpactVector · AIInfraHeat · PortfolioTechnicalRisk(P190) · OHLCV quality bundle · technical brief data confidence · T108~T115 tests
- **v49.20 마일스톤**: KR 5-Page Freshness Audit 편입 — kr-home/kr-supply/kr-macro/kr-technical DOM stale 11건 제거(archive 마킹 포함) · `CRITICAL_PAGE_GROUPS.krMarket` · `AIO.getCriticalKrPageFreshnessAudit()` · stale regex KR 토큰 추가 · T177~T179(P209+P211)
- **v49.49 21 페이지 매트릭스 완성 + R101 버그 fix + R102 휴리스틱 보강**: 사용자 "마저 못 한 라이브 점검과 작업들도 진행" 요청. KR 5 페이지(kr-home 6/kr-supply 4/kr-themes 3/kr-macro 6/kr-technical 5) + guide 5 subSections enumerate + audit 6축 객체 + findings 누적. Chrome MCP 라이브 cell-level 검증: kr-home 32/0, kr-macro 23/0, kr-technical 5/0 (P319 R102 보강 후). **P319 R101 버그 fix**: `js/aio-data.js` L8774 `window.LIVE_SYMBOLS = LIVE_SYMBOLS;` 노출 — top-level const가 window property 아니라 R101 audit이 false positive 131건 보고하던 버그. **P319 R102 보강**: placeholder 휴리스틱 false positive 차단 — `text.length >= 25`면 본문성 텍스트 제외 + 시작 매칭(`^로딩`/`^계산\s*중`)으로 변경 (kr-semi-export-yoy "+157.9% YoY (...대기)" false positive 해소). PAGE_SEQUENTIAL_AUDIT_REGISTRY total subSections **128 → 157** (+29). **21 페이지 모두 1차+2차 enumerate 완료 ✓** (home/signal/breadth/sentiment/briefing/technical/macro/fxbond/fundamental/themes/theme-detail/portfolio/ticker/market-news/options/kr-home/kr-supply/kr-themes/kr-macro/kr-technical/guide). T355~T362 (Group52) 8 신규. P319.
- **v49.48 정직 보강 (R75 일반화 + R101 신규 + R102 신규 + 5 페이지 1차+2차)**: 사용자 "근본 수정 + 재발 방지도 같이 한거지? 또한 전체 페이지에서 모든 내용과 데이터 세밀하게 쪼개서 확인한거지?" 지적 정직 답변. Chrome MCP 라이브 v49.47 진단 결과 부족 영역 3개 명확 — (1) R75 jensen-hardcoded hook 일반화 미완성, (2) R101 LIVE_SYMBOLS coverage audit 부재, (3) cell-level audit 함수 부재. **A1/P316/R75 보강**: `_aioStaticContentLifecycleHook` 일반화 + `_aioPageBus` 모든 페이지 자동 호출 + briefing hook jensen-hardcoded 제거 + index.html briefing-week-may `data-lifecycle-id` 마커 추가 (briefing-week-may-4-10/kr-export-2026-02 등 모든 LIFECYCLE 항목 자동 갱신). **A2/P317/R101 신규**: `AIO.getLiveSymbolsCoverageAudit()` — DOM `[data-live-price]` ticker vs LIVE_SYMBOLS 자동 정합 검증 (P315 재발 방지). **A3/P318/R102 신규**: `AIO.getCellLevelDataAudit(pageId)` — 페이지 cell-level 값/색상/snap-key/live-key/threshold-key 자동 캡쳐 + placeholder 분류. **B**: Chrome MCP 라이브 cell-level 검증: fxbond 42 cells/0 placeholder ✓, options 16/0 ✓, theme-detail 3/1(XSD 캐시 stale). **C**: theme-detail/portfolio/ticker/market-news/options 5 페이지 1차+2차 subSections enumerate + auditStatus 6축 객체 + findings. **getAutoOpsReadiness 26→27축** (liveSymbolsCoverage 통합). PAGE_SEQUENTIAL_AUDIT_REGISTRY total subSections **100 → 128** (+28). T346~T354 (Group51) 9 신규. P316~P318. R75 보강 + R101/R102 신규.
- **v49.47 라이브 21 페이지 정밀 조사 + R97 14건 시드 + Jensen 마킹 + fxbond/fundamental/themes 1차+2차**: 사용자 "지금 브라우저 사이트 연결된김에 각각의 페이지 전체 데이터 하나하나씩 정합성/최신성/로직성 세밀하게 조사" 요청. Chrome MCP로 라이브 v49.46 21 페이지 직접 캡쳐 + R95~R99 audit 일괄 실행. **R96/R98 v2/R99 모두 ok**, **R97 14건 미시드** + **Jensen 34일 overdue** + **sentiment 3 VIX 기간구조 placeholder + theme-detail XSD placeholder** 발견. **A1/P313/R74 보강**: R97 audit aliasMap 14 entries 추가 (`wage-growth→usWageGrowth` 등) + DATA_SNAPSHOT에 `hySpread:289 / tnx2y:4.28 / vkospiPct:-1.20 / krPpi:1.5 / krManufPmi:51.5` 5 시드 신설. **A2/P314/R75 보강**: briefing pageShown hook에 `STATIC_CONTENT_LIFECYCLE.getStatus` 동적 갱신 추가 — `#jensen-interview-stale-days` span에 archiveDue/replaceDue 자동 amber/red 마킹. v49.42 P304가 정적 텍스트만 제거하고 동적 갱신 hook 누락한 갭 시정. **C2/P315**: theme-detail XSD ticker LIVE_SYMBOLS 등록. **B**: fxbond 12 + fundamental 5 + themes 8 subSections + auditStatus 6축 + verify-only findings. PAGE_SEQUENTIAL_AUDIT_REGISTRY total subSections **75 → 100** (+25). T338~T345 (Group50) 8 신규. P313~P315.
- **v49.45 R100 신규**: API 키 저장 시스템 근본 보강 — 사용자 보고 "API 키 모두 날라갔다"의 진짜 메커니즘 진단 (P310/P311 cascading + 캐시 클리어 시 localStorage 손실). **P312**: 단일 저장소(localStorage) + 백업/복원 UX 부재 → R100 3중 안전망 신설. **신규 함수 7개**: `_aioIdbBackupKeys/_aioIdbRestoreKeys` IndexedDB I/O, `_aioCollectKeySnapshot` 11 키 수집, `_aioAutoBackupKeys` 자동 mirror (`_saveApiKey` 호출 시 + 5분마다), `AIO.exportApiKeys({masked})` JSON 다운로드, `AIO.importApiKeys(json)` 복원, `AIO.recoverApiKeysFromIdb()` 자동 복원. **사용자 운영**: 키 입력 후 `AIO.exportApiKeys({masked:false})` 백업 권장. 캐시 클리어 후 `AIO.recoverApiKeysFromIdb()` 자동 복원. **R98 false positive 4건** (aio-core.js r/d/total) 직접 verify 결과 모두 nested function 추적 부정확 — 진짜 충돌 아님. R98 휴리스틱 정확도 보강은 v49.46. P312 + R100.
- **v49.44 CRITICAL HOTFIX**: aio-data.js `refreshHomeDashboard()` const+var ld hoist 충돌 SyntaxError → 전체 파일 parse 실패 → 데이터 파이프라인 마비 시정. **사용자 보고 "데이터 다 안 보임 + 정밀 조사 필요"** → Chrome MCP로 라이브 사이트 콘솔 진단 → `Uncaught SyntaxError: Identifier 'ld' has already been declared` + cascading `_tcLoadFromStorage`/`refreshHomeDashboard`/`computeNewsSentimentScore is not defined` (모두 aio-data.js 안). **P311 근본 원인**: `refreshHomeDashboard()` L10989 함수 top `const ld = window._liveData || {};` + L11085 try block 안 `var ld = window._liveData || {};` → JavaScript `var`는 function-scoped + hoisted → 같은 scope에서 const/var 동일 이름 충돌 → SyntaxError → aio-data.js 전체 parse 실패. **시정**: L11085 `var ld` 라인 삭제 (outer const ld 그대로 사용). **재발 방지 R98 신규**: `AIO.getVarHoistConflictAudit()` — JS 파일별 같은 함수 내 var+const/let 동일 이름 자동 탐지 (휴리스틱 95% 정확도, P311 패턴 100% 탐지). **재발 방지 R99 신규**: `AIO.getShellAssetIntegrityAudit()` — `sw.js` SHELL_ASSETS 각 자산 200 OK 검증 (P310 manifest.json 누락 같은 차단). 라이브 검증: v49.44 + 321 라이브 데이터 키 + SPX 7,401 (live:yahoo) + VIX 18.36. P311 + R98/R99.
- **v49.43 HOTFIX**: manifest.json GitHub UI 삭제 → SW shell cache 404 차단 — 사용자 보고 "데이터 다 안 보임 + API 키 날아감" critical issue 진단. v49.42 push(`206c795`) 직후 사용자가 GitHub UI에서 23 파일 일괄 삭제(9628942 등) 중 `manifest.json` 삭제가 sw.js L18 `SHELL_ASSETS = ['./manifest.json', ...]` cache.add 404 발생 + index.html L22 `<link rel="manifest">` 콘솔 404 에러 야기. **P310 시정**: index.html L22 manifest link 주석 처리 + sw.js SHELL_ASSETS에서 manifest.json 제거 + SW_VERSION v49.42→v49.43 강제 회전 (모든 클라이언트 신규 캐시 빌드 + 이전 v49.42 캐시 폐기). API 키 자동 삭제 코드 0건 검증 — "키 날아감"은 사용자 캐시 클리어/시크릿 모드 등 추정. 사용자 안내: Ctrl+Shift+R 강력 새로고침 + `AIO.forceRefreshAllData()` 콘솔 명령. P310.
- **v49.42 마일스톤**: sentiment/briefing/technical/macro 1차+2차 일괄 점검 + 4 페이지 47 subSections — 사용자 "일단 전체 1차+2차 먼저 진행하자. 다음 진행해줘" 요청. v49.40 P294 / v49.41 패턴(agent 보고 verify → 진짜 issue만 시정) 그대로 적용. Explore agent 2 병렬 보고 CRITICAL 14건 verify → **false alarm 10건** (`_aioRenderSentimentConclusion`/`sent-overall-badge`/`sent-analysis-text`/`fg-needle`/`pc-needle-pos`/`briefing-action ACTION_RULES`/THRESHOLD_REGISTRY/`retail-sales` 등 FRED 동적/`generateMacroStoryline`/온도계 — 모두 코드 이미 구현됨) + **진짜 issue 4건**. (A) **sentiment 페이지 1차+2차**: 12 subSection enumerate + auditStatus 6축 객체 (모두 ok) + 5 verify-only finding (인프라 완성도 우수, 시정 0건). (B) **briefing 페이지 1차+2차**: 12 subSection + auditStatus 6축 (최신성 mostly-ok). **P302/R76 보강**: L5931 "호르무즈/대만 해협 모니터링" 정치/지명 토큰 → "주요 해상 물류 경로(호르무즈/대만 해협 등) 모니터링" 일반화. **P304**: L6060 Jensen 인터뷰 정적 텍스트 "📦 ARCHIVE · 58일 경과 (60일 임박)" → "📦 ARCHIVE" + `#jensen-interview-stale-days` 동적 span 단독 (STATIC_CONTENT_LIFECYCLE.jensen-interview-202603 archiveAfterDays:30/replaceAfterDays:60 자동 계산). **P305 verify-only**: briefing-action ACTION_RULES hook aio-core.js L1485~1499 완전 구현 검증. (C) **technical 페이지 1차+2차**: 11 subSection + auditStatus 6축 (모두 ok). **P306/R94 보강**: L6512 RSI(14) 카드에 `data-threshold-key="RSI"` 마커 부착 — 기존 title 텍스트 인라인이 R94 `getInlineThresholdTableAudit`로 자동 정합 검증 가능. (D) **macro 페이지 1차+2차**: 12 subSection + auditStatus 6축 (모두 ok). 5 verify-only finding (FRED 동적/SCENARIO_REGISTRY/R81 nextUpdate/generateMacroStoryline/온도계 모두 검증 OK) + 1 minor deferred (P307 Phase 5 2024 "연착륙" 라벨 → v49.43). PAGE_SEQUENTIAL_AUDIT_REGISTRY total subSection 28 (v49.41) → **75** (v49.42, +47). P302~P309 + R76/R94 보강. T330~T337 (Group49) 8 신규.
- **v49.41 마일스톤**: signal/breadth 2차 깊이 점검 + 실 시정 + R97 신규 — 사용자 "이처럼 제발 빼먹지 말고 모든 작업 완벽하게 진행하자. 순차적으로 계속 진행해" 요청. v49.40 정직 검증 패턴 (agent 보고 → 직접 verify) 그대로 적용. Explore agent 보고 CRITICAL 13건 verify 결과 **false alarm 9건** (`AIO_SCORE_SCALES`/`diagnoseBreadthConsensus` 바인딩/`bp-*-chart` 렌더러/`rally-quality-verdict`/`exit-spx-level` 등 이미 구현) + **진짜 issue 7건** (4 signal + 3 breadth). signal: (A1/P295/R73 위반) `AIO_SCENARIO_REGISTRY.signalShortTerm` 신설 (optimistic 32.5% / base 42.5% / pessimistic 17.5%) + `_aioPageBus signal pageShown` hook + index.html L5195~5224 `data-scenario-key` 마커 3 → 정적 인라인 확률 동적 연동. "호르무즈 재개"/"사우디 피격" 잔존 정치 토큰 일반화 · (A2/P296/R77 보강) `MACRO_CALENDAR.releases`에 `us-fomc` + `us-fed-rate` 2 entries 추가 (lastRelease 2026-04-29 / nextRelease 2026-06-17) + index.html L4910 `#cp2-fed-rate-meta` D-day dynamic 표시 · (A3/P297) `updateExitTriggers` 호출 보장 검증 (L22675 refreshSignal + L22927 aio:liveQuotes 두 곳 OK — verify-only) · (A4/P298) L5185 + L22485 "브레드쓰 스러스트 (Breadth Thrust)" 영문 병기. breadth: (B1/P299/R74 보강) `DATA_SNAPSHOT`에 `breadth5sma:68 / breadth20sma:75 / breadth50sma:46 / breadth200sma:55` 4 시드 추가 (이전 `_fallback`+인라인 폴백만 의존 → R74 silent pass) · (B2/P300) `index.html` L5471~5484 McClellan 카드 라벨을 "McClellan Summation Index (장기)"로 명확화 + 설명에 Oscillator(단기 ±100) 구분 + 베어 다이버전스 = SPX 신고가 vs Summation 미발동 정의 + `data-mcclellan-signal` 마커. **R97 신규** `AIO.getStaticSeedFallbackAudit()` — 페이지 `[data-snap]` 키 vs `DATA_SNAPSHOT` 시드 자동 정합 검증 (kebab→camel/snake 매핑) · getAutoOpsReadiness 25→26축 통합 · signal `findings` 4 entries + breadth `findings` 3 entries + `auditStatus` 6축 객체 전환 (partial → mostly-ok/ok 매트릭스) · T322~T329 (Group48) + 320→329
- **v49.40 마일스톤**: home 3차 실 점검 + 실 시정 — 사용자 "근데 Home 3차를 빨리 점검했던데 완벽히 한 거지?" 정직한 시정 대응. v49.39가 R95/R96 함수만 정의하고 실제 home 페이지에 audit 실행/시정 안 한 갭 인정. 실 검증 4 영역 진행: (1) **R96 실 검증** — home 7 data-action 핸들러 (`_aioHideParentOnboard`/`_aioRefreshActionPlan`/`_aioScrollApiSection`/`_aioToggleExplain`/`showPage`/`toggleGmoExpand`/`toggleLLM`) 각각 window 정의 위치 확인. **P294 CRITICAL 발견**: `_aioRefreshActionPlan`은 정의 없이 v49.39 knownAliases로 false-positive 통과 — index.html L4063 ↻ 갱신 버튼 click 시 silent no-op · (2) **R95 실 검증** — home 9 ticker (^GSPC/^IXIC/^VIX/^TNX/GC=F/CL=F/DX-Y.NYB/^KS11/BTC-USD) 모두 LIVE_SYMBOLS 등록 확인 + 다른 페이지(signal/sentiment/options/technical/macro/fxbond/kr-home/kr-technical)와의 분포 매핑. fetchLiveQuotes bulk update가 `[data-live-price="T"]` 동시 갱신 보장 OK · (3) **P294 시정**: `window._aioRefreshActionPlan` 신설 — ACTION_RULES 재계산 + home-action-position/sentiment/breadth 3 sink 동기 갱신 + data-refreshed-at 타임스탬프. R96 knownAliases에서 `_aioRefreshActionPlan` 제거 (이제 has_aio로 통과) · (4) **home findings** 5 → 8 entries (P294 시정 + R95/R96 실 검증 결과 추가, verifiedIn 마커) · auditStatus 로직성 'mostly-ok' → 'ok' · P294 + T321
- **v49.39 마일스톤**: home 3차 + signal/breadth 1차 점검 — 사용자 "Home 3차와 Signal+Breadth 1차 이렇게 가능?" 요청. 3개 작업 묶음. (1) **R95 신규** `AIO.getCrossPageIndicatorConsistencyAudit()` — `[data-live-price]` ticker별 그룹화 → 페이지 간 동일 ticker가 다른 텍스트 보일 시 mismatch 보고. placeholder(—/loading) 제외 · (2) **R96 신규** `AIO.getDataActionHandlerAudit()` — 모든 `[data-action]` NAME이 `window` / `AIO` 네임스페이스 / `_aio*` 접두 / known alias (showPage/toggleLLM/runInstitutionalTechnicalBrief 등) 등록 확인. 미정의 핸들러 click 무동작 자동 탐지 · (3) **signal 14 subSection enumerate**: purpose-header / insight-box / lockout-control / explain-page / 20pt-scoring / 2pct-rule / atr-stop / entry-exit / trading-setups / pyramiding / spx-tech-dash / breadth-consensus / macro-scenario / exit-triggers · (4) **breadth 12 subSection enumerate**: insight-box / explain-page / definition / narrow-vs-broad / sma-cards / consensus-readout / static-diagnose / mcclellan / weinstein / nhnl / ad-line / divergence · auditStatus 'partial' (2차 → v49.40) · getAutoOpsReadiness 23→25축 · R95~R96 + P290~P293 + T313~T320
- **v49.38 마일스톤**: home 페이지 2차 깊이 점검 + 시정 — 사용자 "Home 1차 점검이면 2차/3차도 있는거야?" 지적 대응. v49.37 1차 (subSection enumerate)에서 발견 못한 깊이 결함을 실제 위→아래 점검으로 식별. (1) **F1/P286 (R56 위반)**: home L4222 VIX 인라인 표 5 구간(12/20/30/45/∞) vs THRESHOLD_REGISTRY.VIX 6 구간 불일치 + 라벨 "패닉 진입" vs "공포" 등 모순 → 표 6 구간 갱신 + `data-threshold-table="VIX"` 마커 부착 · (2) **F2/P287**: L4224 오타 `뷰블` → `버블` · (3) **F3/P288 (R56 보강)**: THRESHOLD_REGISTRY에 **DXY** (5 bands: < 95/100/105/110/∞) + **YIELD_10Y** (5 bands: < 3/4/4.5/5/∞) 추가 · (4) **F4/P289 (R93 보강)**: home subSections 1차 8개 → 2차 **15개** 재 enumerate (스코어 범례/conclusion-bar/KPI 4 카드/서브 지표 chips/상단 펼쳐보기 표/GMO 해설 추가) + `findings[]` 배열 누적 (5 점검 결과 + fixedIn 추적) · (5) **R94 신규**: `AIO.getInlineThresholdTableAudit()` — `[data-threshold-table]` 마커 표를 REGISTRY와 자동 정합 검증 · getAutoOpsReadiness 22→23축 · P286~P289 + T305~T312
- **v49.37 마일스톤**: 페이지 sequential audit 인프라 + home 1차 점검 — 사용자 "각 페이지 모든 내용 위→아래 하나하나 6축 점검?" 솔직 인정 (이전 audit은 line range/keyword grep만, 실제 sub-section 6축 매트릭스 미실행). (1) **AIO_PAGE_SEQUENTIAL_AUDIT_REGISTRY** 신설 — 21 페이지 × `subSections[{id, order, topic, lines}]` × axes 6 (최신성/정확성/정합성/로직성/직관성/핵심성) 매트릭스 · (2) `AIO.getPageSequentialAuditStatus()` — pending/partial/done 추적 · (3) **home 페이지 1차 sequential 점검**: 8 subSection enumerate (버전 배지 / 상단 스냅 그리드 / 3 카드 / Action Item / 심층 해설 / 리스크 레이더 / F&G+CNN / GMO 표) · (4) **발견 P283 시정**: `live-quote-ts-topbar` JS hook 부재 → `aio-data.js` fetchLiveQuotes 성공/실패 분기에 동기 갱신 추가 (영구 placeholder 잔존 차단) · (5) home L3961 버전 배지 v49.36 → v49.37 갱신 · (6) 빠른 이동 7 chips 페이지 ID 정합 검증 OK · R93 + P282~P285 + T299~T304 · **v49.38~42 잔존**: 20 페이지 sub-section enumerate + 6축 점검 (target: 1000+ 매트릭스 항목)
- **v49.36 마일스톤**: fundamental 페이지 15 기준 100% 커버 (잔존 Roadmap 일괄 시정) — 사용자 "이번 세션 남은 작업들 순차적으로 모두 진행" 요청. v49.35 Roadmap 7 신규 함수 + v49.34 Roadmap CIK_MAP/8-K/FMP segments 일괄 처리. (1) **7 신규 compute/fetch 함수**: `computeFcfYield` (FCF/시총 — Naver+Yahoo mcap, verdict: attractive/fair/low) · `computeBalanceSheetRatios` (Net Debt/EBITDA + Interest Coverage, healthScore: strong/caution) · `computeEvEbitda` (mcap+netDebt EV + sector peer count, verdict: cheap/fair/expensive) · `computeMacroBeta` (SCREENER_DB sector → 11 sector heuristic table, rateBeta/dxyBeta/oilBeta + diversificationVerdict) · `fetchFinnhubInsider` (12주 임원 매수/매도, netShares + verdict: insider-buying/selling/neutral) · `fetchFinnhubShortInterest` (/stock/metric, verdict: normal/elevated/squeeze-candidate) · `fetchSEC13F` (SEC EDGAR full-text + WhaleWisdom URL — AI URL fetch) · (2) **보조 v49.34 Roadmap**: `fetchSECRecentFilings` (8-K event-driven URL) · `fetchFMPSegments` (FMP key 의존) · **CIK_MAP 18 → 50+** (BAC/V/MA/JNJ/PFE/UNH/WMT/PG/KO/XOM/CVX/DIS/MCD/HD/CRM/ORCL/COIN/BRK.B 등 32 추가) · (3) FUNDAMENTAL_PAGE_CRITERIA 7 implFn 갱신 (null → 함수 매핑) · (4) **페이지 L8175 가용성 배지 갱신** ❌4/⚠5 → ✓ 모두 (Moat/Industry Rank 2개만 유료 대체 명시) · 커버리지 박스 40% → **93% (14/15)** · R92 + P278~P281 + T291~T298
- **v49.35 마일스톤**: fundamental 페이지 15 기준 registry + 가용성 가시화 — 사용자 "기업 분석 페이지에 있는 15개의 분석 기준도 세밀하게 쪼개서 조사. 모든 보강 작업은 근본 수정+재발 방지 같이" 지적 대응. **Audit 발견: 3개의 별개 "15기준" 시스템 공존** (v49.25 FUNDAMENTAL_CRITERIA / v49.34 ANALYSIS_FRAMEWORK / 페이지 인라인). 페이지 L8175 인라인 텍스트가 "15개 분석 관점"을 자랑하나 실제 구현은 6/15(40%). (1) `AIO_FUNDAMENTAL_PAGE_CRITERIA` 신설 — 15 entries 각각 `{num:1~15, label, description, dataSource, implFn, plannedFn, requires, frequency, hallucinationRisk}` 등록 (Quality/Moat/Growth/Margin/FCF/Balance/PE/EV/Insider/13F/Short/Revisions/Beat/Industry/Macro) · (2) `AIO.getFundamentalPageCriteriaAudit()` — coveragePct + highRiskCount · (3) `AIO.getCriteriaCrossReferenceAudit()` — 3개 "15기준" registry 비교 안내 · (4) 페이지 L8175~8189 각 기준 옆 인라인 가용성 배지 (✓ 6 / ⚠ 5 / ❌ 4) 추가 + 현재 커버리지 요약 박스 · (5) chat ABSOLUTE RULES 6조 — 미구현 4 기준 학습 데이터 환각 금지 + "수동 확인 권장" 답변 강제 · getAutoOpsReadiness 21→22축 · R91 + P275~P277 + T285~T290 · v49.36 잔존: computeFcfYield/BalanceSheetRatios/EvEbitda/MacroBeta + fetchFinnhubInsider/SEC13F/FinnhubShortInterest 7 함수 신설 → 15/15 (100%) 목표
- **v49.34 마일스톤**: 종목/기업 15 분석 분야 정성 출처 통합 — 사용자 "비즈니스 구조/사업 모델/수익 구조/제품 포트폴리오/CEO 경영진/밸류에이션/협력/공급망/TAM/리스크/경쟁/투자포인트 15개 분석 기법 모두 최신/정확하게 + API/소스 커버 가능?" 지적 대응. **Audit 결과: 9/15 AI 학습 의존 (환각 위험)** → 무료 API 통합으로 시정. (1) `AIO_ANALYSIS_FRAMEWORK_REGISTRY` 신설 — 15 fields 각각 `{label, type:정량/정성/시각, primarySource, implFn, freshness, aiHallucinationRisk:low/medium/high}` 등록 · (2) `AIO.fetchSECBusinessDescription(ticker)` 신설 — SEC EDGAR data.sec.gov + 18 CIK_MAP 메가캡 → 10-K filing URL + SIC + filingDate (Item 1 Business + Item 1A Risk Factors 인용 가능) · (3) `AIO.fetchSECRiskFactors(ticker)` — Item 1A 가이드 · (4) `AIO.fetchWikipediaCompany(ticker)` 신설 — en.wikipedia.org/w/api.php (CORS 지원) intro 2000자 fetch (CEO/제품/연혁/경쟁사) · (5) `AIO.getAnalysisFrameworkCoverageAudit()` + `AIO.assertAnalysisFrameworkCoverage(ticker)` async (종목별 15 분야 가용성 매트릭스) · (6) `_fetchTickerDataForChat` SEC + Wikipedia 병렬 fetch + system 프롬프트 `[SEC 10-K]` / `[Wikipedia]` 라벨 주입 · (7) ABSOLUTE RULES 5조 추가 — "15 분야 출처가 데이터 블록에 없으면 '검증된 데이터 없음' 답변, 학습 데이터 채우기 금지" · getAutoOpsReadiness 20→21축 · R90 신규 + P272~P274 + T279~T284
- **v49.33 마일스톤**: 미룬 작업 일괄 시정 + 채팅 자동 검증 통합 — 사용자 "다른 세션이나 범위 이전 미룬 작업 포함 진행" 요청 대응. (1) **chatSend 응답 자동 검증** — aio-chat.js L3162 `_srcBadge` 다음에 `_accBadge` 추가, assertChatResponseAccuracy + getChatHallucinationAudit 자동 호출 + 가시 배지 (`✓ 가격 정확성` / `⚠ 가격 괴리 high` / `🧠 환각 X/10` / `🧠 클린`) (P269/R89) · (2) **TICKER_NAME_REGISTRY KR 17 종목 등록** — 005930.KS(삼성전자), 000660.KS(SK하이닉스), 005380.KS(현대차), 373220.KS(LGES), 035720.KS(카카오), 035420.KS(NAVER), 207940.KS(삼성바이오), 051910.KS(LG화학), 006400.KS(삼성SDI), 003670.KS(포스코퓨처엠), 012450.KS(한화에어로), 042660.KS(한화오션), 034730.KS(SK), 003550.KS(LG), 011200.KS(HMM), 247540.KQ(에코프로비엠), 086520.KQ(에코프로) — 한자/한글/별명/티커 모두 매핑 (P270) · (3) **15 fundamental criteria implFn 매핑 보강** — 4/15→13/15 구현 (coverage 27%→87%): Naver/Finnhub/Yahoo/SCREENER_DB 기존 함수 매핑, PEG/Insider만 v49.34 잔존 (P271) · (4) **VKOSPI 45.00→17.80** DATA_SNAPSHOT 정합 + `data-snap="vkospi"` 바인딩 + kr-technical 위젯 정합 (R74 보강) · R89 신규 + P269~P271 + T273~T278
- **v49.32 마일스톤**: AI 채팅 종목/기업 데이터 정확성 근본 보강 — 사용자 지적 "퀄컴 주가 150 환각 + 검증/재발 방지 레이어 부재" 시정. (1) chat L54 "147-150" 정량 임계값 일반화 + `AIO_NUMERIC_GUIDELINE_SAFELIST` 8 임계값 등록(B1/P262/R84) · (2) `_fetchTickerDataForChat` HARD GUARDRAIL — 4-line 절대 금지/허용 답변/허용 분석 + ABSOLUTE RULES 4조항(B2/P263/R82) · (3) `dynamicTickerLookup` timeout 12s + 3 프록시(codetabs 추가) + 1회 retry(B4/P265) · (4) `AIO_TICKER_NAME_REGISTRY` 30 메가캡 (NVDA/QCOM/MSFT/...JPM/BAC/WMT 등) + `resolveTickerFromAnyName()` 한글/영문/별명/한자(B5/P266/R85) · (5) `AIO.assertChatResponseAccuracy()` post-hoc 검증 — 응답 $\\d+ 패턴 추출 + ±20% 괴리 차단(P264/R83) · (6) `AIO.getChatHallucinationAudit()` 4 환각 패턴 (라운드/정확 소수/불확실 표현/stale 시점)(P264/R86) · (7) `AIO.assertChatPriceFetchHealth()` 프록시 체인 + 캐시 사이즈(P265) · [확장] `AIO.assertTickerDataIntegrity()` 종목별 6채널(시세/추세/컨센서스/어닝/Naver/메모) 통합(P267/R87) · `AIO_FUNDAMENTAL_CRITERIA` 15 기준 매핑 + `getFundamentalCriteriaAudit()`(P268/R88) · getAutoOpsReadiness 13→20축 · R82~R88 신규 7건 · T259~T272 (14개)
- **v49.31 마일스톤**: Roadmap HIGH 5건 + 지정학 시나리오 인프라 — H1 SCREENER_DB_META 메타 신설 (P257/R80 — 메모 lastBulkUpdate + staleAfterDays/replaceAfterDays + window 노출) · H2 fxbond 2Y `data-snap="tnx-2y"` + `data-live-price="^IRX"` 바인딩 (P258) · H3 **AIO_GEOPOLITICAL_CONTEXT_REGISTRY** 신설 — 호르무즈/이란/대만/우크라/미중 5 시나리오 + `getGeopoliticalReviewAudit()` 14일 overdue 자동 보고 (P259/R79) · H4 FRED 차트 헤더 "다음 갱신: NFP 6/6 · CPI 6/12 · PCE 6/30" 표시 (P260/R81) · H5 themes "◀ 현재 Late Cycle" 정적 라벨 "Late (참고)" 일반화 + `data-cycle-phase` 속성 (P261) · getAutoOpsReadiness 12→13축 (geopolitical 통합) · R79~R81 신규 3건 · T251~T258
- **v49.30 마일스톤**: 전수 최신성 audit + 5 신규 재발 방지 인프라 — 사용자 지적 "각 페이지 모든 내용 최신성 점검 + 재발 방지 포함 근본 수정" 시정. (1) KOSPI/KOSDAQ/KRW 인라인 22% 괴리 정합 (F1/P252/R74 assertSnapshotInlineMatch — 핵심 sink 10개 자동 검증) · (2) Jensen 인터뷰 58일 archive 마킹 + AIO_STATIC_CONTENT_LIFECYCLE 신설 (F2/P253/R75) · (3) macro 유가 시나리오 일반화 + chat Bessent/Warsh 일반화 + AIO_NAMED_ENTITY_REGISTRY + AIO_MACRO_CALENDAR (F3/F4/P254/R76/R77) · (4) KR 반도체 수출 +157.9% 3곳 data-snap 바인딩 + AIO_KR_MACRO_RELEASE (F5/P255/R78) · (5) DATA_SNAPSHOT 거시지표 주석 갱신 (F6/F7) · getAutoOpsReadiness 7→12축 통합 · R74~R78 신규 5건 · T241~T250 · P252~P256
- **v49.29 마일스톤**: 나머지 11개 항목 페이지 적용 — Deep audit 잔여 항목 일괄 시정. (1) signal E3 페이지 목적 헤더 박스 · (2) breadth L2/L3 consensus readout (diagnoseBreadthConsensus 호출) + I1 20SMA 75% amber 색상 정정(R56 BREADTH band) · (3) briefing E4 5대 관전 포인트 최상단 배치 + E2 Action Item 카드 · (4) portfolio E5 4-card 리스크 대시보드(Sharpe/Beta/MDD/Drift) · (5) options E6 ACTION_RULES 기반 동적 추천 + VIX 구간별 전략 매칭 · (6) technical I4 OHLC fallback `data-aio-fallback` 마킹 · (7) fundamental I5 검색 전 가이드 + 예시 4개 (NVDA/AAPL/TSLA/MSFT) · (8) macro I6 placeholder 표준 가이드 · aio-core.js breadth/briefing/options 3개 pageShown listener 추가 · P245~P251 · T231~T240
- **v49.28 마일스톤**: 인프라 → 페이지 실제 적용 — v49.24~27이 재발 방지 인프라만 추가하고 실제 발견 문제를 페이지에 적용하지 않았다는 지적 시정. (1) signal L1 SCORE_SCALES 변환식 + L4 ATR_PRESETS 권장값 명시 · (2) technical L8 RSI 임계값 카드 · (3) home I2/I3 가중치 tooltip + CARD_HIERARCHY primary/secondary 적용 + E1 Action Item 카드 신설 · (4) fundamental L7 PIOTROSKI 자동 채점 가이드 · (5) macro L6 SCENARIO_REGISTRY lastUpdated/validateSum 표시 · (6) themes I7 cycle 동적 판정 readout · aio-data.js home 갱신 시 ACTION_RULES 자동 호출 · aio-core.js themes/macro pageShown listener · P239~P244 · T223~T230
- **v49.27 마일스톤**: 핵심성 정비 + Static→Dynamic 전환 — Deep audit E1~E7 + L6 근본 시정: (1) `AIO_ACTION_RULES` VIX/F&G 구간별 포지션 사이즈 + 행동 가이드 → E1/E2/R69 · (2) `AIO_PAGE_PURPOSE_REGISTRY` 12 페이지 목적/주요카드/우선순위 → E3/E4/R70 · (3) `AIO.getPagePurposeRatioAudit()` 이론 텍스트 vs 동적 sink 비율 → E5/R71 · (4) `AIO_SCENARIO_REGISTRY` 시나리오 확률+lastUpdated+source+triggers → L6/R72 · (5) `AIO.getScenarioFreshnessAudit()` 30일+ 자동 stale · R69~R72 신규 4건 · T215~T222 · P234~P238
- **v49.26 마일스톤**: 직관성 정비 + 재발 방지 인프라 — Deep audit I1~I8 중 7건 근본 시정: (1) `AIO_WEIGHT_REGISTRY` 점수 가중치 단일화 → I2/R64 · (2) `AIO_CARD_HIERARCHY` 시각 위계 (primary/secondary/tertiary) → I3/R65 · (3) `AIO.applyLabelToElement()` 라벨+색상+signal 일괄 적용 → I1 · (4) `AIO.getDuplicateContentAudit()` 중복 콘텐츠 자동 탐지 → I4/R66 · (5) `AIO.getCycleFromMacro()` 경기 사이클 동적 판정 → I7/R67 · (6) R68 placeholder 표준 → I5/I6 · R64~R68 신규 5건 · T207~T214 · P229~P233
- **v49.25 마일스톤**: 로직성 정비 + 재발 방지 인프라 — Deep audit L1~L8 중 6건 근본 시정: (1) `AIO_SCORE_SCALES` 20점/100점 단일 정의 + convert() → L1/R59 · (2) THRESHOLD_REGISTRY에 BREADTH·RSI band 추가 → L2/L8 · (3) `AIO_ATR_PRESETS` 매매 전략 권장값 단일화 → L4/R60 · (4) `AIO.diagnoseBreadthConsensus()` 다중 신호 가중 합의 알고리즘 → L3/R61 · (5) `AIO_PIOTROSKI_CHECKLIST` 9항목 auto-score 함수 → L7/R62 · (6) `AIO.getThresholdLabelAudit()` R56 인라인 라벨 자동 탐지 → R63 · R59~R63 신규 5건 · T199~T206 · P224~P228 (L6 Scenario는 v49.27 본격)
- **v49.24 마일스톤**: 근본 재발 방지 인프라 — v49.23 정합성 패치의 4개 근본 원인을 인프라로 해결: (1) `THRESHOLD_REGISTRY` 단일 라벨 출처(VIX/FG/HY/AAII/SKEW) → P219 재발 방지/R56 · (2) `getSnapshotConsistencyAudit()` cross-page sink 정합 검증 → P216/P218 재발 방지/R55 · (3) `getTableStaleAudit()` 정적 테이블 90일+ 자동 탐지 → P217 재발 방지/R57 · (4) DOM 인라인 vs DATA_SNAPSHOT 3-way 검증 → P213 재발 방지/R58 · getAutoOpsReadiness 5축→7축 통합 · R55~R58 신규 4건 · T193~T198 회귀 테스트 · P220~P223
- **v49.23 마일스톤**: 4축 Deep Content Audit + 정합성 시정 — 21페이지를 정합성/로직성/직관성/핵심성 4축으로 deep audit 후 CRITICAL 정합성 8건 즉시 시정: kr-technical 신용잔고 31.7→19.2조 data-snap(C1, P216) · kr-supply 주간 테이블 2024-03→2026-05(C2, P217) · KOSPI 카드 색상 정합(C3) · HY 289bps Complacent(C4, P219) · F&G ID 단일화(C5, P218) · VIX 18 공포→Risk-On(C6) · AAII 중정도 비관(C7) · F&G 7+2 컴포넌트(C8) · 기관 합산 -472억 정합 · T187~T192
- **v49.22 마일스톤**: Stale 토큰 전수 정리 — kr-home L10846 comment 제거(E-1A) · kr-home 6 snap-dates+DATA_SNAPSHOT 값 정합(E-1B, P213) · bokNext 5/28→5/29 정합(E-1C) · options 4 snap-dates 갱신(E-2, P214) · signal/kr-macro 이란 시나리오 일반화(E-3, P215) · briefing archive 마킹(E-5) · T183~T186
- **v49.21 마일스톤**: KR CHAT_CONTEXTS 신설 + archive 마킹 확장 — CHAT_CONTEXTS에 kr-macro/supply/themes/tech 4개 KR context system() 추가(P212) · kr-home-kosdaq-comment stale 제거(P210) · R54 data-aio-archive 원칙 · T180~T182 tests
- **v49.20 마일스톤**: KR 5페이지 Freshness Audit 편입 — KR DOM stale 11건 제거 · getCriticalKrPageFreshnessAudit() 신설 · CRITICAL_PAGE_GROUPS.krMarket · stale regex KR 토큰 추가(P209/P211) · T177~T179
- **v49.19 마일스톤**: AI Chat Context Freshness Cleanup — CHAT_CONTEXTS 시스템 프롬프트 내 2026-04-12~18 하드코딩 날짜·이란협상·Warsh 취임 등 stale 토큰 13건 제거 · `AIO.getChatContextFreshnessAudit()` 감사 API · T176 회귀 테스트(P208)
- **v49.18 마일스톤**: Deep 10-Page Content Cleanup — 종합 5개 + 시장 분석 5개 페이지의 실제 DOM 콘텐츠를 분리 스캔하고, 오래된 live-like 기본값/날짜/툴팁을 제거하며 아카이브 제외 감사와 T173~T175 회귀 테스트 추가(P207)
- **v49.17 마일스톤**: Critical 10-Page Freshness Audit — 종합 5개 + 시장 분석 5개 페이지를 별도 감사 대상으로 고정하고, FX/채권·거시·기업분석·기술·브리핑의 자동 refresh 심볼 커버리지 확장(P206)
- **v49.13 마일스톤**: Intrinsic Content Simplification Correction — additive decision cards/secondary badges 제거 · core view는 기존 상세/참고 콘텐츠 접기 중심 · T152~T156 재정의(P202)
- **v49.12 마일스톤**: Page Content Simplification UX — 21페이지 watch/decide/next 핵심 카드 · 핵심 보기 토글 · 상세/참고 보조 섹션 라벨 · T152~T156 tests(P201)
- **v49.11 마일스톤**: Auto-Ops Static Data Governance — `AIO.getStaticDataGovernanceAudit()` · `AIO.getAutoOpsReadiness()` · `AIO.forceRefreshAllData()` · T146~T151 tests(P200)
- **v49.10 마일스톤**: Blow-off Top / Event Exhaustion integration — CPI-confirmed context · H2 liquidity regime guardrail · Aether Telegram pipeline audit · T144~T145 tests(P199)
- **v49.9 마일스톤**: Live Site QA Cleanup — mobile watchlist control fit · ticker navigation event delegation · T143 runtime onclick regression test(P198)
- **v49.8 마일스톤**: Live Site Freshness Guardrail — HOME stale-event filter · 2026-05-13 fallback snapshot · T141~T142 freshness tests(P197)
- **v49.7 마일스톤**: Page Focus Brief UX — 페이지별 목적 · 3단계 활용 루틴 · 관련 페이지 동선 · 상세 해설 라벨 간소화(P194)
- **v49.6 마일스톤**: Full Static Fallback Data Refresh — US/KR 지수 · FX/DXY · oil · Cboe put-call · CNN/AAII sentiment fallback seed 최신화(P193)
- **v49.5 마일스톤**: Lockout Rally / OPEX Strategy Engine — 20MA ATR/ADR extension · terminal candle risk · OPEX gamma decay · IWM/RSP/KRE/XBI breadth rotation · final action ladder(P192) · T125~T132 tests
- **v49.4 마일스톤**: Data Freshness Governance — FRESHNESS_POLICY · SnapshotStore · auditAllFreshness(P191) · scheduler telemetry · static fallback WebSearch refresh · T116~T124 tests
- 스택: HTML5 + 인라인 CSS/JS · Chart.js(CDN) · AES-256 · GitHub Pages · 한국어 UI · 다크 테마 · WCAG AA

---

## 작업 유형별 읽을 파일

| 작업 | 읽을 파일 |
|------|----------|
| **index.html 수정** | `_context/CODE-MAP.md` → 해당 line 범위만 Read |
| **버그 수정** | `_context/RULES.md` → `BUG-POSTMORTEM.md` → `QA-CHECKLIST.md` |
| **새 기능** | `_context/RULES.md` → `_context/CODE-MAP.md` → `_context/WORKTREE-AUDIT.md`(워크트리/배포 영향 시) |
| **QA/점검** | `_context/RULES.md` → `BUG-POSTMORTEM.md` → `QA-CHECKLIST.md` |
| **자료 통합** | `/integrate` 스킬 (→ `CHANGELOG.md` + `_context/KNOWLEDGE-BASE.md` 환류) |
| **데이터 갱신** | `/data-refresh` 스킬 |
| **지식 린팅** | `/knowledge-lint` 스킬 |

상세 문서: `_context/CLAUDE.md` (파일 구조 · Hook · Commands↔Skills 매핑 · 복리 루프)

---

## 절대 규칙 (R1~R3만 — 나머지 R4~R53은 `_context/RULES.md`)

**R1. 버전 동기화 7곳**: title · badge · APP_VERSION · version.json · sw.js SW_VERSION · _context/CLAUDE.md · CHANGELOG.md
**R2. 버전 체계**: `v{major}.{patch}` 숫자 단조 증가 (예: v48.76 → v48.77). 최신 실제 체계는 두 자리 patch 허용.
**R3. 버그 수정 시 사후 분석**: `_context/BUG-POSTMORTEM.md`에 P번호 기록
**R27. Commands↔Skills 동기화**: 새 스킬 시 command wrapper 동시 생성

---

## 작업 규칙

- **자동 배포/커밋 금지** — `/deploy` 또는 "배포해줘" 명시 시에만
- **전체 재작성 금지** — CODE-MAP.md 기반 부분 패치만
- **코드 수정 시 자동 반영**: BUG-POSTMORTEM + QA-CHECKLIST + RULES + 버전 6곳 동기화

---

## 복리 루프 (Karpathy Second Brain)

```
작업 수행 → 산출물 → 위키(_context/) 환류 → 다음 작업이 더 정확
```

| 작업 | 환류 대상 |
|------|----------|
| 버그 수정 | BUG-POSTMORTEM → 3회 반복 시 RULES 승격 |
| /integrate | CHAT_CONTEXTS + SCREENER_DB + TECH_KW/MACRO_KW |
| /qa | QA-CHECKLIST 항목 추가 |
| 인사이트 | KNOWLEDGE-BASE (R26) |
| 리팩토링 ±500줄 | CODE-MAP 재스캔 |

에러 복리 방지: `/knowledge-lint` 주기적 실행 (주 1회+). 코드 확인 없이 추측 판단 금지.

---

## 토큰 효율성

- 인사/칭찬/마무리 멘트 금지 · 질문 되풀이 금지 — 바로 작업
- 요청 범위 외 제안/과잉 설계 금지
- index.html은 CODE-MAP 기반 부분 읽기 · 파일은 한 번만 읽기
- 모르면 솔직히 말하기 (경로/함수명 날조 금지)
