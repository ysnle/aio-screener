# 전체 요청 대조표 — 2026-09-09

이 세션의 원래 요청은 로딩 시간 절감에 한정되지 않는다. 페이지·콘텐츠·기능·시스템·알고리즘·함수의 품질과 구조를 실제 사용자 관점에서 검사하고, 필요한 재설계·구현·검증까지 수행하는 작업이다. 후속 AI 요청이 앞선 범위를 대체하지 않는다.

| 요청 | 구현·확인한 내용 | 아직 완료로 판정하지 않는 부분 |
|---|---|---|
| 성능 개선점 탐색과 실제 수정 | 중복 preload 제거, slice 구독, observation 캐시, 증분 hash와 협력적 hydration, 중복 준비 제거, immutable projection 재사용. `../performance-hunt-20260906/REPORT.md`에 전후 측정·fixture 기록. | 배포 CDN·WAN·실사용자 백분위 성능은 별도다. 추가 모듈 분리·theme DOM 축소는 측정에 따른 구조 개선 후보다. |
| 페이지·기능·함수의 구조적 품질 | SEC 회계기간/결측, 포트폴리오 동일 평가 모델, 뉴스 단어 경계, 비인과 관계 기본값, route 실패 정리/재시도, 동적 접근성 이름, guide 검색/초점, 학습 메모 보존, 차트 결측 안내를 수정했다. | 모든 함수·모든 가능한 사용자 입력의 무결성을 인증한 것은 아니다. 최신 전체 QA 결과를 별도 기록한다. |
| 실제 사용자 관점 점검 | 브라우저에서 라우트·검색·필터·상세·학습·13F·AI 패널을 검사하고 재현한 결함을 수정했다. 자동 route 검사와 직접 의미 검토는 구분한다. | 외부 공급자 장애의 모든 조합, 실제 개인 포트폴리오/키/저장 기록을 이용한 검증은 수행하지 않는다. |
| 콘텐츠 의미·품질 | 시장 설명·시점·출처 표시를 교정하고 교육 반복 문장을 제거했다. 현재 자료와 배경 reference를 분리하며 원문 개념 연결과 단어 검색 후보를 구분한다. | 160개 교육 원문의 독립 심층 집필·원문 직접성·계산 검토는 미완료다. 구조/중복 검사 PASS는 내용 인증이 아니다. |
| 근본 재설계 검토 및 적용 | 도메인 단일 계산, 실제 근거 바인딩, 명시적인 결측/권한/수명주기, producer 원자적 쓰기, AI 공통 응답·분석 연결을 적용한다. | 더 큰 코드 분할 자체를 품질 향상으로 간주하지 않는다. 파생 정량 분석·복합 의도 합성은 별도 인수조건이 필요하다. |
| 제공한 작업 환경 가이드 적용 | 확인한 4개 자료를 작업 범위·필요한 참조만 읽기·중복 hook 실행 제거·생성 profile/skill 동기화에 반영했다. `ASTRA-GUIDANCE-PROPOSAL.md`에 실제 적용 범위 기록. | 전역 플러그인 일괄 삭제·실행 중인 앱 번들 교체는 적용하지 않았다. 과거 자료의 권장 CLI 버전과 설치 버전 차이는 당시 확인 사실로만 남긴다. |
| 스킬에 얽매이지 않는 객관적 작업 | 키워드만으로 전체 절차를 실행하지 않도록 지침을 고쳤다. 사용자 지시와 실제 결함·측정·사용 동선에서 필요한 작업을 선택한다. | 필수 회귀 검사와 commit/push/deploy 경계를 없애는 요청으로 해석하지 않는다. |
| 파일 변경을 실제로 확인 가능하게 제공 | 작업 기준선 hash와 비교한 `CHANGED-FILES.md`, `changed-files.json`, 실제 `git diff`를 제공한다. | 앱의 변경 패널 표시 여부는 저장소 파일 수정 여부와 별개다. queued UI 요청을 화면 표시 완료로 말하지 않는다. |
| AI 전체 요소 심층 점검·보강 | provider/Worker timeout·SSE·중지·기록·초점, 출처 등급/독립성, ClaimLedger 바인딩, 검색 인덱스, 질문 전제와 분석 호출을 검사·수정한다. | 실제 유료 모델 답변, 공개 Worker 활성화, A/B·blind benchmark·다중 턴 효능은 미검증이다. |
| AI 초기 설계 의도·전체 연결 확인 | `AI-DESIGN-AUDIT.md`에 초기 Evidence-bound Analyst 목표와 후속 정책, 현재 코드 연결을 대조했다. 과거 진단을 현재 결함으로 복사하지 않았다. | 부분 구현과 기능 존재만으로 완성/우위를 주장하지 않는다. |
| 모델 역할 분담 | Astra: 설계·복잡한 원인 분석·리뷰·통합 검증. Luna MAX: 명확히 분리된 구현·반복 수정·회귀 테스트. 필요하면 어려운 코드는 Astra가 직접 담당한다. | 이전 상속 작업자를 Luna로 소급하여 기록하지 않는다. 현재 UI 통합은 실제 `gpt-5.6-luna`, `max`로 시작했다. |
| 중간 답변 후에도 작업 지속 | 질문은 진행 중 작업의 조정으로 반영하며 발견한 관련 실패까지 수정한다. | 미검증/미완료 항목은 숨기지 않는다. 완료 선언은 증거가 있는 범위에만 적용한다. |

최종 검증 현황은 `REPORT.md`에 집계한다. 자동 검사·격리 runtime·직접 브라우저·외부 live 결과는 각각 구분한다. 이 세션에서는 commit, push, deploy를 수행하지 않았다.
